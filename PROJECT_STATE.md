# PROJECT STATE — Zayjar Restaurant SaaS Platform

> **Phase 3 — Eleva Builder / Preorder Integration — Update 2026-08-10 (Asia/Dubai)**
> **Branch:** `phase-3/eleva-builder` | **Latest commit:** `3e40c22` (`docs: re-verify live DB/browser blockers remain`) | **Remote:** `origin/phase-3/eleva-builder` verified `3e40c22` (pre-task); post-task commit recorded in §19 row 38
> **Live browser/DB verification — COMPLETED 2026-08-10 (this task). CI job `e2e-live` + live spec + build-gate fixes:**
> - **CI (`.github/workflows/ci.yml` + `playwright.live.config.ts`):** new `e2e-live` job (needs `test`+`build`) — postgres:15-alpine + redis:7-alpine services; M4 marked applied via `prisma migrate resolve --applied 20260726000001_orders_partitioning_by_year` (standing R2/PREX-MIG-002 parking — no repo modification); canonical `migrate deploy` 13/13 + seed; full `pnpm build`; `/etc/hosts` maps `albaik.localhost`; API compiled dist on :8000; qr-menu **production** build (`next start`) on :3000; `npx playwright install --with-deps chromium`; runs `tests/e2e/verify-phase3-live.spec.ts` via `playwright.live.config.ts` — deliberately NO `webServer` entries (the earlier `npx --yes pnpm` webServer overhead caused the 120s timeouts); Playwright report artifact on failure
> - **Live spec (`tests/e2e/verify-phase3-live.spec.ts`) — NO API mocks** (real Chromium → real Next → real API → real PostgreSQL), closing the recorded gaps: (1) SSR menu renders real seeded data, `design:null` → ProductGrid fallback; (2) live cart add/open/close scroll preservation + qty ±/remove; (3) live place order → real `orders` row PENDING + `order_items` + `kitchen_queues`; (4) live preorder via the UI → `isPreorder=true, preorderStatus=SCHEDULED, scheduledAt≈+45min` persisted in PostgreSQL + server-side 15-min rule (HTTP 400) via direct API; (5) live draft-vs-published: `TenantDesign` row created through the real tenant-scoped client with distinct markers → public menu returns published only, never draft (cleanup restores pristine state). DB access uses the proven `dbTenantContext.run({tenantId}, async () => await …)` pattern (lazy PrismaPromise + outer `await` loses the ALS context — fail-safe blocks it; discovered and corrected in-task)
> - **Build-gate findings — the Phase 3 branch was NEVER production-built (`pnpm build` was genuinely red):** (a) api tsc: 1 error — `rbac-permission.guard.spec.ts` mock `Order` missing the new Phase 3 fields (`isPreorder/scheduledAt/preorderStatus`) — added (type-only); (b) qr-menu `next build` failed ESLint on ~24 `@typescript-eslint/no-explicit-any` in the Phase 3 `DesignSections` renderer + untyped checkout payload — replaced with structural types (`DesignSection`, `TenantDesignPayload`, `PublicCategory`, `PublicProduct`, `CreateGuestOrderPayload`) — type-only, zero behavior change; `lib/types.ts` contract mirror gained `isPreorder?`/`scheduledAt?` (the UI already sent them); (c) **real defect found live: `transform: translateZ(0)` on the qr-menu app container made it the containing block for the `position:fixed` modal/cart overlays — the bottom sheet rendered at document-bottom (A/B-proven: with transform → overlay 1710px tall spanning the whole document, sheet at page y≈1311–1779, below the fold at scrollY=0; without → overlay = viewport 720px, sheet at 252–720)** — removed; the earlier "scroll jump" measurements were Playwright auto-scrolling to the alphabetically-first product (`Baklava`, page bottom — `findFirst orderBy name asc`), NOT an app scroll defect
> - **Verification run (2026-08-10, native runtime identical to the CI job sequence):** PostgreSQL 17.10 + Redis 8.0.2 (apt), migrations 13/13 shippable (M4 parked via `migrate resolve`), canonical seed (2 tenants / 14 tables / 14 products), API :8000, qr-menu production build :3000, Chromium headless 149 — **5/5 live tests PASS**; DB evidence: `ORD-2026-82791` PENDING/CASH + `ORD-2026-99952` PENDING/`isPreorder=t`/`preorderStatus=SCHEDULED`/`scheduledAt=2026-08-10T13:45:00Z` (+45min) + 2 `kitchen_queues` rows; qr-menu lint clean + 4 suites 34/34 PASS; `rbac-permission.guard.spec` 10/10 PASS
> - **Remaining (honest):** live RTL/LTR still not browser-verified (minor, unit-covered); the GitHub-hosted `e2e-live` run remains the environment of record (the local native run is the documented equivalent, §14 decision-12 pattern; PG 15 vs 17.10 — both Prisma-supported)
> **Verified tests/results (no invention):** `preorder.spec.ts` 4/4 PASS, `order.service.spec.ts` 27/27 PASS, `public-menu.controller.spec.ts` 16/16 PASS; full `jest --runInBand` 82 passed / 5 failed pre-existing (identical to main) + 1 skipped; `pnpm --filter @zayjar/db build` green; `next dev` boots `✓ Ready in 1.6s` `GET / 200`; **live browser/DB 5/5 PASS (2026-08-10, §19 row 38)**
> **Known verification gaps (NOT VERIFIED LIVE):** live RTL/LTR only (all other Phase 3 gaps closed 2026-08-10 — §19 row 38)
> **Remaining risks:** `featured/popular` use placeholder `flatProducts.slice` (no `Product.isFeatured` — explicitly not invented), Prisma 7 `validate` needs `prisma.config.ts`, 5 pre-existing Jest failures must not be hidden, `.git` snapshot wipe (periodically loses `.git/config`)
> **Exact next recommended task:** `featured`/`popular` product-selection design decision (`Product.isFeatured` vs section-level selection — currently placeholder `flatProducts.slice`); after that the GitHub-hosted `e2e-live` run should be confirmed green on first push
> **Live browser/database verification:** ✅ COMPLETED 2026-08-10 — CI job + live spec committed (this update); executed locally on the identical native stack: 5/5 PASS (details §19 row 38)


---

> Canonical engineering state document.
> Generation date: 2026-07-29 (Asia/Dubai). Independent audit corrections applied 2026-07-30 (full repository verification + live re-run of every test/type/build count).
> 2026-08-05 Phase 2 Stabilization — dead code removed, duplicate types consolidated, hardcoded UUIDs eliminated from KDS page, broad eslint-disable directives narrowed, console.log removed from production components, unused export deleted, import statements merged, obsolete AdminPanel references cleaned from JSDoc. No business logic changed. Verified: tsc 0/0/0/0, ESLint 6/6, 906 passed / 0 failed, build 6/6.
> 2026-08-04 Sprint 3 Tasks 2–4 — **P0 COMPLETE** (AUDIT-001 CSS pipeline, AUDIT-002 real payments, AUDIT-006/007 menu+location CRUD) and **Phase 2 AUDIT-014** started (Backoffice Products + Categories). Eleven defects were reproduced at runtime before any fix and re-verified after. Highlights: **AUDIT-001** — 308 Tailwind class attributes with no CSS pipeline whatsoever (no dep/config/stylesheet/import); production emitted zero CSS. **AUDIT-002** — payment verification accepted any invented id (`I-JUST-MADE-THIS-UP` -> `verified:true`), the `payments` table was never written by app code, Tap returned fabricated charge ids even with a real key, and the charged amount came from client-supplied `dto.amount`; a 5th defect (malformed `orderId` -> HTTP 500) forced 15 `*Id` DTO fields to `@IsUUID('4')`. **AUDIT-006/007** — 12 endpoints (update/soft-delete/restore) for Products/Categories/Branches/Tables, no hard-delete anywhere; `idx_tables_qr_token` was a FULL unique index while the QR token is a deterministic HMAC of `tenantId:branchId:number`, so archiving table "12" permanently burned that number — 5 indexes rebuilt as PARTIAL per DOC-002 §234/§602. **Security found during frontend integration:** `CustomerController` had **no `@UseGuards`** and the app has no global auth guard, so `GET /api/v1/customers` returned **HTTP 200 with the entire customer PII table to unauthenticated callers**; and `CsrfGuard` (an `APP_GUARD`) ran **before** `JwtAuthGuard`, so `request.user` was always undefined and a forged token (`X-CSRF-Token: TOTALLY-BOGUS-VALUE-123`) was accepted on all **51** mutating routes — its unit tests passed only because they inject `user` by hand. A CORS wildcard (`origin:'*'` without credentials) also broke **every** browser call; invisible to curl, which does not enforce CORS. Verified: tsc 0/0/0/0, ESLint 6/6, **837 pass / 2 skip / 0 fail** (was 613; +224), build 6/6, browser E2E Products **19/19** + Categories **21/21** in real Chromium against the real API and PostgreSQL, adversarial **7/7** + **13/13**. Commit `dee3527` (§29 "Last Completed Work").
> 2026-08-03 Sprint 3 Task 1 — **AUDIT-004 (Staff User Management)** FIXED+VERIFIED under CTO approval. The platform could not create a user: the only `user.create` in the entire codebase was the owner row inside tenant onboarding (`tenant.service.ts:127`), so the Cashier/Backoffice login screens delivered in Sprint 2 Task 1 had **no accounts to authenticate** — 51 route decorators existed and not one managed staff. New `apps/api/src/user/` module delivers full CRUD: `POST /api/v1/users`, `GET /api/v1/users` (filters `isActive`, `branchId`), `GET /api/v1/users/:id`, `PUT /api/v1/users/:id`, `PUT /api/v1/users/:id/roles`, `PUT /api/v1/users/:id/branches`, `DELETE /api/v1/users/:id` (soft delete + deactivate per DOC-002 deletion policy). **Branch isolation** required a schema addition — DOC-005 §4.2 mandates that a staff token carry assigned branches, but no User↔Branch link existed, so the `branches` claim consumed by `CaslAbilityFactory` (BRANCH_MANAGER `$nin` rules) had no persistent source and was permanently empty: new `UserBranch` model + migration `20260803000000_add_user_branches` (composite PK, tenant-owned rows, 3 FKs cascade). **RBAC**: 4 new seed permissions (`user:read/create/update/delete`) — the `User` CASL subject and its guard repository entry already existed but no permission row granted them; owner-only by construction (manager/cashier/kitchen seed filters are resource-allowlisted and exclude `user`, SQL-verified 4/4 owner vs 0 cashier). **Auth integration**: passwords hashed via the existing `AuthService.hashPassword` (Argon2id), so created credentials work with the unmodified login path. Verified: tsc 0/0/0, ESLint 6/6, suites **328/2/0** + root **565/2/0** (was 311/2/0 + 548/2/0; +17 new tests), build 6/6, migration chain 9/9 on fresh PG 17.10, and a full live matrix — create 201 → **new user logs in 200 with correct CASHIER claims** → RBAC 403 for cashier / 200 owner / 401 anon → cross-tenant GET/PUT/DELETE all **404** (no existence leak, foreign row untouched) → foreign branch assignment **400** (scope unchanged) → role swap CASHIER→MANAGER reflected in a fresh JWT → password rotation old **401** / new **200** → self-delete **400** → soft delete → deleted user 404, login 401, excluded from list (§5 Sprint 3 row, §19 row 37).
> 2026-08-03 Sprint 2 Task 8 (ENV-TSK-0006 — install/provisioning toolchain): the last **High**-priority open defect is FIXED+VERIFIED. Three root causes, three minimal fixes: **(1)** `pnpm-workspace.yaml` `allowBuilds` now approves the four packages whose install scripts are genuinely required — `@prisma/engines` (postinstall downloads the query + schema engine binaries), `@prisma/client`, `prisma`, and `argon2` (its install script runs `node-gyp-build`, compiling a working native binding; the shipped `argon2.glibc.node` prebuild SIGSEGVs on modern glibc). Least privilege preserved: `sharp`, `@nestjs/core`, `protobufjs`, `unrs-resolver`, `@firebase/util` are deliberately NOT approved and were each verified to load correctly without their scripts (`sharp` resolves via the prebuilt `@img/sharp-linux-x64` package). **(2)** `packages/db/prisma/migrations/20250101000000_init/migration.sql` carried an upstream UTF-8 BOM (`EF BB BF`) that made `prisma migrate deploy` hard-fail at migration 1 with `P3018 / 42601 syntax error at or near "\ufeff"` — exactly 3 leading bytes removed, remainder byte-identical (26 794 → 26 791 B, `a[3:] == b` proven). This **disproves** the §19 row 23 parenthetical "Prisma engines unaffected" (that claim held only for the psql-with-strip-filter workaround; the CLI path was never exercised). **(3)** CI Job 3's "Validate Prisma schema" step could never pass — `prisma validate` resolves `url = env("DATABASE_URL")` before validating and no `DATABASE_URL` exists anywhere in `ci.yml` (grep = 0 hits) → unconditional `P1012`; the step now carries a connection-free placeholder URL (static schema check only, no database service added). Verified: clean-slate `rm -rf node_modules && pnpm install --frozen-lockfile` → engines present + argon2 compiled + `argon2.hash/verify` true/false correct, **with zero manual workarounds**; `prisma migrate deploy` applied the full documented chain **8/8 green** on fresh PostgreSQL 17.10 (M4 excluded per the standing R2 parking — it now fails on its own known layer-2 architecture defect, not on the BOM); canonical `pnpm --filter @zayjar/db run prisma:seed` 17/17 entity groups; live API boot on that CLI-provisioned DB with the full runtime matrix — `/health` 200, guest table 200, guest menu 200, **real argon2 login 200** (JWT issued) / wrong-password **401**, `/auth/me` 200, RBAC products 200, guest checkout **201** (`ORD-2026-15027`, paymentMethod CASH persisted), discount `SAVE10` → 2.40 off, `kitchen_queues` rows created. Static gates unchanged: tsc 0/0/0, ESLint 6/6, suites **311/2/0** + root **548/2/0**, build 6/6 (§5 Sprint 2 row, §19 row 36).
> 2026-07-31 Sprint 2 Task 7 (Firebase Cloud Messaging — §15 item 8 / SPEC-DRIFT): the FCM log-only stub is replaced with a real provider — new `FcmService` (`firebase-admin` SDK, lazy init from `FIREBASE_SERVICE_ACCOUNT` JSON or `FIREBASE_SERVICE_ACCOUNT_PATH`, graceful unavailable state when unconfigured), `DeviceTokenService.sendPushNotification` now sends a real FCM multicast when available (prunes `messaging/registration-token-not-registered` tokens, persists an in-app Notification mirror) and keeps the log-only fallback otherwise (dev/test behavior preserved); the `notifications` table gained its missing tenant FK/relation + `TenantNotificationRepository` (migration `20260731000002_add_notifications_relations`). Verified: tsc 0/0/0, ESLint 6/6, suites **311/2/0** + root **548/2/0** (was 304/2/0 + 541/2/0; +7 new tests), build 6/6, runtime — migration M9 clean (FK+index), API boots with new wiring, live device-token registration (201), notification repository create/read against real PG (tenant-scoped), live push fallback `{sent:1,payloads:1}` with correct lazy-init warning (§5 Sprint 2 row, §19 row 35). Real FCM send covered by unit tests (mocked SDK; no real Firebase credentials in sandbox).
> 2026-07-31 Sprint 2 Task 6 (Socket.io Redis Adapter — SPEC_INDEX §7.6): the KDS WebSocket gateway now attaches the Socket.io **Redis adapter** (`@socket.io/redis-adapter` + `redis` pub/sub clients) in `afterInit`, so room broadcasts (`tenant:{tenantId}:branch:{branchId}`) are delivered across ALL API pods (k8s HPA scales 2–10) sharing Redis. URL: `SOCKET_IO_REDIS_URL` → `REDIS_URL`; TLS via `REDIS_TLS`; graceful fallback to the in-memory adapter when Redis is unconfigured/unreachable (single-pod/dev behavior unchanged — no crash); pub/sub clients quit on module destroy. Verified: tsc 0/0/0, ESLint 6/6, suites **304/2/0** + root **541/2/0** (was 300/2/0 + 537/2/0; +4 new adapter tests), build 6/6, **runtime cross-pod proof** — two independent Socket.io servers ("pods") sharing Redis: a `ticket.created` broadcast from Pod A reached the client connected to Pod B (§5 Sprint 2 row, §19 row 34).
> 2026-07-31 Sprint 2 Task 5 (Real Invoice PDF — §15 item 8 / §22): the fabricated `pdfUrl = https://cdn.zayjar.com/invoices/...` string is replaced with real PDF generation + storage — new `InvoicePdfService` (pdfkit render of a proper A4 invoice with real order/customer/branch/restaurant data), `InvoiceStorageService` (reuses the StorageProvider abstraction: local `/uploads/invoices/<tenant>/<num>.pdf` or S3), `InvoiceModule` wired into OrderModule, and a static-assets route in main.ts so `/uploads/*` URLs are actually servable. `generateInvoice` now resolves real display data (customer/branch/restaurant via prisma) and persists the real stored URL. Verified: tsc 0/0/0, ESLint 6/6, suites **300/2/0** + root **537/2/0** (was 296/2/0 + 533/2/0; +4 new tests), build 6/6, runtime — completing an order produced `INV-2026-767883` PDF (1926 bytes, %PDF magic) stored on disk and served over HTTP 200 application/pdf, embedding real company/branch/order/totals (§5 Sprint 2 row, §19 row 33). `pdfkit` (+ `@types/pdfkit`) added to `@zayjar/api` deps.
> 2026-07-31 Sprint 2 Task 4 (Discount Engine — §15 item 8 / §22): the `discountAmount = 0.00` placeholder is replaced with a real, tenant-scoped discount engine — new `Discount` model + `DiscountType` enum (migration `20260731000001_add_discount_engine`), pure pricing/validation `DiscountService` (percentage/fixed, capped at subtotal, 2dp; uniform anti-oracle rejection), optional `discountCode` on the order DTO, applied atomically inside the checkout transaction (`tx.discount.findUnique` + usage increment + order snapshot `discountId`/`discountCode`), demo discounts `SAVE10`/`FIXED5` seeded for albaik. Verified: tsc 0/0/0, ESLint 6/6, suites **296/2/0** + root **533/2/0** (was 279/2/0 + 516/2/0; +17 new tests), build 6/6, runtime on fresh DB — SAVE10 → 2.40 off (10%), FIXED5 → 5.00, invalid → 400 uniform message, cross-tenant code rejected (400, usage unchanged) (§5 Sprint 2 row, §19 row 32).
> 2026-07-31 Sprint 2 Task 3 (Order.paymentMethod persistence — §15 item 3): the checkout contract's `paymentMethod` (guest UI sends CASH) is now persisted on `Order` instead of silently dropped — new nullable column (`PaymentMethodType?`, no fabricated backfill; historical rows stay NULL), migration `20260731000000_add_order_payment_method`, service writes `dto.paymentMethod` on the single shared create path, response echoes the real value. Verified: api tsc 0 / db 0 / types 0, ESLint 6/6, suites 279/2/0 + root 516/2/0 (no regression), build chain 6/6, live runtime — staff checkout ORD-2026-79953 `paymentMethod=CASH` persisted+echoed, guest checkout ORD-2026-46938 `CASH`, pre-existing seeded order stays NULL (§5 Sprint 2 row, §19 row 31). Prisma client regenerated in-sandbox for the schema change (also materialized the previously-stale `branding` column + KDS indexes — see LATENT-BRAND-001 update §7/§20).
> 2026-07-31 Sprint 2 Task 2 (RT-ONB-002): onboarding RBAC provisioning defect FIXED+VERIFIED — every newly onboarded owner now receives the canonical seeded RESTAURANT_OWNER permission set (22 rows, incl. 8 guard-vocabulary additions: product/branch/table/order/tenant × create/read/update) cloned at provisioning time with fail-fast on a missing baseline; seeded≡onboarded parity SQL-proven, staff flow verified end-to-end as an onboarded owner (menu 200, checkout 201, tables 201, branches 201, tenant 200 — previously all 403); backward compatibility preserved (manager 10 / cashier 6 / kitchen 4 unchanged) (§5 Sprint 2 row, §19 row 30).
> 2026-07-31 Sprint 2 Task 1 (Staff Authentication UI): the documented Auth-UI gap (§15 item 6) is now closed — both staff apps (cashier, backoffice) ship a standalone `/login` flow wired to the existing auth surface (login, MFA challenge, session persistence, CSRF echo, logout) with auth-gated entry pages; zero backend files changed; all gates green + live runtime matrix (login 200/401/403, MFA 401→200, CSRF cookies, logout blacklist — §5 Sprint 2 row, §19 row 29). RT-ONB-002 (onboarding provisions permission-less owner roles — discovered during Task-1 runtime verification) recorded §7/§20/§23 item 11 as the next task.
> 2026-07-30 official-state update: sections §19–§28 added (Verification History, Open Blocking Defects, Quality Gate, Technical Debt, TODO, End-of-Session Procedure, Session Recovery, CTO Rules, Future Architecture, Session Completion Policy); Sprint-1 Step-4 live verification and Runtime Defects R4/R5 (both FIXED+VERIFIED) incorporated; all document-vs-repository contradictions corrected in place (see "Repository Corrections" note in §19 footer).
> 2026-07-31 H-3/DEPLOY-003 deployment fix: Engineering Audit v1 High finding H-3 FIXED+VERIFIED under CTO execution command — the CD placeholder ("Deployment would proceed here") replaced with the real Kubernetes deployment flow: both deploy jobs now set up kubectl, load cluster credentials from environment-scoped kubeconfig secrets (`KUBECONFIG_STAGING`/`KUBECONFIG_PRODUCTION`, raw-or-base64 with fail-fast when unconfigured), converge the application manifests (namespace/configmap + the five app dirs; `k8s/secrets.yml` placeholders and the stateful data tier deliberately not re-applied), pin all five workloads to the immutable build tag (`kubectl set image`, worker reuses the api image), and wait on every rollout; automatic rollback now covers staging and production, and `rollback.sh` gained a Kubernetes strategy (rollout undo ×5) as its primary path with the former ECS/compose fallbacks byte-preserved. Deployment health gates aligned to the real route (H-1/H-2): `health-check.sh` endpoint `/api/v1/health` → `/health` plus an empty-URL guard fix that un-deadlocked its own skip path, and the production smoke test now curls `/health`. Smoke/health behavior executed live: PASS 200 against the running API on attempt 1, SKIP on unconfigured URL, FAIL exit 1 after 10 retries on a dead port; rollback executed against a stub kubectl — argv-exact (5 rollout-undos in manifest order + status wait). Static gates: workflow YAML-parsed (15 embedded run blocks bash-n clean, zero placeholder strings), ci.yml byte-untouched, build↔manifest↔pin name mapping asserted, `kubectl kustomize` renders 22 manifests with zero stale probe paths, offline `kubectl set image --local` transforms all five workloads to the pinned tag, build chain 6/6 serialized (CI Job 3 reproduced), suites 277/2/0 + root 514/2/0 (zero application code touched), tsc api 0 / db 0, eslint 6/6 (§7 DEPLOY-003, §19 row 28). Cycle-19 sandbox wipe survived pre-fix (§6 pins).
> 2026-07-31 H-2/DEPLOY-002 deployment fix: Engineering Audit v1 High finding H-2 FIXED+VERIFIED under CTO execution command — the health endpoint is now infrastructure-facing: `TenantContextMiddleware` excludes exactly the path `health` at the consumer level (`app.module.ts`), so Kubernetes liveness/readiness probes and container healthchecks receive HTTP 200 without tenant resolution, while every other route keeps the full tenant fail-safe (the exemption is path-exact — verified live). Runtime matrix: `/health` **200** on default Host (apex), kubelet-style `Host: 10.244.1.17`, HEAD (compose `wget --spider` semantics), tenant host, and `X-Tenant-ID` (body `status: ok`); preservation byte-exact: `/` apex 403, `/healthx` 403 (no widening), `/api/v1/health` 403 apex / 404 tenant (H-1 evidence holds), anonymous public resolve 403 apex / 200 tenant, C-1 attack 403 with the verbatim message on the real protected route, forged PLATFORM_OWNER apex 403 on non-health surfaces (C-2 intact — `/health` no longer evaluates any token, so nothing is conferred), clean staff protected 200. +4 wiring specs (root 510 → **514/2/0, 60/61**; filter 277/2/0 unchanged — glob-depth semantics), tsc api 0 / db 0, eslint 6/6 (§7 DEPLOY-002, §19 row 27). Cycle-18 sandbox wipe survived pre-fix (§6 pins; `/tmp` + apt stack survived, no environment rebuild). H-3 remains untouched per CTO sequencing.
> 2026-07-31 H-1/DEPLOY-001 deployment fix: Engineering Audit v1 High finding H-1 FIXED+VERIFIED under CTO execution command — the Kubernetes liveness/readiness probe paths (`k8s/api/deployment.yml`) and the production/staging compose healthchecks pointed at `/api/v1/health`, a route that does not exist (the actual health endpoint is `GET /health`, root-mounted — no global prefix); all four probe paths now match the actual health endpoint with every other probe parameter unchanged (YAML-parsed assertions) and zero application-code change. Runtime A/B: old path 404 on tenant hosts / 403 apex (probe could never succeed), new path **200** on both seeded tenants (body `status: ok`) and via `X-Tenant-ID`; the apex 403 residual is H-2 (health-endpoint tenant-gating) — untouched per CTO sequencing, as is H-3; the same-turn-discovered staging-compose site (beyond the audit's enumeration, identical defect class) was fixed in the same commit and reported openly. Suites 277/2/0 + root 510/2/0, tsc api 0 / db 0, eslint 6/6 0 errors (§7 DEPLOY-001, §19 row 26). Cycle-17 sandbox wipe survived pre-fix (§6 pins; this cycle also wiped `/tmp` + the apt-installed PostgreSQL/Redis — runtime stack rebuilt clean, seed 16/16 re-verified).
> 2026-07-31 C-2/AUTHZ-002 security patch: Engineering Audit v1 Critical finding C-2 FIXED+VERIFIED under CTO execution command — every authorization decision based on `jwt.decode()` eliminated: platform-owner context in `TenantContextMiddleware` is now derived ONLY from signature-verified tokens (`jwt.verify` against the access secret; forged/malformed/expired confer nothing), and the refresh flow's claim source moved inside the already-verified rotation (`rotateRefreshToken` returns `sub` from `verifyAsync`; the unverified `decodeToken` helper deleted) — zero unverified-decode sites remain in production code. Runtime A/B: forged PLATFORM_OWNER at apex **403** (pre-fix 200 via decode bypass — audit-verified path), valid owner **200 preserved**, expired 403, staff apex 403 unchanged, C-1 attack matrix re-verified 403, forged bearer on protected route **401** (layered defense intact). Suites 277/2/0 + root 510/2/0 (= 504 + 6 new C-2 tests), tsc api 0 / db 0, eslint 6/6 (§7 AUTHZ-002, §19 row 25). Cycle-16 sandbox wipe survived pre-fix (§6 pins).
> 2026-07-31 C-1/AUTHZ-001 security patch: Engineering Audit v1 Critical finding C-1 FIXED+VERIFIED under CTO execution command — authenticated cross-tenant isolation bypass via caller-supplied `X-Tenant-ID` closed at the identity layer (`JwtStrategy.validate` reconciles the runtime tenant context with the signature-verified JWT tenant; mismatched `X-Tenant-ID` → 403, mismatched middleware-resolved context → 403; PLATFORM_OWNER exempt per design). Runtime A/B proof: pre-fix attack returned HTTP 200 (leak live), post-fix 403 on both attack vectors; suites 277/2/0 + 504/2/0 (root, +8 reconciliation tests); tsc api 0 / db 0, eslint 0 unchanged (§7 AUTHZ-001, §19 row 24). Cycle-15 sandbox wipe survived pre-fix (§6 pins).
> 2026-07-31 Step-5 RE-RUN update: final quality gate re-executed under CTO execution command on the post-remediation branch (CAT program COMPLETE — PREX-TSC-001/002 eliminated): TypeScript api 0 / db 0 / types 0 ✅, ESLint 6/6 ✅, suites 277/2/0 (filter) + 496/2/0 (root CI command) ✅, full production build chain 6/6 under official turbo tooling ✅, runtime sanity 5/5 migrations + 16/16 seed + probe-matrix exact ✅, PROJECT_STATE consistency ✅ — **zero code changes required in-gate; no new defects; Sprint 1 closure decision rests with the CTO** (§19 row 23, §21). Docker / CI Job 4 ⚠ CANNOT VERIFY (no daemon — environment-only). Sandbox wipe cycle 14 survived pre-gate (§6 pins).
> 2026-07-30 Step-5 update: Sprint 1 final quality gate executed under CTO mandate (§19 row 11, §21); Runtime Defect R7 / RT-ONB-001 (public-onboarding fail-safe block) found in-gate and FIXED+VERIFIED per defect policy A (§19 row 10) — PREX-E2E-001 closed as its canary; wiped/local hash lineage of R1/R3/R6 pinned (§6).
> Rule applied: every statement below is verified against the repository (file paths cited). Anything not verifiable from the repository is marked **UNKNOWN** or **CANNOT CONFIRM**. Nothing is speculative.

---

# 1. Project Overview

## 1.1 Project goal

Multi-tenant Restaurant SaaS platform. Verified scope identifiers from `DOC-001.md` and code comments: tenants (restaurant brands) with staff apps (Backoffice admin, Cashier POS), guest QR ordering channel (scan table QR → browse menu → checkout without account), Kitchen Display System (KDS) with realtime ticket events, and Stripe-based subscription billing with plan limits.

## 1.2 Current platform scope

- Monorepo (pnpm workspaces + Turbo), root `package.json`, `turbo.json`.
- Specification documents verified present at repo root: `DOC-001.md` … `DOC-010.md`, `SPEC_INDEX.md`, `IMPLEMENTATION_ROADMAP.md`, `TSK-0006_HEALTH_CHECK.md`.
- In-progress execution sprint: **Sprint 1 — public QR ordering channel end-to-end** (Steps 1–4 complete — Step 4 live-verified 2026-07-30 on a native runtime; **Step 5 final gate RE-EXECUTED 2026-07-31 under CTO execution command — all attainable criteria GREEN (§19 row 23); closure decision rests with the CTO** — plan recorded in commits `d1c6035`…`5967db9`).
- Post-Step-4 runtime defects: **R4 (PREX-MIG-001) and R5 (PREX-SEED-001) FIXED+VERIFIED 2026-07-30** (commits `c15843f`, `084dd37`); **R1/R2/R3 remain OPEN** (§20).
- Overall project status: Sprint 1 feature-complete pending final gate — see §6 (Current Sprint Status) and §21 (Quality Gate).

---

# 2. Repository Structure

Verified via `ls`:

```
apps/
  api/          NestJS 10 backend (listens on PORT, default 8000 — src/main.ts:71; docker-compose maps ${PORT:-8000}:8000)
  backoffice/   Next.js 14 admin app (compose maps host 3001:3000)
  cashier/      Next.js 14 POS app (compose maps host 3002:3000)
  qr-menu/      Next.js 14 guest QR ordering app (dev/start port 3000 per package.json; compose 3000:3000)
packages/
  db/           Prisma 5.22 schema + migrations + client + tenant repositories (@zayjar/db)
  types/        Shared enums/types (@zayjar/types)
  jest-preset/  Shared Jest preset (@zayjar/jest-preset: jsdom, @testing-library)
k8s/            Kubernetes manifests (api hpa 2–10 pods verified in k8s/api/hpa.yml)
nginx.conf      Production ingress reverse proxy at repo root (upstream api-core:8000)
docker-compose.yml / docker-compose.staging.yml / docker-compose.production.yml  Local/staging/prod stacks
docs/roadmap/   Supplementary planning docs
infra/cloudfront/  CDN infrastructure assets
tests/e2e/      Playwright e2e suites (root playwright.config.ts)
.github/workflows/  ci.yml (lint → test → build → docker) + cd.yml (production CD pipeline)
Root config: jest.config.js (root jest target of CI Job 2), tsconfig.json, docker-entrypoint.sh
Root non-spec docs: PROJECT_COMPLETION_REPORT.md, PROJECT_MANIFEST.md
```

Root scripts (verified `package.json`): `build` = `turbo run build`, `test` = `jest --runInBand --forceExit`, `lint` = `turbo run lint`. Engines: `node >= 20.0.0`.

---

# 3. Technology Stack

Verified from `package.json` files and build output:

| Layer | Technology | Version (declared) |
|---|---|---|
| Backend framework | NestJS (`@nestjs/core`, `@nestjs/common`) | ^10.3.8 |
| Auth/JWT | `@nestjs/jwt`, `jsonwebtoken`, `argon2` | ^11.0.2 / ^9.0.2 / ^0.45.0 |
| DB ORM | Prisma + `@prisma/client` | ^5.22.0 |
| Database | PostgreSQL 15 (schema targets: `Decimal`, `@db.Uuid`, `Timestamptz`) | `postgres:15-alpine` in docker-compose.yml |
| Cache/queue broker | Redis (ioredis, redis clients) | `redis:7.2-alpine`; clients ^5.11.1 / ^6.1.0 |
| Queue worker | BullMQ (DOC-010 §9.4 worker architecture) | ^5.81.2 |
| Frontend apps | Next.js | ^14.2.3 (14.2.35 resolved in pnpm-lock.yaml) |
| UI runtime | React, react-dom | ^18.3.1 |
| Realtime | socket.io (KDS gateway) | ^4.7.5 |
| Payments | stripe (api dep) | ^14.10.0 |
| Monorepo | turbo | ^1.13.3 |
| Tests | jest, ts-jest | ^29.7.0 / ^29.4.11 |
| Language | TypeScript | ^5.4.5 |
| CI | GitHub Actions, pnpm 9, Node 20 | `.github/workflows/ci.yml` (+ `cd.yml` production CD) |

---

# 4. Current Architecture

## 4.1 API (`apps/api`, NestJS)

- Global `TenantContextMiddleware` (`src/common/middleware/tenant-context.middleware.ts`): resolves tenant from `X-Tenant-ID` header → else Host subdomain (`*.zayjar.com`, `*.localhost`, ports stripped) → else custom domain (Redis-cached DB lookups). Missing tenant + non-platform-owner → 403. Wraps request in `dbTenantContext` AsyncLocalStorage.
- Other global middlewares, all `forRoutes('*')` and applied before tenant resolution (app.module.ts:48–55): `CorrelationIdMiddleware`, `HttpLoggingMiddleware`, `SanitizationMiddleware` (DOC-006 §5.4 XSS input sanitization).
- Guards: only global APP_GUARD is `CsrfGuard` (mutations only; skips `@Public()` routes and unauthenticated requests — verified `src/common/csrf/csrf.guard.ts`). `JwtAuthGuard` + `RbacPermissionGuard` are controller-level, bypass on `@Public()` via `IS_PUBLIC_KEY` reflector.
- Rate limiting: `RateLimitGuard` + `@RateLimit()` decorator (`src/common/rate-limit/`). Tiers (verified `rate-limit.service.ts` `getTierConfig`): **public 120/min, checkout 30/min, auth 10/min** per IP; Redis INCR/EXPIRE fixed window with in-memory fallback.
- Queue worker `src/worker.ts` (email/sms/push/webhook/websocket dispatch channels; BullMQ `Worker` cases verified lines 158–193).

## 4.2 QR Menu (`apps/qr-menu`)

- **Sprint 1 Step 3 state (committed 5967db9):** `src/app/page.tsx` is an async Server Component (`export const dynamic = 'force-dynamic'`). Reads `?t=<token>` from `searchParams`, resolves API base from incoming Host (`lib/guest-api.ts resolveServerApiBase`, env override `API_INTERNAL_URL`), SSR-fetches `GET /api/v1/public/menu?token=…` with `cache: 'no-store'`.
- `src/app/components/MenuBrowser.tsx` (`'use client'`): real cart (signature-merge, qty, removal, drawer, special notes), checkout POST to relative `/api/v1/public/orders/checkout`, confirmation view with server order number.
- `src/app/lib/`: `types.ts` (contract mirror of public API), `pricing.ts` (DOC-005 §4.3 rules), `guest-api.ts`, `format.ts`.
- `next.config.mjs`: `images.remotePatterns` (https any + localhost dev), `rewrites()` proxying `/api/*` → `API_INTERNAL_URL` (default `http://localhost:3001`) for local dev only — the dev default assumes the API on port 3001, which mismatches the API's default port 8000; see **LATENT-ENV-001** in §7.
- **No mock data anywhere in production src** (grep-verified 2026-07-29; re-verified clean 2026-07-30).

## 4.3 Cashier (`apps/cashier`)

- Next.js app, `CashierTerminal.tsx`; sends `X-Tenant-ID` header (verified lines 104, 258) and calls staff checkout `POST /api/v1/orders/checkout` (fetch at lines 253–258); reads `localStorage.accessToken` — the **only in-repo writer** of that key is the backoffice onboarding wizard (`RestaurantCreationWizard.tsx:253`, post-creation auto-login); there is **no standalone login screen** in any frontend (auth-UI gap — see §7 inventory and §15 item 5).

## 4.4 KDS

- Backend: `src/kds/kds.gateway.ts` (socket.io gateway, JWT-verified sockets with `JWT_CONFIG.accessTokenSecret`), `kds.service.ts` (REST ticket hydration), `kds.controller.ts`.
- Frontend: KDS terminal component lives in backoffice (`apps/backoffice/src/app/components/KDSTerminal.tsx`).

## 4.5 Backoffice (`apps/backoffice`)

- Next.js app. **AUDIT-014 (2026-08-04):** `BackofficeShell.tsx` is the entry point — a six-tab CRUD shell (Products, Categories, Branches, Tables, Customers, Staff). **Products** and **Categories** are fully wired; the remaining four render a placeholder pending implementation.
- Shared `lib/api-client.ts` centralises auth, tenant context and CSRF: it attaches `X-CSRF-Token` to every POST/PUT/DELETE/PATCH (required since the `CsrfGuard` fix — see §29) and preserves NestJS `message` arrays so validation detail reaches the operator instead of a bare "Bad Request".
- Tenant id now comes from the verified session. The hardcoded `'tenant-uuid-1111'` / `'branch-uuid-1234'` literals are gone from `page.tsx`.
- `AdminPanel.tsx` (read-only, zero `useMutation`) has been **deleted**; `/kds` uses `KDSTerminal.tsx` directly.
- Also present: `RestaurantCreationWizard.tsx`, `KDSTerminal.tsx`, `LoginForm.tsx`.

## 4.6 Billing

- `src/billing/billing.controller.ts` (`api/v1/billing`), `billing.service.ts`: Stripe webhook `@Public()` with `STRIPE_WEBHOOK_SECRET` signature verification; subscription lifecycle statuses (TRIALING/ACTIVE/PAST_DUE/UNPAID/CANCELED enum verified in schema).

## 4.7 Authentication

- HS256 symmetric JWT currently (`src/auth/config/jwt.config.ts`: `JWT_SECRET`/`JWT_REFRESH_SECRET` env with fallback defaults — production refuses defaults by design, `accessTokenExpiry: '15m'`, `refreshTokenExpiry: '7d'`). Refresh rotation + Redis blacklist; argon2id password hashing; MFA via speakeasy.
- `@Public()` route decorator at `src/auth/decorators/public.decorator.ts`; CASL ability factory for RBAC; `@RequirePermission` decorator.
- **Spec deviation (documented inventory):** specification requires RS256 2048-bit; implementation is HS256. Migration decision recorded as deferred (session-approved; not yet in repository code).

## 4.8 Database (`packages/db`)

- Prisma schema `packages/db/prisma/schema.prisma`: **30 models, 9 enums** (verified counts; enums: TenantStatus, SubscriptionStatus, TableStatus, OrderType, OrderStatus, CookingStatus, PaymentMethodType, PaymentStatus, MediaType).
- `packages/db/src/index.ts` exports `prisma` (PrismaClient + `$extends` tenant-scoping extension), `prismaRead`, `dbTenantContext` (AsyncLocalStorage), generated client, repositories. Extension `unscopedModels = ['Tenant','SubscriptionPlan','AuditLog','Notification']`; injects `tenantId` into where/create; blocks createMany/upsert family; fail-safe error without context (`Fail-Safe Block`, src/index.ts:34).
- `BaseTenantRepository` + 16 tenant repositories (verified `src/repositories/` listing: 18 files incl. Base + index.ts), incl. `TenantTableRepository.findByQrCodeToken` (added Sprint 1 Step 2; tenant-scoped + `deletedAt: null`, lines 15–20).
- Seed: `packages/db/prisma/seed.ts` — 3 plans (plan-starter-001/growth-002/enterprise-003), demo tenant `albaik`, tables with `qrCodeToken` like `qr-albaik-r-1-<ts>`, full menu.

---

# 5. Completed Work

Organized by sprint/step. Commit stats are the verification anchor (exact file counts + insertions/deletions reproduced identically across session re-creations).

| Sprint | Step | Commit (current local) | Content | Verification at completion |
|---|---|---|---|---|
| Sprint 3 | Task 1 — AUDIT-004 Staff User Management | see §19 row 37 | 12 files — NEW `apps/api/src/user/` (`user.service.ts` full CRUD + role/branch assignment, `user.controller.ts` 7 routes with `@RequirePermission(*, 'User')`, `user.module.ts`, 4 DTOs, `user.service.spec.ts` 17 tests); `packages/db/prisma/schema.prisma` (`UserBranch` model + `User.userBranches` / `Branch.userBranches` / `Tenant.userBranches` back-relations); migration `20260803000000_add_user_branches`; regenerated Prisma client; `packages/db/prisma/seed.ts` (+4 `user:*` permission rows, deterministic UUIDs per R5 convention); `apps/api/src/app.module.ts` (register `UserModule`) | ✅ VERIFIED 2026-08-03 — tsc api 0 / db 0 / types 0; ESLint 6/6; user suite 17/17; api filter **328/2/0 (36/37)** + root **565/2/0 (65/66)**; build 6/6; migration chain **9/9** clean on fresh PG 17.10 + canonical seed; live matrix: create 201, **new user login 200** (CASHIER claims), cashier 403 / owner 200 / anon 401, cross-tenant 404×3 (row untouched), foreign branch 400, role swap → fresh JWT MANAGER, password rotation 401/200, self-delete 400, soft delete → 404 + login 401 + list-excluded |
| Sprint 2 | Task 8 — ENV-TSK-0006 (install/provisioning toolchain) | see §19 row 36 | 3 files, +30/−1 — `pnpm-workspace.yaml` (`allowBuilds`: add `@prisma/client`, `@prisma/engines`, `prisma`, `argon2` — each justified in an inline comment block; `sharp`/`@nestjs/core`/`protobufjs`/`unrs-resolver`/`@firebase/util` deliberately left unapproved and verified unnecessary), `packages/db/prisma/migrations/20250101000000_init/migration.sql` (strip upstream UTF-8 BOM — exactly 3 leading bytes, remainder byte-identical), `.github/workflows/ci.yml` (Job 3 "Validate Prisma schema": add connection-free placeholder `DATABASE_URL` + explanatory comment; no database service added) | ✅ VERIFIED 2026-08-03 — clean-slate install with **zero manual workarounds** (engines auto-downloaded, argon2 compiled from source, hash/verify true+false correct); `prisma migrate deploy` **8/8** on fresh PostgreSQL 17.10 (M4 excluded per standing R2 parking); canonical seed 17/17; live API on the CLI-provisioned DB: `/health` 200, guest table/menu 200, **argon2 login 200 + wrong-password 401**, `/auth/me` 200, RBAC products 200, guest checkout **201** (paymentMethod CASH), `SAVE10` → 2.40, kitchen_queues rows; static gates unchanged — tsc 0/0/0, ESLint 6/6, filter **311/2/0**, root **548/2/0**, build 6/6 |
| Sprint 2 | Task 7 — Firebase Cloud Messaging (FCM) (§15 item 8 / SPEC-DRIFT) | see §19 row 35 (local, pre-push) | 16 files — `apps/api/src/fcm/fcm.service.ts` (new: real FCM via `firebase-admin`, lazy init `FIREBASE_SERVICE_ACCOUNT`/`_PATH`, `isAvailable()`, `sendPush` multicast with unregistered-token detection, never throws) + `fcm.service.spec.ts` (4 tests: unavailable w/o creds, init+send+data coercion, unregistered flagging, send-error → full failure); `device-token.service.ts` (real path when FcmService available: send → prune unregistered → persist Notification mirror; log-only fallback preserved) + spec (+3 real-path tests) + module (provides FcmService); schema `Notification`↔`Tenant` relation + `Tenant.notifications`; migration `20260731000002_add_notifications_relations` (FK cascade + tenant index); `TenantNotificationRepository` (new) + export; regenerated client; deps `firebase-admin` | ✅ VERIFIED 2026-07-31 — tsc 0/0/0; ESLint 6/6; fcm 4/4 + device-token 10/10; api filter **311/2/0 (35/36)** + root **548/2/0 (64/65)** (was 304/2/0 + 541/2/0; +7 new tests); build 6/6. **Runtime** (fresh `zayjar_rt8`, 9-migration chain incl. M9 clean; FK+index verified): API boots with new wiring; live device-token registration **201** (+DB row); `TenantNotificationRepository` create/read against real PG tenant-scoped; live push fallback `{sent:1, payloads:1}` with correct FCM-disabled lazy-init warning. Real FCM send covered by unit tests (mocked SDK; no real Firebase creds in sandbox — documented) |
| Sprint 2 | Task 6 — Socket.io Redis Adapter (SPEC_INDEX §7.6) | see §19 row 34 (local, pre-push) | 4 files — `apps/api/src/kds/kds.gateway.ts` (+73: `OnGatewayInit` + `OnModuleDestroy`; `afterInit` attaches `createAdapter(pubClient, subClient)` from `@socket.io/redis-adapter` using `redis` clients — URL `SOCKET_IO_REDIS_URL`→`REDIS_URL`, `REDIS_TLS` support, graceful in-memory fallback, clients quit on destroy), `apps/api/src/kds/kds.gateway.redis.spec.ts` (new, 4 tests: no-URL keeps in-memory, attaches on REDIS_URL, falls back on unreachable, quits on destroy), `apps/api/package.json` + `pnpm-lock.yaml` (`@socket.io/redis-adapter`) | ✅ VERIFIED 2026-07-31 — tsc 0/0/0; ESLint 6/6; kds suites 45/45; api filter **304/2/0 (34/35)** + root **541/2/0 (63/64)** (was 300/2/0 + 537/2/0; +4 new tests); build 6/6. **Runtime cross-pod proof** (Redis 8.0.2): two independent Socket.io server instances ("pod A"/"pod B") each with the Redis adapter; a client on pod B joined room `tenant:t1:branch:b1`; pod A `to(ROOM).emit('ticket.created', {ticketId:'t-1', pod:'A'})` → **received by pod B's client** (JSON payload verified) — impossible with the default in-memory adapter, exactly the multi-pod KDS requirement |
| Sprint 2 | Task 5 — Real Invoice PDF Generation (§15 item 8 / §22) | see §19 row 33 (local, pre-push) | 8 files — `apps/api/src/invoice/invoice-pdf.service.ts` (new: pdfkit A4 invoice renderer, pure service), `invoice-pdf.service.spec.ts` (3 tests: %PDF magic, embedded text via decompressed hex runs, content-deterministic), `invoice-storage.service.ts` (new: StorageProvider-backed persistence, local/S3 by env, deterministic key `invoices/<tenant>/<num>.pdf`), `invoice.module.ts` (new, exports both); `order.service.ts` (generateInvoice resolves real customer/branch/restaurant data, renders + stores PDF, persists real URL; renderAndStoreInvoicePdf fallback to CDN string only if PDF pipeline unwired), `order.service.spec.ts` (+1 test proving real stored URL when services wired), `order.module.ts` (imports InvoiceModule), `main.ts` (NestExpressApplication + static `/uploads/*` route); deps: `pdfkit`, `@types/pdfkit` (package.json + lockfile) | ✅ VERIFIED 2026-07-31 — tsc 0/0/0; ESLint 6/6; api filter **300/2/0 (33/34)** + root **537/2/0 (62/63)** (was 296/2/0 + 533/2/0; +4 new tests); build chain 6/6. **Runtime** (fresh `zayjar_rt7`, 8-migration chain, API with STORAGE_PROVIDER=local): completed order → invoice `INV-2026-767883` with real `pdfUrl /uploads/invoices/<tenant>/INV-2026-767883.pdf`; file on disk 1926 bytes; HTTP GET → 200 `application/pdf`, `%PDF-1.3` magic; embedded text verified to contain real company (Invoice Kitchen), branch (Main Branch), order number, and totals | 
| Sprint 2 | Task 4 — Discount Engine (§15 item 8 / §22) | see §19 row 32 (local, pre-push) | 13 files — `apps/api/src/discount/discount.service.ts` (new, pure engine: computeDiscountAmount percentage/fixed capped at subtotal 2dp; validateDiscount uniform anti-oracle rejection) + `discount.service.spec.ts` (12 tests); `packages/db/prisma/schema.prisma` (`Discount` model, `DiscountType` enum, `Order.discountId/discountCode` + relation, `Tenant.discounts`) + migration `20260731000001_add_discount_engine`; regenerated client; `order/dto/create-order-request.dto.ts` (optional `discountCode`); `order.service.ts` (resolve code inside checkout tx via `tx.discount.findUnique`, compute + persist discountAmount/discountId/discountCode, atomic usageCount increment); `order.service.spec.ts` (+3), `order.checkout.integration.spec.ts` (+2 + extended mockTransaction); `seed.ts` (demo SAVE10/FIXED5); `rbac-permission.guard.spec.ts` (client-regeneration knock-on) | ✅ VERIFIED 2026-07-31 — tsc 0/0/0; ESLint 6/6; api filter **296/2/0 (32/33)** + root **533/2/0 (61/62)** (was 279/2/0 + 516/2/0; +17 new tests); build 6/6. **Runtime** (fresh `zayjar_rt6`, 8-migration chain clean, 31 tables, seed incl. 2 discounts): no code → 0 discount / total 27.60; `SAVE10` → 2.40 (10% of 24) persisted + usageCount 1; `FIXED5` → 5.00 persisted + usageCount 1; invalid `NOPE` → 400 uniform `The discount code is invalid or expired.` (no order, no usage); **cross-tenant isolation** — albaik's `SAVE10` on a fresh onboarded tenant → 400, usage unchanged | 
| Sprint 2 | Task 3 — Order.paymentMethod persistence (§15 item 3) | see §19 row 31 (local, pre-push) | 13 files — `packages/db/prisma/schema.prisma` (Order.paymentMethod `PaymentMethodType?`), new migration `20260731000000_add_order_payment_method` (nullable column; no fabricated backfill), regenerated Prisma client (`packages/db/src/generated-client/*`), `apps/api/src/order/order.service.ts` (persist `dto.paymentMethod` on the single shared create path), spec updates (`order.service.spec.ts` create assertion, `order.checkout.integration.spec.ts` response assertion + mock field, `rbac-permission.guard.spec.ts` mock field knock-on), PROJECT_STATE.md record | ✅ VERIFIED 2026-07-31 — tsc api 0 / db 0 / types 0; ESLint 6/6; order suites 60/60; api filter **279/2/0** + root **516/2/0** (unchanged — no regression); build chain 6/6 serialized. **Runtime** (fresh DB `zayjar_rt5`, migrations M1→M2→M3→M5→M6→**M7** clean, 30 tables + new column): staff checkout **ORD-2026-79953** → response `paymentMethod: CASH` + DB row `paymentMethod=CASH`; guest checkout (public QR path) **ORD-2026-46938** → `CASH` persisted + echoed; pre-existing seeded order stays `NULL` (behavior preserved, no backfill). Client regeneration note: the committed generated client was stale (predated `branding` + KDS indexes — LATENT-BRAND-001); regenerating for this schema change materialized them too — the client now matches `schema.prisma` exactly (see §7/§20 LATENT-BRAND-001 update) |
| Sprint 2 | Task 2 — RT-ONB-002 fix (onboarding RBAC provisioning) | see §19 row 30 (local, pre-push) | 4 files, +150/−10 — `apps/api/src/tenant/tenant.service.ts` (D1: clone the canonical seeded RESTAURANT_OWNER permission set onto the new per-tenant role, fail-fast on missing baseline), `packages/db/prisma/seed.ts` (+8 guard-vocabulary permission rows: product read/create, branch create, table read/create, order create/update, tenant update — owner gets "everything" automatically; manager/cashier/kitchen unchanged by filter construction), `tenant.service.spec.ts` (+2 fail-fast tests, provisioning assertions), `common/e2e.spec.ts` (provisioning assertion) | ✅ VERIFIED 2026-07-31 — gates: api filter **279/2/0** (was 277/2/0; +2 new tenant tests), root **516/2/0** (was 514/2/0), tsc api 0 / db 0 / types 0, ESLint 6/6, build chain 6/6 serialized. **Runtime matrix** (fresh DB `zayjar_rt4`, PostgreSQL 17.10 + Redis 8.0.2 + API :8000): seed → 22 permission rows, owner role **22**, manager 10 / cashier 6 / kitchen 4 (unchanged — backward compatible); onboard 201 → **SQL set-parity proof: onboarded owner role permissions ≡ seeded owner role permissions (22=22, identical both directions)**; staff flow as the onboarded owner — branches 200, menu/products 200, category 201, product 201, **checkout 201** (was 403), tables 200, table create 201 (64-char QR), branch create 201, tenant update 200 (growth plan); **fail-fast runtime proof:** DB with plans/permissions but no roles → onboard **500** with log `Cannot provision owner permissions: no RESTAURANT_OWNER role exists in the database. Run the canonical seed to provision the baseline roles.` |
| Sprint 2 | Task 1 — Staff Authentication UI (Auth-UI gap, §15 item 6) | see §19 row 29 (local, pre-push) | 17 files, +1,065/−44 — standalone login flow wired to the existing backend auth surface for BOTH staff apps: `apps/cashier` + `apps/backoffice` each gain `src/app/lib/auth.ts` (login/MFA/session/CSRF/logout client + `resolveApiBase` subdomain-preserving dev mapping), `src/app/components/LoginForm.tsx` (+spec), `src/app/login/page.tsx`, and an auth gate on the entry page (cashier `/` redirects to `/login`; backoffice `/` redirects to `/login` instead of the creation wizard — wizard stays at `/setup`). `CashierTerminal.tsx`: CSRF double-submit echo on staff checkout (`X-CSRF-Token`), branch guard, Sign-out (server blacklist + local clear). Zero backend files changed | ✅ VERIFIED 2026-07-31 — gates: cashier 28/28 (3 suites), backoffice 42/42 (5 suites), api filter 277/2/0 (31/32) + root 514/2/0 (60/61) unchanged (new specs are `.spec.tsx`, excluded from the root-node run by design), tsc api 0 / db 0 / types 0, ESLint 6/6, build chain 6/6 serialized. **Live runtime matrix** (native PostgreSQL 17.10 + Redis 8.0.2 + API :8000, fresh DB `zayjar_rt3`): onboarding 201 → login **200** (exact response shape the client parses: accessToken/csrfToken/expiresIn/user{id,tenantId,email,roles,firstName,lastName,mfaEnabled}); wrong password **401** `Invalid credentials`; unknown tenant host **403** middleware; `Set-Cookie: __Host-CSRF-Token` JS-readable + `__Host-Refresh-Token` HttpOnly; CSRF echo (`X-CSRF-Token`) accepted on authenticated mutations; MFA enable 200 → verify with real TOTP 200 (`mfaEnabled: true`) → login without code **401** `"MFA token required"` (exact string the form detects) → login with TOTP **200**; logout 200 `{success:true}` → same token now **401** (blacklisted). **New pre-existing defect discovered (RT-ONB-002):** onboarded-tenant owners get a per-tenant `RESTAURANT_OWNER` role with ZERO `role_permissions` → RBAC-gated staff endpoints 403 (`read Product` / `read Branch` live-verified) — backend provisioning gap, out of this task's scope, recorded §7/§20/§23 |
| Sprint 1 | Step 2 — guest checkout + DEFECT-A + DEFECT-B | `0741d5b` | 8 files, 710+/24− — `public-order.controller.ts` (new), `order.service.ts`, `create-order-request.dto.ts`, `order.module.ts`, `TenantTableRepository.ts`, 3 spec files | order 50/50, menu 23/23, kds 41/41, full api 478 passed / 1 documented pre-existing failure / 2 skipped; ESLint 0; tsc 149→145 (4 removed on touched lines, 0 added); db tsc unchanged at 16 |
| Sprint 1 | Step 3 — qr-menu SSR + real cart + checkout wiring | `5967db9` | 10 files, 1157+/269− — `page.tsx` SSR rewrite, `MenuBrowser.tsx` cart/checkout + `MenuBrowser.spec.tsx` updates, new `lib/` (4 source files + 2 specs), `next.config.mjs` | qr-menu build PASS (route `/` = ƒ Dynamic SSR in build output); qr-menu tests 28/28 (3 suites); zero mock data remains in production src (grep-verified); api order 50/50 + menu 23/23 regression green |
| Sprint 1 | Step 4 — live end-to-end verification | — (verification-only; zero repo changes) | Native runtime substituted for absent Docker: PostgreSQL 17.10 + Redis 8.0.2 + API `dist/main.js` :8000 + qr-menu `next dev` :3000 + `/etc/hosts` tenant subdomain; fixture-based data (repo seed then non-executable → R5) | ✅ VERIFIED 2026-07-30: menu/table GETs 200 (Decimal→Number, branding); uniform anti-oracle 404; 403 no-tenant; gating UNPAID/CANCELED→403 vs PAST_DUE/ACTIVE→200; 4× checkout 201 with server totals (42.55 / 5.75 / 11.50 / 16.67 — last via qr-menu rewrite proxy = browser path); `tableId` server-forced; `kitchen_queues` rows; WS `ticket.created` real names; SSR branding + error views; **1 CANNOT VERIFY** (pointer cart clicks — no browser automation; covered via proxy path). Side findings: R1 CONFIRMED, R2–R5 red, LATENT-ENV-001 confirmed, LATENT-MEDIA-001 runtime fear disproven. Evidence: `/home/user/e2e/` (outside repo) |
| Post-Step-4 | Runtime Defect R4 (PREX-MIG-001) fix | `c15843f` (local, pre-push) | 2 files, 10+/5− — `20260726100001_add_lifecycle_triggers/migration.sql` identifier corrections + PROJECT_STATE.md record | ✅ VERIFIED 2026-07-30: 8/8 triggered tables accept UPDATEs (backdate-probe override); audit conditional + literals preserved; `PUT /api/v1/orders/:id/status` → HTTP 200 (Step-4: 500); api suite 275 pass / 2 skip / 1 pre-existing fail = baseline |
| Post-Step-4 | Runtime Defect R5 (PREX-SEED-001) fix | `084dd37` (local, pre-push) | 2 files, 107+/102− — `seed.ts` 93 deterministic UUID IDs + PROJECT_STATE.md record | ✅ VERIFIED 2026-07-30: complete fresh-DB seed ×2 via canonical `pnpm prisma:seed`; demo tenant/restaurant/products/categories/QR tokens SQL-verified; db lint 0, db tsc 16, api suite 275/2/1 = baselines |
| Post-Step-4 | Runtime Defect R1 (LATENT-KDS-001) fix | see §19 row 7 (local, pre-push) | 2 files — `kds.service.ts:76` one-line relation ordering + PROJECT_STATE.md record | ✅ VERIFIED 2026-07-30: tsc kds 6→1 / api 145→140 (0 added); kds 41/41 + full api 275/2/1 = baselines; live `GET /api/v1/kds/tickets` → HTTP 200 (was 500), oldest-first order, status filter + hydration + 404 path intact |
| Post-Step-4 | Runtime Defect R3 (PREX-MIG-003) fix | see §19 row 8 (local, pre-push) | 2 files — `20260726100000_add_search_indexes/migration.sql` (5 quoted-identifier corrections + provenance comment) + PROJECT_STATE.md record | ✅ VERIFIED 2026-07-30: fresh-DB chain M1→M2→M3→M5→M6 clean (M5 previously aborted at line 14 `column "tenant_id" does not exist`); `tsv_menu_search` generated column + all 3 indexes live with schema-map names; FTS A/B-weight probe functional; tsc api 140 / db 16, api suites 275 pass / 2 skip / 1 pre-existing fail, kds 41/41 = baselines |
| Post-Step-4 | Runtime Defect R6 (guest-checkout gating parity) fix | see §19 row 9 (local, pre-push) | 4 files — `order.service.ts` tenant-status gate + `order.service.spec.ts` G6 coverage + `order.checkout.integration.spec.ts` P1/P5 fixtures + PROJECT_STATE.md record | ✅ VERIFIED 2026-07-30: checkout now 403 under tenant UNPAID/CANCELED (was 201), ACTIVE 201 / PAST_DUE grace 201 preserved, uniform message parity with menu surface; zero order leakage during gated states; suites 276 pass / 2 skip / 1 pre-existing fail (**new baseline**, +1 R6 test), order suites 51/51, tsc api 140 / db 16, kds 41/41 |
| Sprint-1 Step-5 | Runtime Defect R7 (RT-ONB-001; root cause of canary PREX-E2E-001) fix | see §19 row 10 (local, pre-push) | 2 code/spec files, +17/−1 — `tenant.service.ts` platform-scope wrapper on `onboard` (+13/−1) + `common/e2e.spec.ts` hermetic mock (+4) — + PROJECT_STATE.md record | ✅ VERIFIED 2026-07-30: pre-fix live 500 with verbatim `Fail-Safe Block: Cross-tenant data insertion attempt detected and blocked.` → post-fix anonymous onboard 201 (full provisioning SQL-counted), 409 dup-subdomain / 409 dup-email / 403 apex invariant; full api **277 pass / 2 skip / 0 fail** (new baseline), tsc api 140 / db 16 unchanged, eslint 0 on modified files |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-1a (PREX-TSC-002 — db delegate-interface root) fix | see §19 row 12 (local, pre-push) | 2 files — `packages/db/src/repositories/BaseTenantRepository.ts` (ctor `unknown`-unify at choke point) + PROJECT_STATE.md record | ✅ VERIFIED 2026-07-30: db tsc **16 → 0** (1 unique defect eliminated all 16 TS2345), api tsc 140 unchanged (0 added), db build leg GREEN (tsc + client copy), suites 277/2/0 = baseline, db eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-5 (third-party library type drift — 6 library roots) fix | see §19 row 13 (local, pre-push) | 7 code files + PROJECT_STATE.md record — `jwt.config.ts`, `public.decorator.ts`, `require-permission.decorator.ts`, `casl-ability.factory.ts`, `rbac-permission.guard.ts`, `datadog-apm.ts`, `logger.service.ts` | ✅ VERIFIED 2026-07-30: api tsc **140 → 127** (exactly −13 predicted), db tsc 0 maintained, suites 277/2/0 = baseline, full-api eslint 0, current-user.decorator (CAT-3a) untouched per scope |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-6 (null/undefined narrowing — subscription plan guards + Stripe eventId root) fix | see §19 row 14 (local, pre-push) | 2 code files + PROJECT_STATE.md record — `subscription.service.ts` (4 guard blocks, +16/−0), `billing.service.ts` (choke-point narrowing + contract-honest `action`, +11/−10 net) | ✅ VERIFIED 2026-07-30: api tsc **127 → 117** (exactly −10 predicted; verbatim delta = the 10 CAT-6 lines, 0 genuinely added), db tsc 0 maintained, suites 277/2/0 = baseline, full-api eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-1c (Prisma write-input strictness — create-array / nullable-Json / enum writes / Decimal read-contracts) fix | see §19 row 15 (local, pre-push) | 5 code files + PROJECT_STATE.md record — `order.service.ts`, `audit.service.ts`, `billing.service.ts`, `tenant.service.ts`, `admin.service.ts` | ✅ VERIFIED 2026-07-30: api tsc **117 → 110** (exactly −7 predicted; normalized-signature delta = 6 signatures/7 sites retired, 0 new), db tsc 0 maintained, suites 277/2/0 = baseline, full-api eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-4 (response/return-contract drift — service contracts re-declared to the verified runtime truth) fix | see §19 row 16 (local, pre-push) | 5 code files + PROJECT_STATE.md record — `email.service.ts`, `tenant.service.ts`, `billing.service.ts`, `kds.controller.ts`, `wallet.service.ts` | ✅ VERIFIED 2026-07-30: api tsc **110 → 100** (exactly −10 predicted; normalized-signature delta = 9 signatures/10 sites retired, 0 new), db tsc 0 maintained, suites 277/2/0 = baseline, full-api eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-3b (AuthenticatedUser/Request contract — dead `.sub` fallback reads, rawBody cast hop, spec fixture contracts) fix | see §19 row 17 (local, pre-push) | 5 code/spec files + PROJECT_STATE.md record — `asset.controller.ts`, `billing.controller.ts`, `device-token.controller.ts`, `payment.controller.ts`, `media.controller.integration.spec.ts` | ✅ VERIFIED 2026-07-30: api tsc **100 → 89** (exactly −11 predicted; sorted-list delta verbatim the 11 recorded CAT-3b errors, normalized-signature 11/0), db tsc 0 maintained, suites 277/2/0 = baseline, full eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-3a (Express HTTP-layer typing drift — request/response contracts vs @types/express 5.0.6) fix | see §19 row 18 (local, pre-push) | 7 code/spec files + PROJECT_STATE.md record — `audit.interceptor.ts`, `audit.service.ts` (AuditLogEntry), `current-user.decorator.ts`, `http-logging.middleware.ts`, `correlation-id.middleware.spec.ts`, `media.controller.ts`, `order.controller.ts` | ✅ VERIFIED 2026-07-31: api tsc **89 → 66** (exactly −23 predicted; sorted-list delta verbatim the 23 recorded CAT-3a errors, normalized-signature 23/0; one masked knock-on unmasked + resolved in-root), db tsc 0 maintained, suites 277/2/0 = baseline, full eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-2 (loose service payloads — Stripe event objects, notification dispatch payloads, invoice DTO, webhook catch) fix | see §19 row 19 (local, pre-push) | 6 code files + PROJECT_STATE.md record — `billing.service.ts`, `dispatch.service.ts`, `worker.ts`, `order.service.ts`, `webhook.service.ts`, `email.service.ts` | ✅ VERIFIED 2026-07-31: api tsc **66 → 37** (exactly −29 predicted; sorted-list delta verbatim the 29 recorded CAT-2 errors, normalized-signature 29/0; assertion-only — emitted JS byte-identical), db tsc 0 maintained, suites 277/2/0 = baseline, full eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-1b (spec mocks vs the $extended Prisma client — order lifecycle fixture, audit/subscription mock implementations, tenant $transaction implementations) fix | see §19 row 20 (local, pre-push) | 4 spec files + PROJECT_STATE.md record — `order.service.spec.ts`, `audit.service.spec.ts`, `subscription.service.spec.ts`, `tenant.service.spec.ts` | ✅ VERIFIED 2026-07-31: api tsc **37 → 24** (exactly −13 predicted; sorted-list delta verbatim the 13 recorded CAT-1b errors, normalized-signature 13/0; type-only assertions/annotations inside specs — zero runtime surface), db tsc 0 maintained, suites 277/2/0 = baseline, full eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-7 (spec mocks vs repo type surfaces — jest.mock factory self-reference, interface-declared jest mocks, FCM-payload unknown reads, drifted concurrency seed) fix | see §19 row 21 (local, pre-push) | 4 spec files + PROJECT_STATE.md record — `media.service.spec.ts`, `media-retry-cleanup.spec.ts`, `device-token.service.spec.ts`, `media-concurrency.integration.spec.ts` | ✅ VERIFIED 2026-07-31: api tsc **24 → 7** (exactly −17 predicted; sorted-list delta verbatim the 17 recorded CAT-7 errors, normalized-signature 17/0; type-only spec edits — zero runtime surface), db tsc 0 maintained, suites 277/2/0 = baseline, full eslint 0 |
| Sprint-1 remediation (TS program, CTO mandate) | TypeScript blocker CAT-8 (final projection/nullability debts — media retry-locals + config tuples, tenant stale-client branding, queue-config index) fix | see §19 row 22 (local, pre-push) | 3 code files + PROJECT_STATE.md record — `media.service.ts`, `tenant.service.ts`, `queue-health.service.ts` | ✅ VERIFIED 2026-07-31: api tsc **7 → 0 — PREX-TSC-001 eliminated** (exactly −7; sorted-list delta verbatim the 7 recorded CAT-8 errors, 0 added; type-only assertions/annotations — zero runtime delta), db tsc 0 maintained, suites 277/2/0 = baseline, full eslint 0 |

Pre-Sprint-1 baseline (upstream `df51f37` "Fix restaurant setup flow" and earlier): full platform scaffolding — monorepo, all four apps, DB schema + tenant extension, auth/JWT, billing, KDS gateway, CI, k8s, docs.

---

# 6. Current Sprint Status

**Sprint 1 — "Public QR Ordering Channel fully functional end-to-end":**

| Step | Status |
|---|---|
| Step 1 — public guest read API | ✅ Complete (committed) |
| Step 2 — checkout token verification + DEFECT-A + DEFECT-B | ✅ Complete (committed) |
| Step 3 — qr-menu SSR/cart/checkout | ✅ Complete (committed) |
| Step 4 — e2e/local live verification | ✅ Complete 2026-07-30 — executed on a **native runtime substituted for absent Docker** (PostgreSQL 17.10 + Redis 8.0.2 + API :8000 + qr-menu :3000; §17 degraded path honored); results + 1 CANNOT VERIFY item in §19 row 4; evidence `/home/user/e2e/` |
| Step 5 — final gate (lint 0, suites green, `turbo build`, final commit) | ⚠️ **EXECUTED 2026-07-30** under CTO mandate — full results §19 row 11 / §21: ESLint ✅ 6/6 workspaces; suites ✅ **277/2/0** (R7 fixed in-gate per defect policy A; PREX-E2E-001 closed); TypeScript ❌ api 140 / db 16 (PREX-TSC-001/002 — larger-refactor class, documented, not fixed per mandate); build ❌ on db+api tsc only (all 3 frontends individually PASS; concurrent turbo failure reproduced as sandbox resource contention); Docker ⚠ CANNOT VERIFY (no daemon — environment-only); runtime sanity ✅ (fresh chain 5/5 + canonical seed + boot + health/table/menu 200); PROJECT_STATE consistency ✅. **Sprint closure blocked by the TS gate; CTO-mandated remediation program IN PROGRESS — CAT-1a complete 2026-07-30 (db tsc 16 → 0, §19 row 12); CAT-5 complete 2026-07-30 (api tsc 140 → 127, §19 row 13); CAT-6 complete 2026-07-30 (api tsc 127 → 117, §19 row 14); CAT-1c complete 2026-07-30 (api tsc 117 → 110, §19 row 15); CAT-4 complete 2026-07-30 (api tsc 110 → 100, §19 row 16); CAT-3b complete 2026-07-30 (api tsc 100 → 89, §19 row 17); CAT-3a complete 2026-07-31 (api tsc 89 → 66, §19 row 18); CAT-2 complete 2026-07-31 (api tsc 66 → 37, §19 row 19); CAT-1b complete 2026-07-31 (api tsc 37 → 24, §19 row 20); CAT-7 complete 2026-07-31 (api tsc 24 → 7, §19 row 21); CAT-8 complete 2026-07-31 (api tsc 7 → 0, §19 row 22); **PREX-TSC-001 eliminated — api tsc = 0 (db = 0); Sprint-1 Step-5 **RE-EXECUTED 2026-07-31** under CTO execution command (§19 row 23 / §21): ALL attainable criteria GREEN — TypeScript 0/0/0, ESLint 6/6, suites 277/2/0 (filter) + 496/2/0 (root), build chain 6/6 (per-package + official serialized turbo), runtime probes exact on fresh `zayjar_step5`, consistency ✅; Docker/CI-Job-4 environment-only CANNOT VERIFY; zero in-gate code changes; closure decision with the CTO**** |

Post-Step-4 approved fixes (one isolated commit each): **R4 `c15843f`**, **R5 `084dd37`**, **R1 (see §19 row 7)**, **R3 (see §19 row 8)**, **R6 — guest-checkout gating parity (see §19 row 9)** (all 2026-07-30 — details §5 table and §19), **R7 — public-onboarding fail-safe block (see §19 row 10)** (Step-5 mandate, defect policy A, 2026-07-30). Guest-checkout subscription-gating parity FIXED+VERIFIED 2026-07-30 (R6, §19 row 9). Public onboarding FIXED+VERIFIED 2026-07-30 (R7, §19 row 10 — canary PREX-E2E-001 closed). Remaining verified runtime defects: **R2 = PREX-MIG-002** — CLOSED 2026-07-30 as investigation-complete (CTO decision): layer-1 partition-key identifiers proven correct, layer-2 architectural incompatibility proven (R2.1 option analysis A–F) — M4 parked non-shippable pending architecture RFC (§20 row).

**Repository safety note (verified repeatedly — 5+ sandbox wipe cycles by 2026-07-30):** the session sandbox periodically wipes `.git/config` (including remote + identity), `node_modules`, `dist/`, and **locally created commits**; working-tree file contents survive. All sprint/fix commits have been deterministically re-created from file contents each time (stat-identical). Durable persistence requires a push to GitHub. **Push policy (user directive, 2026-07-30 — supersedes the earlier "blocked: no remote/credentials" phrasing):** pushes are performed personally by the user; the session never pushes and never requests credentials. `origin` (`https://github.com/amarspk/zayjar-specification.git`) is re-added in-session as needed — `.git/config` is snapshot-excluded, so its presence is not durable. Wiped-and-recreated local hashes on record (identical contents/stats, new timestamps): R4 = `8b65531` → `4ab74e0` → **`c15843f`**; R5 = `1325e95` → **`084dd37`**. Pinned 2026-07-30 (recovery + history-reconstruction accepted by CTO): R1 = `ab2a06b` → **`eaf6618`**; R3 = `6033246` → **`d090d62`**; R6 = `e759a58` → **`bf56b7c`** (contents/stats byte-verified identical to the wiped originals). Second 2026-07-30 wipe cycle pinned (same stat-identical re-creation): R7 = `64e8c29` → `3fcd3a3` → **`082f33f`**; Step-5 docs = `e690c89` → `73df31c` → **`7ebf0dd`**. Third cycle pinned: CAT-1a = `6ccd7a9` → **`628e7d8`** (stat-identical). Fourth + fifth + sixth cycles pinned (identical reconstruction protocol; anchors reproduced each time: CAT-1a +27/−11, CAT-5 +62/−22, CAT-6 +30/−11, CAT-1c +38/−20): CAT-1a = `6ccd7a9` → `628e7d8` → `7048b62` → `ec636d4` → **`46aee42`**; CAT-5 = `8c2e856` → `e9f11c2` → `bb93efd` → **`bff07dd`**; CAT-6 = `b4733a8` → `5ef52f9` → **`b1363ae`**; CAT-1c = `2d4595f` → **`99b1733`**. Seventh cycle pinned (identical reconstruction protocol; anchors reproduced each time, now including CAT-4 +97/−37): CAT-1a = `6ccd7a9` → `628e7d8` → `7048b62` → `ec636d4` → `46aee42` → **`7f30084`**; CAT-5 = `8c2e856` → `e9f11c2` → `bb93efd` → `bff07dd` → **`192a5b5`**; CAT-6 = `b4733a8` → `5ef52f9` → `b1363ae` → **`106ec35`**; CAT-1c = `2d4595f` → `99b1733` → **`47a5f6a`**; CAT-4 = `09e3c3e` → **`e0b06b6`**. Eighth cycle pinned (identical reconstruction protocol; all six anchors reproduced: CAT-1a +27/−11, CAT-5 +62/−22, CAT-6 +30/−11, CAT-1c +38/−20, CAT-4 +97/−37, CAT-3b +23/−20): CAT-1a = `6ccd7a9` → `628e7d8` → `7048b62` → `ec636d4` → `46aee42` → `7f30084` → **`d89731d`**; CAT-5 = `8c2e856` → `e9f11c2` → `bb93efd` → `bff07dd` → `192a5b5` → **`5341cab`**; CAT-6 = `b4733a8` → `5ef52f9` → `b1363ae` → `106ec35` → **`636aa42`**; CAT-1c = `2d4595f` → `99b1733` → `47a5f6a` → **`d22023f`**; CAT-4 = `09e3c3e` → `e0b06b6` → **`e303d11`**; CAT-3b = `98fd62d` → **`e49b354`** (cycle-7 SHA-backed backup `cat-recon-backup/c7/` + layer file `c8/` made this the fastest full reconstruction yet). Ninth cycle pinned (identical reconstruction protocol; all seven anchors reproduced: CAT-1a +27/−11, CAT-5 +62/−22, CAT-6 +30/−11, CAT-1c +38/−20, CAT-4 +97/−37, CAT-3b +23/−20, CAT-3a +35/−22): CAT-1a = `6ccd7a9` → `628e7d8` → `7048b62` → `ec636d4` → `46aee42` → `7f30084` → `d89731d` → **`526619e`**; CAT-5 = `8c2e856` → `e9f11c2` → `bb93efd` → `bff07dd` → `192a5b5` → `5341cab` → **`8baaa45`**; CAT-6 = `b4733a8` → `5ef52f9` → `b1363ae` → `106ec35` → `636aa42` → **`e3c81e1`**; CAT-1c = `2d4595f` → `99b1733` → `47a5f6a` → `d22023f` → **`9d93347`**; CAT-4 = `09e3c3e` → `e0b06b6` → `e303d11` → **`b031879`**; CAT-3b = `98fd62d` → `e49b354` → **`86d9a95`**; CAT-3a = `6c99e1f` → **`97a4549`**. Tenth cycle pinned (identical reconstruction protocol; all eight anchors reproduced: CAT-1a +27/−11, CAT-5 +62/−22, CAT-6 +30/−11, CAT-1c +38/−20, CAT-4 +97/−37, CAT-3b +23/−20, CAT-3a +35/−22, CAT-2 +47/−45): CAT-1a = `6ccd7a9` → `628e7d8` → `7048b62` → `ec636d4` → `46aee42` → `7f30084` → `d89731d` → `526619e` → **`ac1f43d`**; CAT-5 = `8c2e856` → `e9f11c2` → `bb93efd` → `bff07dd` → `192a5b5` → `5341cab` → `8baaa45` → **`a5e6a5c`**; CAT-6 = `b4733a8` → `5ef52f9` → `b1363ae` → `106ec35` → `636aa42` → `e3c81e1` → **`5411b07`**; CAT-1c = `2d4595f` → `99b1733` → `47a5f6a` → `d22023f` → `9d93347` → **`050cc1b`**; CAT-4 = `09e3c3e` → `e0b06b6` → `e303d11` → `b031879` → **`44b0993`**; CAT-3b = `98fd62d` → `e49b354` → `86d9a95` → **`84b9fc5`**; CAT-3a = `6c99e1f` → `97a4549` → **`e550ec7`**; CAT-2 = `2f1903d` → **`8384856`**. Eleventh cycle pinned (identical reconstruction protocol; all nine anchors reproduced: CAT-1a +27/−11, CAT-5 +62/−22, CAT-6 +30/−11, CAT-1c +38/−20, CAT-4 +97/−37, CAT-3b +23/−20, CAT-3a +35/−22, CAT-2 +47/−45, CAT-1b +21/−19): CAT-1a = `6ccd7a9` → `628e7d8` → `7048b62` → `ec636d4` → `46aee42` → `7f30084` → `d89731d` → `526619e` → `ac1f43d` → **`e86da07`**; CAT-5 = `8c2e856` → `e9f11c2` → `bb93efd` → `bff07dd` → `192a5b5` → `5341cab` → `8baaa45` → `a5e6a5c` → **`3ac1f3e`**; CAT-6 = `b4733a8` → `5ef52f9` → `b1363ae` → `106ec35` → `636aa42` → `e3c81e1` → `5411b07` → **`84ec2f4`**; CAT-1c = `2d4595f` → `99b1733` → `47a5f6a` → `d22023f` → `9d93347` → `050cc1b` → **`13fab2e`**; CAT-4 = `09e3c3e` → `e0b06b6` → `e303d11` → `b031879` → `44b0993` → **`f5c829d`**; CAT-3b = `98fd62d` → `e49b354` → `86d9a95` → `84b9fc5` → **`72464bc`**; CAT-3a = `6c99e1f` → `97a4549` → `e550ec7` → **`4cdc419`**; CAT-2 = `2f1903d` → `8384856` → **`c31732e`**; CAT-1b = `218c4fe` → **`e6a73ee`**. Twelfth cycle pinned (identical reconstruction protocol; all ten anchors reproduced: CAT-1a +27/−11, CAT-5 +62/−22, CAT-6 +30/−11, CAT-1c +38/−20, CAT-4 +97/−37, CAT-3b +23/−20, CAT-3a +35/−22, CAT-2 +47/−45, CAT-1b +21/−19, CAT-7 +28/−19 from `cat-recon-backup/c12/`): CAT-1a = `6ccd7a9` → `628e7d8` → `7048b62` → `ec636d4` → `46aee42` → `7f30084` → `d89731d` → `526619e` → `ac1f43d` → **`53e8bf1`**; CAT-5 = `8c2e856` → `e9f11c2` → `bb93efd` → `bff07dd` → `192a5b5` → `5341cab` → `8baaa45` → `a5e6a5c` → **`7430acb`**; CAT-6 = `b4733a8` → `5ef52f9` → `b1363ae` → `106ec35` → `636aa42` → `e3c81e1` → `5411b07` → **`56b493b`**; CAT-1c = `2d4595f` → `99b1733` → `47a5f6a` → `d22023f` → `9d93347` → `050cc1b` → **`0479df7`**; CAT-4 = `09e3c3e` → `e0b06b6` → `e303d11` → `b031879` → `44b0993` → **`de3fe73`**; CAT-3b = `98fd62d` → `e49b354` → `86d9a95` → `84b9fc5` → **`1ef39b6`**; CAT-3a = `6c99e1f` → `97a4549` → `e550ec7` → `4cdc419` → **`7d80513`**; CAT-2 = `2f1903d` → `8384856` → `c31732e` → **`a2755a8`**; CAT-1b = `218c4fe` → `e6a73ee` → **`3e9f92a`**; CAT-7 = `65a504b` → **`8803525`**. Thirteenth cycle pinned (mid-turn wipe during CAT-8 — wipe between api-tsc=0 verification and lint/test gates; CAT-8 edits survived uncommitted in-tree, snapshotted to `/home/user/cat8-final/`, 10 layers rebuilt with the same ten anchors, edits re-applied from `cat-recon-backup/c13/`, all gates re-run): CAT-1a = `6ccd7a9` → `628e7d8` → `7048b62` → `ec636d4` → `46aee42` → `7f30084` → `d89731d` → `526619e` → `ac1f43d` → `53e8bf1` → **`3c774f9`**; CAT-5 = `8c2e856` → `e9f11c2` → `bb93efd` → `bff07dd` → `192a5b5` → `5341cab` → `8baaa45` → `a5e6a5c` → `7430acb` → **`6712edf`**; CAT-6 = `b4733a8` → `5ef52f9` → `b1363ae` → `106ec35` → `636aa42` → `e3c81e1` → `5411b07` → `56b493b` → **`6aabe17`**; CAT-1c = `2d4595f` → `99b1733` → `47a5f6a` → `d22023f` → `9d93347` → `050cc1b` → `0479df7` → **`dbd56dc`**; CAT-4 = `09e3c3e` → `e0b06b6` → `e303d11` → `b031879` → `44b0993` → `de3fe73` → **`c40fd2d`**; CAT-3b = `98fd62d` → `e49b354` → `86d9a95` → `84b9fc5` → `1ef39b6` → **`bcec3da`**; CAT-3a = `6c99e1f` → `97a4549` → `e550ec7` → `4cdc419` → `7d80513` → **`5b4d87d`**; CAT-2 = `2f1903d` → `8384856` → `c31732e` → `a2755a8` → **`d25876b`**; CAT-1b = `218c4fe` → `e6a73ee` → `3e9f92a` → **`f99f570`**; CAT-7 = `65a504b` → `8803525` → **`da702d7`**. Cycle-7 was the first to also wipe `/tmp` (verified error lists + PSM layer files lost): the CAT-1a-era PSM layer was re-derived from the sha-pinned layerB backup by inverse-editing the hunk-level CAT-1a/CAT-5 classification that the +27/−11 and +62/−22 anchors jointly force to be unique (derived layer diffed exactly +12/−10 vs `7ebf0dd`; final reconstructed tree converged byte-identical to the pre-ritual working tree across all 18 files; derived layer sha256 `7a7016df…` now pinned in the durable `cat-recon-backup/c7/`). Cycle-5/6 verification strengthened beyond stats: the fact-derived CAT-5 PSM layer converged byte-identical (sha256 `bcbee362…`) to the independent cycle-4 backup restore, and each reconstructed HEAD state reproduced the previous cycle's verified api tsc list byte-for-byte (117 at cycle 5; 110 at cycle 6; 100 at cycle 7 — fresh sorted-list capture + per-module recount matched the recorded post-CAT-4 map exactly; 89 at cycle 8 — baseline byte-compared identical (`diff` clean) against the previous cycle's verified 89-error list; 66 at cycle 9 — same byte-identical baseline proof against the verified 66-error list; 37 at cycle 10 — same byte-identical baseline proof against the verified 37-error list (`cat-work/after-cat2.txt`); 24 at cycle 11 — same byte-identical baseline proof against the verified 24-error list (`cat-work/after-cat1b.txt`); 7 at cycle 12 — same byte-identical baseline proof against the verified 7-error list (`cat-work/after-cat7.txt`); 0 at cycle 13 — post-reconstruction api tsc reproduced exactly 0 with the 7-line sorted-list delta vs the cycle-12 baseline (7 removed, 0 added); cycle 13 was mid-turn — the wipe landed between the api-tsc=0 verification and the lint/test gates: `/home/user/cat8-final/` snapshot preserved the CAT-8 edits, the 10-layer history was rebuilt, the 3 edited files re-applied from `cat-recon-backup/c13/`, toolchain reinstalled, and **all gates re-run post-reconstruction** with identical results) before the new category work began. Current verified chain (newest → oldest, `git log` order): CAT-8 (single new commit, local pre-push; hash pinned at the next-cycle §6 pins per lineage rule) → `da702d7`(CAT-7) → `f99f570`(CAT-1b) → `d25876b`(CAT-2) → `5b4d87d`(CAT-3a) → `bcec3da`(CAT-3b) → `c40fd2d`(CAT-4) → `dbd56dc`(CAT-1c) → `6aabe17`(CAT-6) → `6712edf`(CAT-5) → `3c774f9`(CAT-1a) → `7ebf0dd`(Step-5 docs) → `082f33f`(R7) → `bf56b7c`(R6) → `d090d62`(R3) → `eaf6618`(R1) → `1b7f1e9` → `084dd37`(R5) → `c15843f`(R4) → `2ab226b` → `5967db9`(Step 3) → `0741d5b`(Step 2) → `d1c6035`(Step 1) → upstream `df51f37`. **Fourteenth cycle pinned** (2026-07-31, Step-5 re-run turn; identical reconstruction protocol — all eleven anchors reproduced: CAT-1a +27/−11, CAT-5 +62/−22, CAT-6 +30/−11, CAT-1c +38/−20, CAT-4 +97/−37, CAT-3b +23/−20, CAT-3a +35/−22, CAT-2 +47/−45, CAT-1b +21/−19, CAT-7 +28/−19, CAT-8 +30/−21 from `cat-recon-backup/root,c5–c13/`; baseline byte-identical to the verified zero-error CAT-8 list `cat-work/after-cat8.txt`; tree byte-identical to the pre-reconstruction snapshot `tree-cycle14-snapshot/`; PSM sha `f2ab2d33…` verified pre-update): CAT-1a `3c774f9` → **`9dbcc2d`**; CAT-5 `6712edf` → **`5355fe2`**; CAT-6 `6aabe17` → **`6531deb`**; CAT-1c `dbd56dc` → **`e0518b5`**; CAT-4 `c40fd2d` → **`0f9bc76`**; CAT-3b `bcec3da` → **`2454503`**; CAT-3a `5b4d87d` → **`f15f30a`**; CAT-2 `d25876b` → **`5c1be83`**; CAT-1b `f99f570` → **`b2b1eba`**; CAT-7 `da702d7` → **`3963b3c`**; CAT-8 `046a506` → **`45f4bee`**. Updated current verified chain (newest → oldest, `git log` order): `45f4bee`(CAT-8) → `3963b3c`(CAT-7) → `b2b1eba`(CAT-1b) → `5c1be83`(CAT-2) → `f15f30a`(CAT-3a) → `2454503`(CAT-3b) → `0f9bc76`(CAT-4) → `e0518b5`(CAT-1c) → `6531deb`(CAT-6) → `5355fe2`(CAT-5) → `9dbcc2d`(CAT-1a) → `7ebf0dd`(Step-5 docs) → `082f33f`(R7) → `bf56b7c`(R6) → `d090d62`(R3) → `eaf6618`(R1) → `1b7f1e9` → `084dd37`(R5) → `c15843f`(R4) → `2ab226b` → `5967db9`(Step 3) → `0741d5b`(Step 2) → `d1c6035`(Step 1) → upstream `df51f37`; the Step-5 re-run docs commit (§19 row 23) sits on top and its hash is pinned at the next cycle per the lineage rule. **Fifteenth cycle pinned** (2026-07-31, C-1/AUTHZ-001 turn; identical reconstruction protocol — all twelve anchors reproduced: CAT-1a +27/−11 … CAT-8 +30/−21, Step-5-rerun docs +20/−18 from `cat-recon-backup/c14/`; baseline byte-identical to the verified zero-error CAT-8 list; tree byte-identical to `tree-cycle15-snapshot/`: ) CAT-1a `9dbcc2d` → **`945c21b`**; CAT-5 `5355fe2` → **`b7b42e2`**; CAT-6 `6531deb` → **`1b765aa`**; CAT-1c `e0518b5` → **`bc75757`**; CAT-4 `0f9bc76` → **`cbdca1f`**; CAT-3b `2454503` → **`1af3743`**; CAT-3a `f15f30a` → **`48306e4`**; CAT-2 `5c1be83` → **`d96d7fd`**; CAT-1b `b2b1eba` → **`e13b992`**; CAT-7 `3963b3c` → **`a4cf9a2`**; CAT-8 `45f4bee` → **`0d67af5`**; Step-5-rerun docs `745d8d7` → **`b98c807`**. Updated current verified chain (newest → oldest): `b98c807`(Step-5-rerun docs) → `0d67af5`(CAT-8) → `a4cf9a2`(CAT-7) → `e13b992`(CAT-1b) → `d96d7fd`(CAT-2) → `48306e4`(CAT-3a) → `1af3743`(CAT-3b) → `cbdca1f`(CAT-4) → `bc75757`(CAT-1c) → `1b765aa`(CAT-6) → `b7b42e2`(CAT-5) → `945c21b`(CAT-1a) → `7ebf0dd`(Step-5 docs) → `082f33f`(R7) → `bf56b7c`(R6) → `d090d62`(R3) → `eaf6618`(R1) → `1b7f1e9` → `084dd37`(R5) → `c15843f`(R4) → `2ab226b` → `5967db9`(Step 3) → `0741d5b`(Step 2) → `d1c6035`(Step 1) → upstream `df51f37`; the C-1 fix commit (§19 row 24) sits on top and its hash is pinned at the next cycle per the lineage rule. **Sixteenth cycle pinned** (2026-07-31, C-2/AUTHZ-002 turn; identical reconstruction protocol — all thirteen anchors reproduced: CAT-1a +27/−11 … CAT-8 +30/−21, Step-5-rerun docs +20/−18 (`c14/`), C-1 +138/−3 (`c15/`); baseline byte-identical to the verified zero-error CAT-8 list; tree byte-identical to `tree-cycle16-snapshot/`; one iteration-honesty event recorded in §19 row 25: first attempt mislayered the untracked C-1 spec into CAT-1a (anchor mismatch caught, layer reset, re-run green)): CAT-1a `945c21b` → **`aa67e93`**; CAT-5 `b7b42e2` → **`c649fb1`**; CAT-6 `1b765aa` → **`b3e3631`**; CAT-1c `bc75757` → **`8f5d84f`**; CAT-4 `cbdca1f` → **`5289be4`**; CAT-3b `1af3743` → **`49e2df5`**; CAT-3a `48306e4` → **`b5729b6`**; CAT-2 `d96d7fd` → **`1c6798a`**; CAT-1b `e13b992` → **`eb9f66d`**; CAT-7 `a4cf9a2` → **`7f9d98a`**; CAT-8 `0d67af5` → **`e4a79c2`**; Step-5-rerun docs `b98c807` → **`827b53e`**; C-1 `ab72ecf` → **`6d39f2f`**. Updated current verified chain (newest → oldest): `6d39f2f`(C-1) → `827b53e`(Step-5-rerun docs) → `e4a79c2`(CAT-8) → `7f9d98a`(CAT-7) → `eb9f66d`(CAT-1b) → `1c6798a`(CAT-2) → `b5729b6`(CAT-3a) → `49e2df5`(CAT-3b) → `5289be4`(CAT-4) → `8f5d84f`(CAT-1c) → `b3e3631`(CAT-6) → `c649fb1`(CAT-5) → `aa67e93`(CAT-1a) → `7ebf0dd`(Step-5 docs) → `082f33f`(R7) → `bf56b7c`(R6) → `d090d62`(R3) → `eaf6618`(R1) → `1b7f1e9` → `084dd37`(R5) → `c15843f`(R4) → `2ab226b` → `5967db9`(Step 3) → `0741d5b`(Step 2) → `d1c6035`(Step 1) → upstream `df51f37`; the C-2 fix commit (§19 row 25) sits on top and its hash is pinned at the next cycle per the lineage rule. **Seventeenth cycle pinned** (2026-07-31, H-1/DEPLOY-001 turn; identical reconstruction protocol — all fourteen anchors reproduced: CAT-1a +27/−11, CAT-5 +62/−22, CAT-6 +30/−11, CAT-1c +38/−20, CAT-4 +97/−37, CAT-3b +23/−20, CAT-3a +35/−22, CAT-2 +47/−45, CAT-1b +21/−19, CAT-7 +28/−19, CAT-8 +30/−21, Step-5-rerun docs +20/−18 (`c14/`), C-1 +138/−3 (`c15/`), C-2 +112/−30 (`c16/`); baseline byte-identical to the verified zero-error CAT-8 list `cat-work/after-cat8.txt`; tree byte-identical to `tree-cycle17-snapshot/`; iteration honesty: (i) first CAT-1a attempt staged only the repository fix (anchor 1f 15/1 ≠ expected 2f 27/11 — halt-on-mismatch fired; the CAT-1a-era PSM was restored from its sha-pinned backup `c7/layerA1.psm`, +12/−10 vs `7ebf0dd` re-verified, re-run green); (ii) apt-installed postgresql-17/redis and the `/tmp` cluster did not survive this cycle — stack reinstalled, `zayjar_c1` rebuilt: 5/5 migrations + 16/16 seed): CAT-1a `aa67e93` → **`28d331c`**; CAT-5 `c649fb1` → **`b1b46d8`**; CAT-6 `b3e3631` → **`e90c148`**; CAT-1c `8f5d84f` → **`127993f`**; CAT-4 `5289be4` → **`775c499`**; CAT-3b `49e2df5` → **`d95c8ae`**; CAT-3a `b5729b6` → **`bc4ed56`**; CAT-2 `1c6798a` → **`52ad5c2`**; CAT-1b `eb9f66d` → **`e2ce400`**; CAT-7 `7f9d98a` → **`af6ff84`**; CAT-8 `e4a79c2` → **`6cdc1f4`**; Step-5-rerun docs `827b53e` → **`f467fd2`**; C-1 `6d39f2f` → **`d1a307f`**; C-2 `8039b8e` → **`f0ccca2`**. Updated current verified chain (newest → oldest, `git log` order): `f0ccca2`(C-2) → `d1a307f`(C-1) → `f467fd2`(Step-5-rerun docs) → `6cdc1f4`(CAT-8) → `af6ff84`(CAT-7) → `e2ce400`(CAT-1b) → `52ad5c2`(CAT-2) → `bc4ed56`(CAT-3a) → `d95c8ae`(CAT-3b) → `775c499`(CAT-4) → `127993f`(CAT-1c) → `e90c148`(CAT-6) → `b1b46d8`(CAT-5) → `28d331c`(CAT-1a) → `7ebf0dd`(Step-5 docs) → `082f33f`(R7) → `bf56b7c`(R6) → `d090d62`(R3) → `eaf6618`(R1) → `1b7f1e9` → `084dd37`(R5) → `c15843f`(R4) → `2ab226b` → `5967db9`(Step 3) → `0741d5b`(Step 2) → `d1c6035`(Step 1) → upstream `df51f37`; the H-1 fix commit (§19 row 26) sits on top and its hash is pinned at the next cycle per the lineage rule. **Eighteenth cycle pinned** (2026-07-31, H-2/DEPLOY-002 turn; identical reconstruction protocol — all fifteen anchors reproduced: CAT-1a +27/−11 … C-2 +112/−30 (`c16/`), H-1 +10/−6 (`c17/`); baseline byte-identical to the verified zero-error CAT-8 list; trees byte-identical to `tree-cycle18-snapshot/`; `/tmp` + apt-installed postgresql-17/redis SURVIVED this cycle — cluster `pgs6` reused, seed 16/16 re-verified, zero environment rebuild): CAT-1a `28d331c` → **`bdd6329`**; CAT-5 `b1b46d8` → **`00fbf91`**; CAT-6 `e90c148` → **`0046155`**; CAT-1c `127993f` → **`49a1467`**; CAT-4 `775c499` → **`e6937da`**; CAT-3b `d95c8ae` → **`1d4f1ac`**; CAT-3a `bc4ed56` → **`b1d1e3d`**; CAT-2 `52ad5c2` → **`b1f64df`**; CAT-1b `e2ce400` → **`6cc2581`**; CAT-7 `af6ff84` → **`39900ad`**; CAT-8 `6cdc1f4` → **`f9f6132`**; Step-5-rerun docs `f467fd2` → **`7c7dd14`**; C-1 `d1a307f` → **`feba959`**; C-2 `f0ccca2` → **`758968b`**; H-1 `6791933` → **`948e278`**. Updated current verified chain (newest → oldest, `git log` order): `948e278`(H-1) → `758968b`(C-2) → `feba959`(C-1) → `7c7dd14`(Step-5-rerun docs) → `f9f6132`(CAT-8) → `39900ad`(CAT-7) → `6cc2581`(CAT-1b) → `b1f64df`(CAT-2) → `b1d1e3d`(CAT-3a) → `1d4f1ac`(CAT-3b) → `e6937da`(CAT-4) → `49a1467`(CAT-1c) → `0046155`(CAT-6) → `00fbf91`(CAT-5) → `bdd6329`(CAT-1a) → `7ebf0dd`(Step-5 docs) → `082f33f`(R7) → `bf56b7c`(R6) → `d090d62`(R3) → `eaf6618`(R1) → `1b7f1e9` → `084dd37`(R5) → `c15843f`(R4) → `2ab226b` → `5967db9`(Step 3) → `0741d5b`(Step 2) → `d1c6035`(Step 1) → upstream `df51f37`; the H-2 fix commit (§19 row 27) sits on top and its hash is pinned at the next cycle per the lineage rule. **Nineteenth cycle pinned** (2026-07-31, H-3/DEPLOY-003 turn; identical reconstruction protocol — all sixteen anchors reproduced: CAT-1a +27/−11 … C-2 +112/−30 (`c16/`), H-1 +10/−6 (`c17/`), H-2 +92/−1 (`c18/`); baseline byte-identical to the verified zero-error CAT-8 list; trees byte-identical to `tree-cycle19-snapshot/`; `/tmp` + apt stack survived; kubectl v1.30.0 provisioned in-sandbox for client-side k8s verification — zero repo delta): CAT-1a `bdd6329` → **`5ff6ebe`**; CAT-5 `00fbf91` → **`78a6c2e`**; CAT-6 `0046155` → **`e6d05ae`**; CAT-1c `49a1467` → **`bbd3233`**; CAT-4 `e6937da` → **`612beb3`**; CAT-3b `1d4f1ac` → **`e15bf4a`**; CAT-3a `b1d1e3d` → **`23c3794`**; CAT-2 `b1f64df` → **`3101052`**; CAT-1b `6cc2581` → **`fc8f1d8`**; CAT-7 `39900ad` → **`de5c038`**; CAT-8 `f9f6132` → **`4c14b6d`**; Step-5-rerun docs `7c7dd14` → **`ceff865`**; C-1 `feba959` → **`a6c844a`**; C-2 `758968b` → **`76de82a`**; H-1 `948e278` → **`39c6c80`**; H-2 `695ffcb` → **`55c359e`**. Updated current verified chain (newest → oldest, `git log` order): `55c359e`(H-2) → `39c6c80`(H-1) → `76de82a`(C-2) → `a6c844a`(C-1) → `ceff865`(Step-5-rerun docs) → `4c14b6d`(CAT-8) → `de5c038`(CAT-7) → `fc8f1d8`(CAT-1b) → `3101052`(CAT-2) → `23c3794`(CAT-3a) → `e15bf4a`(CAT-3b) → `612beb3`(CAT-4) → `bbd3233`(CAT-1c) → `e6d05ae`(CAT-6) → `78a6c2e`(CAT-5) → `5ff6ebe`(CAT-1a) → `7ebf0dd`(Step-5 docs) → `082f33f`(R7) → `bf56b7c`(R6) → `d090d62`(R3) → `eaf6618`(R1) → `1b7f1e9` → `084dd37`(R5) → `c15843f`(R4) → `2ab226b` → `5967db9`(Step 3) → `0741d5b`(Step 2) → `d1c6035`(Step 1) → upstream `df51f37`; the H-3 fix commit (§19 row 28) sits on top and its hash is pinned at the next cycle per the lineage rule.

---

# 7. Known Defects

| ID | Description | Status | Affected modules |
|---|---|---|---|
| DEFECT-A | `variantId` was stored on order items but variant price never applied (pricing used basePrice + sizeAdjustment only), violating DOC-005 §4.3 Condition C absolute override | **FIXED in Step 2** (`order.service.ts:172–180` pricing loop: variant absolute price, `stockQuantity<=0` rejection, size skipped when variant selected; unit tests `order.service.spec.ts:623/668/695` + integration P5) | order |
| DEFECT-B | Transaction `include` carried only `orderItemAddons`; WS `ticket.created` payload read `item.product?.name` → `undefined` → "Unknown Product" on live KDS tickets | **FIXED in Step 2** (post-transaction name-resolution read at `order.service.ts:291`, response payload byte-identical; tests `order.service.spec.ts:714` + `:781` + real-gateway integration) | order → kds |
| PREX-E2E-001 | `src/common/e2e.spec.ts` "E2E Verification 1: Tenant Onboarding" failed with `Fail-Safe Block: Access to model 'User' was blocked due to missing tenant context` (`packages/db/src/index.ts:34`). Proven pre-existing on pristine HEAD via stash test; reproduced in live full-suite run 2026-07-30 | **CLOSED 2026-07-30 — demonstrable canary of runtime defect R7/RT-ONB-001** (public onboarding dead in production): resolved by the R7 service fix (platform-scope wrapper) + spec hermeticity repair; suite now green (full api 277/2/0). See RT-ONB-001 row | common (e2e spec), db extension |
| PREX-TSC-001 | Pre-existing TypeScript debt in `apps/api`: **0 errors currently — ELIMINATED 2026-07-31** (149 at Sprint-1 start; −4 removed on Step-2-touched lines; −5 removed by the R1/LATENT-KDS-001 one-line fix on 2026-07-30 — its own TS2353 plus 4 knock-on collapses in the same module; **−13 removed by CAT-5 on 2026-07-30**, §19 row 13; **−10 removed by CAT-6 on 2026-07-30**, §19 row 14; **−7 removed by CAT-1c on 2026-07-30**, §19 row 15; **−10 removed by CAT-4 on 2026-07-30**, §19 row 16; **−11 removed by CAT-3b on 2026-07-30**, §19 row 17; **−23 removed by CAT-3a on 2026-07-31**, §19 row 18; **−29 removed by CAT-2 on 2026-07-31**, §19 row 19; **−13 removed by CAT-1b on 2026-07-31**, §19 row 20; **−17 removed by CAT-7 on 2026-07-31**, §19 row 21; **−7 removed by CAT-8 on 2026-07-31**, §19 row 22; 0 introduced by Sprint 1 — re-verified 2026-07-30). `tsc` is the api `build` script → **production build (`pnpm build`) and CI Job 3 "Build Verification" fail on main** (verified: build script `tsc`, tsconfig includes `src/**/*`, CI runs `pnpm build`; Dockerfile api builder runs `pnpm run build`) | **FIXED+VERIFIED 2026-07-31 — CAT program complete (1a✅, 5✅, 6✅, 1c✅, 4✅, 3b✅, 3a✅, 2✅, 1b✅, 7✅, 8✅): api tsc 149 → 0 (§19 rows 12–22); Sprint-1 Step-5 re-run eligible, NOT started per CTO** | api-wide; per-module counts (fresh `tsc --noEmit`, 2026-07-30, post-R1): media 24, billing 23, order 15, audit 15, common 15, auth 10, subscription 8, tenant 7, notification 7, **kds 1** (`kds.controller.ts:46` TS2322 — pre-existing on an untouched line), device-token 6, worker 4 (`src/worker.ts`), payment 2, webhook/asset/admin 1 each — sums to exactly 140. **CAT-5 recounted state (2026-07-30, §19 row 13 — 127 total):** media 24, billing 23, order 15, audit 15, **common 11**, subscription 8, tenant 7, notification 7, device-token 6, worker 4, payment 2, **auth 1** (`current-user.decorator.ts:8` — CAT-3a), kds 1, webhook/asset/admin 1 each. **CAT-6 recounted state (2026-07-30, §19 row 14 — 117 total):** media 24, **billing 20**, order 15, audit 15, common 11, **subscription 1** (`subscription.service.spec.ts:158` — CAT-1b), tenant 7, notification 7, device-token 6, worker 4, payment 2, auth 1, kds 1, webhook/asset/admin 1 each. **CAT-1c recounted state (2026-07-30, §19 row 15 — 110 total):** media 24, **billing 18, order 14, audit 13**, common 11, notification 7, **tenant 6**, device-token 6, worker 4, payment 2, subscription 1, auth 1, kds 1, webhook 1, asset 1 each — **admin 0 (retired)**. **CAT-4 recounted state (2026-07-30, §19 row 16 — 100 total):** media 24, **billing 16**, order 14, audit 13, **common 8**, device-token 6, **tenant 5, notification 5**, worker 4, subscription 1, auth 1, **payment 1**, webhook 1, asset 1 each — **kds 0 (retired)**, admin 0. **CAT-3b recounted state (2026-07-30, §19 row 17 — 89 total):** **media 20, billing 14**, order 14, audit 13, common 8, tenant 5, notification 5, worker 4, **device-token 3**, subscription 1, auth 1, webhook 1 — **payment 0, asset 0** — kds 0, admin 0. **CAT-3a recounted state (2026-07-31, §19 row 18 — 66 total):** **media 17**, billing 14, **order 13**, tenant 5, notification 5, worker 4, device-token 3, **audit 2, common 1**, webhook 1, subscription 1 — **auth 0** — payment/asset/kds/admin 0. **CAT-2 recounted state (2026-07-31, §19 row 19 — 37 total):** media 17, order 8, tenant 5, device-token 3, audit 2, subscription 1, common 1 — **billing 0, notification 0, webhook 0, worker 0** (remaining 37 partition exactly onto CAT-1b 13 / CAT-7 17 / CAT-8 7). **CAT-1b recounted state (2026-07-31, §19 row 20 — 24 total):** media 17, tenant 3, device-token 3, common 1 — **order 0, audit 0, subscription 0 (retired)** (remaining 24 partition exactly onto CAT-7 17 / CAT-8 7). **CAT-7 recounted state (2026-07-31, §19 row 21 — 7 total):** media 3, tenant 3, common 1 — **device-token 0 (retired)** (remaining 7 are exactly CAT-8). **CAT-8 recounted state (2026-07-31, §19 row 22 — 0 total):** every module 0 — **media 0, tenant 0, common 0 (retired); PREX-TSC-001 closed** |
| PREX-TSC-002 | `packages/db` tsc: **16 pre-existing TS2345 errors** — one per `TenantXRepository` constructor `super(prisma.<model>)` site; root = `BaseTenantRepository` hand-rolled `PrismaModelDelegate<T>` interface structurally incompatible with the $extended tenant-scoped client's `DynamicModelExtensionThis` delegates (arrow-property params checked contravariantly: `Record<string, unknown>` ≰ `Exact<A, …Args>` (`{ [x: string]: {} }` index); method-signature variant also rejected by the same param chain) | **FIXED 2026-07-30 (CAT-1a → 1 unique defect produced all 16 errors)** — constructor accepts `unknown` + single internal assertion `delegate as PrismaModelDelegate<TModel>` at the choke point (`BaseTenantRepository.ts`); subclass call sites and the repository contract unchanged; no `any` (eslint `no-explicit-any: error` honored), zero behavior change. Verified: db tsc **16 → 0** (empty output), db build leg GREEN (`tsc` + client copy), api tsc 140 unchanged (0 added), api suites 277/2/0 = baseline, db eslint 0 | db (repositories) |
| PREX-MIG-001 (Runtime Defect R4) | Migration `20260726100001_add_lifecycle_triggers` wrote triggers against snake_case columns that exist nowhere in the schema: `update_updated_at()` set `NEW."updated_at"` (real: `"updatedAt"` on all 8 models, no `@map`) → **every UPDATE failed on all 8 triggered tables** (tenants, users, branches, products, orders, customers, subscriptions, media): psql `record "new" has no field "updated_at"`; API `PUT /api/v1/orders/:id/status` → HTTP 500 (entire order state machine dead at runtime). Same disease in `log_order_status_change()`: insert into `audit_logs` used `tenant_id/user_id/entity_name/entity_id/old_values/new_values/ip_address/user_agent/created_at` (real: camelCase per `schema.prisma` AuditLog) + read `NEW."tenant_id"` | **FIXED 2026-07-30** — identifier-only rewrite (function/trigger names, BEFORE/AFTER timing, 8-table set, `IS DISTINCT FROM` guard, literal values `'STATUS_CHANGE'/'Order'/NULL/'system'/'database-trigger'`, webhooks exclusion all preserved). Live-verified on PostgreSQL 17.10 verification DB: all 8 tables accept UPDATEs with trigger overriding `updatedAt` (backdate probe → ~now()); non-status order UPDATE writes zero audit rows; status changes DRAFT→PENDING (psql) and PENDING→ACCEPTED (API `PUT` → HTTP 200) each wrote exactly one correct `STATUS_CHANGE` audit row; app-level audit row also logged; full api suite re-run = 275 pass / 2 skipped / 1 pre-existing fail (byte-identical baseline → no regression) | db (migrations), api (all UPDATE paths) |
| PREX-SEED-001 (Runtime Defect R5) | `prisma/seed.ts` used 93 hardcoded non-UUID slug IDs (`plan-starter-001`, `tenant-demo-001`, `role-owner-001`, `user-admin-001`, `rest-albaik-001`, `branch-albaik-riyadh-001`, `cat-appetizers-001`, …) for PKs that are all `String @db.Uuid` (schema-verified: every model `id` is `@id @default(uuid()) @db.Uuid`) → canonical `pnpm --filter @zayjar/db prisma:seed` failed at `seed.ts:45` (`subscriptionPlan.create`) with Prisma **P2023** `Error creating UUID, invalid character: expected an optional prefix of urn:uuid: followed by [0-9a-fA-F-], found 'p' at 1` (verbatim, reproduced 2026-07-30 on fresh DB). Secondary nuance: bare `prisma db seed` invoked without pnpm PATH additionally fails `spawn ts-node ENOENT` — the canonical pnpm script is the working invocation | **FIXED 2026-07-30** — data-only fix confined to `seed.ts` (105 insertions / 101 deletions): all 93 slug IDs (79 PK literals + 14 permission-data entries) replaced by deterministic, schema-valid UUIDs derived from the original slugs (sha256, v4-shaped → reproducible across clean databases); cross-references updated consistently (6 `sizeId` + 1 `addonItemId` literals; permission template → `p.id`); provenance comment added; **zero logic/enum/field changes** (all enums/fields schema-verified beforehand: `PaymentMethodType.CASH`, `CookingStatus`, `TenantStatus`/`SubscriptionStatus`, `TableStatus`, RolePermission/UserRole composite `@@id`, ProductVariant.tenantId required, Invoice.order `Restrict`, others `Cascade`). Live-verified PostgreSQL 17.10 fresh DB `zayjar_seed2` (M1/M2/M3 + corrected M6): full seed completes (`plans 3, tenants 2, subscriptions 2, users 5, roles 4, permissions 14, restaurants 2, branches 3, tables 14, categories 7, products 14, productSizes 12, productVariants 6, customers 3, orders 3, orderItems 6`); immediate re-run clean (FK-cascade cleanup); verified demo tenant `albaik` ACTIVE (`80a00898-…`), restaurant Al-Baik Chicken SAR 15.00%, 11 tenant-1 products, 5 tenant-1 categories, 14 QR tokens (`qr-albaik-r-*` ×8 / `qr-albaik-j-*` ×6, `deletedAt` NULL) | db (seed) |
| PREX-MIG-002 (Runtime Defect R2) | Migration `20260726000001_orders_partitioning_by_year` **fails on apply**: `column "created_at" named in partition key does not exist` — real column is `"createdAt"` (schema-verified). Same snake_case disease family as R4 | **CLOSED 2026-07-30 — investigation complete (CTO decision)**: approved identifier fix proved the layer-1 hypothesis (both partition statements then built — 14 yearly partitions, correct range routing + both window-end rejections, live-verified) and exposed layer-2 architectural defect: `LIKE … INCLUDING CONSTRAINTS` drops the PRIMARY KEY, and PostgreSQL requires unique constraints on partitioned tables to include all partition-key columns → `PRIMARY KEY (id)` + single-column inbound FKs cannot coexist with `RANGE ("createdAt")` partitioning (verbatim: `there is no unique constraint matching given keys` and `unique constraint on partitioned table must include all partitioning columns`). R2.1 option analysis A–F, all proven live: **ranked recommendation F — no partitioning (indexes only, zero behavior change)**; fallback D — `HASH ("id")` (only declarative layout proven legal with Prisma single-column `@id` + existing FKs); A/B/C/E disqualified (composite-PK FK breakage / self-contradictory / feature nonexistent in PG17 / silently-broken uniqueness+FK visibility). Approved identifier fix reverted per CTO closure; M4 parked non-shippable pending architecture RFC | db (migrations), orders partitioning |
| PREX-MIG-003 (Runtime Defect R3) | Migration `20260726100000_add_search_indexes` **failed at line 14**: `column "tenant_id" does not exist` — real column `"tenantId"`; mirror faults at line 17 (`"order_id"`, `"cooking_status"`). Partial apply previously left drift: `products.tsv_menu_search` present, indexes absent | **FIXED 2026-07-30** — identifiers-only (5 quoted snake_case → real camelCase columns) + provenance comment; statements, index names, index structures unchanged. Live-verified fresh DB `zayjar_r3f`: chain M1→M2→M3→M5→M6 clean (`ALTER TABLE` + 3× `CREATE INDEX`); objects: `tsv_menu_search` (generated ALWAYS) + `idx_products_tsv_menu_search` (GIN) + `idx_orders_kds_polling` btree (`"tenantId","branchId",status,"createdAt" DESC`) + `idx_order_items_kds` btree (`"orderId","cookingStatus"`) — verbatim match to `schema.prisma:481`/`:508` map names; FTS probe (broast 2A / chicken 5B weights) with @@ hits on both weights. Drift note: pre-fix partial-apply environments require manual reconcile (naive re-run fails at statement 1 `column already exists` — by design; no persistent drifted environment known). Regression: tsc api 140 / db 16, api suites 275 pass / 2 skip / 1 pre-existing fail, kds 41/41 = baselines | db (migrations), search indexes |
| RT-ONB-001 (Runtime Defect R7) | Public self-service onboarding was dead in production: `POST /api/v1/tenants` is `@Public()` (`tenant.controller.ts:34-43`) and the handler ran without any `dbTenantContext` — the only request-level context producer is `TenantContextMiddleware:83`, which either 403s context-less apex hosts or scopes the flow under an *existing* tenant (whose id then mismatches every tx create). Live-verified pre-fix 2026-07-30 (API `dist/main.js` pre-fix emit, DB `zayjar_step5`, anonymous `Host: albaik.localhost:8000`): **HTTP 500**, API log verbatim `Fail-Safe Block: Cross-tenant data insertion attempt detected and blocked.` (`ExceptionsHandler`). Hermetic-suite canary: `src/common/e2e.spec.ts` Verification 1 (verbatim `Fail-Safe Block: Access to model 'User' was blocked due to missing tenant context.`, `packages/db/src/index.ts:34`) = PREX-E2E-001 | **FIXED 2026-07-30** — `onboard()` now declares the explicit platform-level scope it semantically is (DOC-005 §4.1): `dbTenantContext.run({ isPlatformOwner: true }, () => this.executeOnboarding(dto))` (`tenant.service.ts:45-59`, body unchanged; fail-safe extension and middleware host gating deliberately untouched; + spec hermeticity repair: the previously unmocked cross-tenant email check `prisma.user.findFirst` → null, mirroring the existing pattern). Live post-fix: anonymous onboard → **201** (tenant TRIALING + owner + RESTAURANT_OWNER role + restaurant + Main Branch, each SQL-counted =1); duplicate subdomain → 409 `The requested subdomain is already registered.`; duplicate email → 409 `An account with this email address already exists.` (proves the email check is genuinely global again); apex localhost anonymous → 403 (invariant unchanged). Suites: full api **277 pass / 2 skip / 0 fail / 279** (new baseline; PREX-E2E-001 green), tenant suites 16/16, e2e suite 5/5, tsc api 140 / db 16 unchanged (0 added), eslint on modified files 0 | api (tenant onboarding), db extension |
| RT-GATE-001 (Runtime Defect R6) | Guest checkout bypassed the tenant-status gate: public menu/table resolve rejected UNPAID/CANCELED tenants (`public-menu.service.ts` `assertGuestOrderingAllowed(tenant.status)`), but `createGuestOrder` performed no status check — runtime-verified 2026-07-30 with parity matrix (3 surfaces × 5 states): checkout returned **201** under tenant UNPAID and CANCELED (orders accepted on paused accounts) | **FIXED 2026-07-30** — tenant-status gate in `createGuestOrder` after scan resolution (DOC-001 1.10; UNPAID/CANCELED → ForbiddenException with the same public message as the menu surface). Live matrix re-verified on `zayjar_rtv`: UNPAID/CANCELED → menu 403 / checkout 403; ACTIVE → 200/201; PAST_DUE grace → 200/201; zero orders leaked during gated states. Suites: order 51/51 (new G6 gate test), full api 276 pass / 2 skip / 1 pre-existing fail (new baseline, +1), tsc api 140 / db 16, kds 41/41 | api (order), guest channel |
| LATENT-KDS-001 (R1) | `kds.service.ts:76` `orderBy: { createdAt: 'asc' }` on `KitchenQueue`, a model with **no `createdAt` column** (schema verified) → Prisma `ArgumentValidationError` on KDS REST ticket list. Static corroboration: same line was pre-existing tsc error **TS2353** (`kds.service.ts(76,20)`); 4 further kds-module tsc errors (:100 TS2551, :107/:141/:143 TS7006) were knock-on type collapses of the same invalid query | **FIXED 2026-07-30** — one-line relation ordering `orderBy: { order: { createdAt: 'asc' } }` (semantics match component design: ticket `createdAt`/elapsed already derive from `order.createdAt` at :101/:139). tsc: kds-module 6→1 (remaining `kds.controller.ts:46` TS2322 is pre-existing on an untouched line — NOT fixed per Option-B), api total **145→140**, 0 added. Suites: kds 41/41 (4 suites), full api 275/2/1 = baseline. **Live-verified** (PostgreSQL 17.10, DB `zayjar_r1`, newest-first inserted rows): `GET /api/v1/kds/tickets?branchId=…` → **HTTP 200** (pre-fix: 500), tickets returned oldest-order-first, COMPLETED-order ticket excluded (status filter intact), full hydration (`E2E Classic Burger` qty 2 / size `Large` / addons `[Cheese]`; `E2E Fries`; elapsedMinutes 177/117; RUSH escalations broadcast), unknown branch → 404, zero `Unknown argument` in API log | kds |
| LATENT-MEDIA-001 | `media.service.ts:112` TS2552 `Cannot find name 'urls'` → previously feared ReferenceError if that branch executes. **Correction 2026-07-30 (Step-4 evidence): runtime hazard disproven** — `let attemptUrls: typeof urls;` is a type annotation; emitted `dist/media/media.service.js:105` is a bare declaration `let attemptUrls;` → compile-time TS2552 only | **FIXED 2026-07-31 (CAT-8, §19 row 22)** — the stray `typeof urls` re-declared to the truthful URL-bundle type (`{ originalUrl: string; thumbnailUrl: string \| null; mediumUrl: string \| null; largeUrl: string \| null }` per the Media columns schema:695–698); emitted declaration unchanged; runtime disproval reconfirmed | media |
| LATENT-TEN-001 | ~~`tenant.service.ts` reads `.branding` from a `SubscriptionPlan`-shaped object~~ — **AUDIT-VERIFIED NON-ISSUE (2026-07-30):** every `.branding` access in `tenant.service.ts` targets the `Tenant` model, which owns the column (`schema.prisma:99`): `:236` (`prismaRead.tenant.findUnique` result), `:304` / `:313` (`prisma.tenant.*` results, lines 295–305 are DTO-side by design). `SubscriptionPlan` indeed has no `branding` (verified, 0 occurrences), and no code ever reads it | **CLOSED — not a defect** (former suspicion disproven by audit). **Refinement 2026-07-31 (CAT-8 evidence):** the closure stands for the audited claim (code targets the correct model) — but a distinct **runtime** delivery gap was verified separately: the distributed generated client predates the column, so `tenant.branding` is `undefined` at runtime → see LATENT-BRAND-001 | tenant |
| LATENT-SUB-001 | `subscription.service.ts:114–116` dereferences `subscription.plan` after `as … \| undefined` cast without guard (`!plan.allowCustomDomains` at :116) → TypeError if plan missing at runtime; second unguarded site `:102` (`plan.name` after cast at :86); correctly guarded counterpart exists at :61. **Runtime hazard DISPROVEN 2026-07-30 (Step-4 live):** `getActiveSubscription` throws a graceful 404 `No subscription found for tenant` (`subscription.service.ts:22`) — verified live via `POST /api/v1/menu/products` as a no-subscription tenant (404, not 500); a null `plan` with a present subscription row is DB-impossible (`Subscription.planId String @db.Uuid` NOT NULL + `onDelete: Restrict`) | OPEN — compile-time caution only (counted inside PREX-TSC-001; runtime claim corrected 2026-07-30) | subscription |
| LATENT-BRAND-001 | Dynamic-branding JSONB was **runtime-degraded in the distributed client** (verified 2026-07-31 during CAT-8): the checked-in generated Prisma client predated the `branding` column — authoritative `packages/db/prisma/schema.prisma:99` has `branding Json?` and migration M2 (`add_tenant_branding_jsonb`) is applied, but the client's own snapshot `packages/db/src/generated-client/schema.prisma` contained **zero** `branding` refs (and neither did the client DMMF), so Prisma never SELECTed the column → `tenant.branding` reads evaluated `undefined` at runtime with `\|\| {}` fallbacks silently yielding `{}` | **RESOLVED+VERIFIED 2026-07-31** — client regenerated (Task 3) AND **runtime re-verified live** (post-Task-7, on fresh `zayjar_rt8`): after `UPDATE tenants SET branding='{"theme":"dark","heroBanner":"welcome.png"}'`, `GET /api/v1/tenants/:id` returns `branding: { logoUrl, bannerUrl, primaryColor, secondaryColor, theme:'dark', heroBanner:'welcome.png' }` — the JSONB dynamic fields are now SELECTed and merged (pre-fix they were absent/`{}`). Dynamic-branding delivery is functional end-to-end | tenant |
| LATENT-ENV-001 | Dev-port mismatch: qr-menu dev defaults assume the API on **port 3001** (`apps/qr-menu/src/app/lib/guest-api.ts:111–123`; `next.config.mjs` rewrite default `http://localhost:3001`), while the API listens on `PORT \|\| 8000` (`apps/api/src/main.ts:71`) and docker-compose maps `${PORT:-8000}:8000` → local dev outside compose without `PORT`/`API_INTERNAL_URL` set misroutes SSR fetches and the browser `/api` rewrite proxy | OPEN — **CONFIRMED 2026-07-30 (Step-4 live run):** with default env the SSR page renders the error view (fetch to `:3001` refused while API on `:8000`); `API_INTERNAL_URL=http://albaik.localhost:8000` makes the same page green. Canonical port decision pending | qr-menu ↔ api |
| ENV-TSK-0006 | Native/install build scripts blocked by pnpm script approval (`@prisma/client`, `@prisma/engines`, `prisma`, `argon2`, `sharp`) — class described in `TSK-0006_HEALTH_CHECK.md`; sandbox workaround formerly recorded in §18 (argon2 node-gyp rebuild; shipped `argon2.glibc.node` prebuild segfaults under modern glibc — segfault reproduced live 2026-07-30 and again 2026-08-03, rebuild fixed it). **Full root-cause set established 2026-08-03 (three independent causes, all reproduced from a clean slate):** (a) pnpm 10+ blocks dependency lifecycle scripts by default and `pnpm-workspace.yaml` `allowBuilds` listed only the 4 Datadog/msgpackr entries → `@prisma/engines`' postinstall never ran (engine binaries absent → CLI `generate`/`migrate`/`validate` unusable) and `argon2`'s install script never ran (leaving only the SIGSEGV-ing prebuild); (b) the init migration carried an upstream UTF-8 BOM that made `prisma migrate deploy` hard-fail at migration 1 (`P3018` / `42601 syntax error at or near "\ufeff"`) — the CLI path had never been exercised, so this was masked by the psql-with-strip-filter workaround; (c) CI Job 3's `prisma validate` step had no `DATABASE_URL` → unconditional `P1012`. **`sharp` was NOT a cause** (loads via the prebuilt `@img/sharp-linux-x64` package — audit-corrected: the original TSK-0006 enumeration over-attributed it) | **FIXED+VERIFIED 2026-08-03 (Sprint 2 Task 8, §19 row 36)** — least-privilege `allowBuilds` (4 justified additions), BOM stripped (3 bytes, remainder byte-identical), CI validate step given a connection-free placeholder URL; clean-slate install now needs **zero manual workarounds** and `prisma migrate deploy` applies the documented chain 8/8 with live end-to-end proof (argon2 login 200 / 401, guest checkout 201) | db, api (auth) |
| AUTHZ-001 (Audit v1 Critical C-1) | Caller-supplied tenant context could override the verified JWT identity end-to-end: `TenantContextMiddleware:38–41` accepted an unvalidated `X-Tenant-ID` from any caller and made it the sole DB-scope source (ALS), while `JwtStrategy.validate` never reconciled the verified `payload.tenantId` claim nor rejected header/context mismatches → any staff member of tenant A holding a valid JWT could run repository reads/writes scoped to tenant B (victim UUID known). Verified pre-fix live 2026-07-31 (audit + A/B reproduction): staff token (tenant A) + `X-Tenant-ID: <tenant B>` on `GET /api/v1/auth/me` → **HTTP 200** | **FIXED+VERIFIED 2026-07-31 (CTO execution command)** — reconciliation at the identity layer in `JwtStrategy.validate` post-signature-verification (post-blacklist): explicit `X-Tenant-ID` ≠ claim → **403** `Tenant context mismatch: X-Tenant-ID does not match the authenticated tenant.`; middleware-resolved context ≠ claim → **403** `Tenant context mismatch: resolved tenant does not match the authenticated tenant.`; otherwise the request-scoped ALS store is reconciled to the verified claim so every downstream repository is scoped by the JWT identity; PLATFORM_OWNER payloads exempt (cross-tenant administration preserved per design); array-valued headers normalized. Runtime A/B (PG 17.10 + Redis 8 + seeded `zayjar_c1`, `dist/main.js` post-fix emit): pre-fix P2-style attack = HTTP 200 (leak live — stale process observation recorded verbatim in §19 row 24); post-fix: P1 control 200, P2 header attack **403** (verbatim new message), P3 foreign-host attack **403**, P4 matching header 200, P5 anonymous public table resolve 200 (public path regression-clean), P6 repo-surface attack on `GET /api/v1/orders` **403 with the C-1 message thrown pre-RBAC** (control: clean token 403s with the distinct RBAC message — channels distinguishable). Suites: filter script **277 pass / 2 skip / 0 fail** (glob-depth semantics unchanged — new spec lives at depth 3), root `pnpm test` **504 pass / 2 skip / 0 fail** (59/60 suites; +8 reconciliation tests in `auth/strategies/jwt.strategy.spec.ts`), tsc api **0** / db **0** maintained, eslint 0 on changed files | auth (jwt strategy), api tenant context |
| AUTHZ-002 (Audit v1 Critical C-2) | Platform-owner context was derived from UNVERIFIED claims: `tenant-context.middleware.ts:27` used `jwt.decode(token)` (no signature check) and trusted `roles: [PLATFORM_OWNER]` from any well-formed token to bypass the tenant fail-safe (`isPlatformOwner = true`) → forged garbage-JWTs downgraded the fail-safe to a no-op on every `@Public()` surface (e.g., cross-tenant unscoped login lookup `auth.service.ts:190`, platform-wide reads on any non-token-scoped public handler). Secondary site: `auth.service.decodeToken` (`jwtService.decode`, "without verification") fed the refresh handler's CSRF-subject claim — reachable only after `rotateRefreshToken`'s `verifyAsync`, but an unverified decode is an authorization smell by construction | **FIXED+VERIFIED 2026-07-31 (CTO execution command)** — (a) middleware now runs `jwt.verify(token, JWT_CONFIG.accessTokenSecret)`: ONLY signature-verified claims may set `isPlatformOwner`; forged/malformed/expired tokens confer nothing (fail-safe then applies normally); behavior for valid authenticated users byte-identical (verified owner apex still bypasses, staff apex still 403s, tenant-host resolution unchanged, store shape `{tenantId, isPlatformOwner}` preserved); (b) `rotateRefreshToken` now returns `sub` taken from the ALREADY VERIFIED `verifyAsync` payload, the controller consumes it, and the unverified `decodeToken` helper is DELETED → **zero `jwt.decode`-based decisions remain in production code** (grep-verified; only a code comment mentions the old call). Runtime A/B (PG 17.10 + Redis 8, seeded `zayjar_c1`, post-fix dist): forged PLATFORM_OWNER at apex → **403** (pre-fix path = 200 via decode bypass — established in the audit), valid owner → **200**, expired owner → **403**, staff apex → **403** unchanged, C-1 matrix re-run (X-Tenant-ID attack **403**, clean staff 200), forged bearer on protected `/auth/me` → **401** via strategy (layered defense intact), anonymous public table resolve 200. Suites: middleware spec +6 (verified-owner, forged, malformed, expired, staff-apex, owner-on-tenant-host store shape), root **510 pass / 2 skip / 0 fail** (= 504 + 6), filter script 277/2/0 unchanged (glob-depth), tsc api **0** / db **0** maintained, eslint 6/6 | auth (middleware platform flag), auth (refresh flow) |
| DEPLOY-001 (Audit v1 High H-1) | Kubernetes + compose probe paths pointed at a non-existent route: liveness/readiness `path: /api/v1/health` (`k8s/api/deployment.yml:143,151`) and compose healthchecks curling `http://localhost:8000/api/v1/health` (`docker-compose.production.yml:29`, `docker-compose.staging.yml:26`), while the actual health endpoint is `GET /health` on the root-mounted `HealthController` (`health.controller.ts:5`; no global prefix) → probes could never succeed (runtime-verified: 404 on resolvable tenant hosts, 403 apex) | **FIXED+VERIFIED 2026-07-31 (§19 row 26)** — all four probe/healthcheck paths now `/health` (staging site found same-turn beyond the audit's enumeration — same defect class, reported openly); all other probe/healthcheck params YAML-asserted unchanged; `k8s/README.md` probe row synced in-commit; runtime A/B: new path **200** on both seeded tenants + via `X-Tenant-ID`, apex 403 = H-2 residual (untouched per CTO sequencing); zero application-code change; suites 277/2/0 + root 510/2/0; tsc 0/0; eslint 6/6 | deploy manifests (k8s + compose) |
| DEPLOY-002 (Audit v1 High H-2) | The health endpoint was tenant-gated: the `TenantContextMiddleware` fail-safe rejected any request without a resolved tenant, so infrastructure probes (kubelet liveness/readiness, container healthchecks — pod-IP/apex Hosts) could never receive HTTP 200 even after the H-1 path fix | **FIXED+VERIFIED 2026-07-31 (§19 row 27)** — consumer-level, path-exact exemption `.exclude('health')` on the tenant-middleware segment only (`app.module.ts`); the C-2-hardened middleware file is byte-untouched (protection preserved by construction); live matrix: `/health` 200 apex / kubelet-Host / HEAD / tenant-host / `X-Tenant-ID` (body `status: ok`); preservation byte-exact: `/` 403, `/healthx` 403 (no widening), `/api/v1/health` 403-apex/404-tenant, public surface 403-apex/200-tenant, C-1 attack 403 verbatim, forged PLATFORM_OWNER apex 403 on non-health surfaces; +4 wiring specs (root 514/2/0); tsc 0/0; eslint 6/6 | api middleware wiring |
| DEPLOY-003 (Audit v1 High H-3) | The CD pipeline could never deploy: both deploy jobs ended at a printed placeholder (`Deployment would proceed here.`), the post-deploy health gate curled a non-existent route (`/api/v1/health`), the referenced `rollback.sh` had no Kubernetes strategy (the repo's actual topology), and the health-check script's `${1:?}` guard hard-failed on an unconfigured URL making its own skip branch dead code | **FIXED+VERIFIED 2026-07-31 (§19 row 28)** — real k8s deploy flow in both jobs (kubectl setup, kubeconfig secrets raw-or-base64 with fail-fast, selective manifest apply that skips placeholder secrets + the stateful data tier, `set image` pin ×5, rollout status ×5), rollback on failure for staging AND production with a new primary k8s strategy in `rollback.sh` (rollout undo ×5 — stub-kubectl argv-exact live-verified), health gates on the real `/health` (PASS/SKIP/FAIL paths executed live); statically verified (YAML parse, 15 run blocks bash-n, zero placeholders, name-mapping matrix, kustomize 22 manifests, offline set-image ×5); ci.yml byte-untouched; zero application-code change | deployment automation (cd.yml + .github/scripts) |
| SPEC-DRIFT inventory (documented in session audit) | RS256 (spec) vs HS256 (code); QR page spec says SSR vs previous mock CSR (RESOLVED Step 3); Socket.io Redis adapter claimed in `SPEC_INDEX §7.6` but absent in code (`createAdapter` grep = 0) while `k8s/api/hpa.yml` scales API 2–10 pods; FCM log-only stub (`device-token.service.ts`, no `firebase-admin` dep); DOC-006 §5.6 "Token Bucket" title vs fixed-window code; DOC-003 nested menu paths vs flat code routes; ~16 shipped routes undocumented in DOC-003; `notifications` table lacks FK/relation/repository; Tailwind/Radix mandated by docs but absent; README clone URL wrong (`zayjar/platform-core`); README says "29 tables" vs 30 models verified; KDS room-naming drift: SPEC_INDEX `tenant_id:branch_id:kds` (:276) / `tenant_id:branch_id` (:445) vs code `tenant:{tenantId}:branch:{branchId}` (`kds.gateway.ts:82`) | OPEN (documentation/decision backlog for Sprint-3 doc baseline) | cross-cutting |
| RT-ONB-002 (discovered 2026-07-31, Sprint 2 Task 1 verification) | Public onboarding (`POST /api/v1/tenants`, R7-fixed) provisioned a **per-tenant `RESTAURANT_OWNER` role with ZERO `role_permissions`** (`tenant.service.ts` step D: `tx.role.create` with no permission links; live-verified: onboarded owner's role had 0 `role_permissions` rows vs the seed's owner role with 14). Consequence (live-verified): an onboarded tenant's owner CAN log in (200) but every RBAC-gated staff endpoint 403s — `GET /api/v1/menu/products` → 403 `[read on Product]`; `GET /api/v1/branches` → 403 `[read on Branch]`. Seeded demo users have full permissions but cannot log in (placeholder argon2 hash, `seed.ts:298` — verify returns false). Net: no user could use the staff apps end-to-end | **FIXED+VERIFIED 2026-07-31 (Sprint 2 Task 2, §19 row 30)** — onboarding now clones the canonical seeded RESTAURANT_OWNER permission set onto the new role (`tx.role.findFirst` platform-scope with `NOT: { id: ownerRole.id }` + `rolePermission.createMany`; fail-fast if the baseline role/links are missing); the seed gained the 8 guard-vocabulary permission rows the RBAC guard actually checks (product/branch/table/order/tenant × create/read/update), so the canonical owner set is functional. **Vocabulary finding (verified):** the seed's legacy rows (`menu`, `kds`, `customer`, `billing`, `analytics` resources; `write` action) never match `CaslAbilityFactory`'s Subjects/Actions (`Product/Order/Branch/Table/Tenant` × create/read/update/delete) — retained for backward compatibility (manager/cashier/kitchen filter them) and harmless for the owner; parity (seeded ≡ onboarded, 22=22) proven by SQL set-equality at runtime | api (tenant onboarding), db (seed) |

---

# 8. Existing API Contracts

**Public (unauthenticated) contracts currently implemented** (`@Public()` verified in each controller). All tenant-scoped via `TenantContextMiddleware` (Host subdomain / custom domain / `X-Tenant-ID`).

## 8.1 `GET /api/v1/public/table/:token` (Sprint 1 Step 1)
- Guard-free (`@Public()`), `@RateLimit('public')` (120/min/IP).
- Response: `{ table: { number }, branch: { id, name }, restaurant: { name, currency }, tenant: { name, logoUrl, bannerUrl, primaryColor, secondaryColor } }`.
- 403 when tenant subscription `UNPAID`/`CANCELED` (DOC-001 §1.10); uniform 404 for unknown/expired token (no existence oracle).

## 8.2 `GET /api/v1/public/menu?token=…` (Sprint 1 Step 1)
- `@Public()`, `@RateLimit('public')`.
- Response: table/branch/restaurant/tenant projection (as 8.1) + `categories[].products[]` with `sizes[]`, `variants[]` (`price`, `stockQuantity`), `addons[]` groups with `options[]`; filters `isActive/deletedAt/isAvailable` server-side; single nested relational query; Decimal→Number mapping.

## 8.3 `POST /api/v1/public/orders/checkout` (Sprint 1 Step 2)
- `@Public()`, `@RateLimit('checkout')` (30/min/IP). `PublicOrderController` in OrderModule.
- Body (`CreateOrderRequestDto`, class-validator whitelist + forbidNonWhitelisted): `{ branchId, qrCodeToken (required here), type: 'DINE_IN'|'TAKE_AWAY'|'DELIVERY', items: [{ productId, quantity≥1, sizeId?, variantId?, addons?: [{ addonItemId }] }], paymentMethod: 'CASH'|'CREDIT_CARD'|'APPLE_PAY'|'LOCAL_WALLET', tableId?, specialNotes? }`.
- Behavior: verifies `qrCodeToken` via `TenantTableRepository.findByQrCodeToken` (tenant-scoped, `deletedAt:null`); explicit branch/table mismatch → 400; unknown token → uniform 404; branch/table forced server-side; then identical staff pipeline (server-computed pricing incl. variant absolute override + stock validation; atomic tx order + kitchen queue entry; WS `ticket.created` with resolved names; legacy `order.created` broadcast).
- Response: HTTP 201 (explicit `@HttpCode(HttpStatus.CREATED)`), persisted order record (`id`, `orderNumber` `ORD-YYYY-NNNNN`, `status: 'PENDING'`, totals incl. tax from restaurant `taxPercentage`, `orderItems`). CSRF: global guard bypasses `@Public()` routes — cookie-less guest POST passes (verified `csrf.guard.ts`).

## 8.4 Pre-existing public endpoints (unchanged by Sprint 1)
- `POST /api/v1/auth/login`, `POST /api/v1/auth/refresh` (`@Public()`, auth tier 10/min).
- `POST /api/v1/billing/webhooks` (`@Public()`, Stripe signature via `STRIPE_WEBHOOK_SECRET`).
- `POST /api/v1/customers` (`@Public()`, registration; tenant from middleware, never client payload).
- `POST /api/v1/tenants`, `GET /api/v1/tenants/plans` (`@Public()`).

## 8.5 Authenticated staff surface (context only, unchanged)
- `POST /api/v1/orders/checkout` (JwtAuthGuard + RBAC `create:Order`, checkout tier) — signature and behavior byte-identical after Step 2 (verified by unmodified staff test #18 `tableId` passthrough among 23 pre-existing integration tests).

---

# 9. Database Status

- **Tables: 30 models** in `packages/db/prisma/schema.prisma` (README's "29 tables" is stale — see SPEC-DRIFT).
- **Enums: 9** (names in §4.8).
- **Migrations:** baseline committed at `packages/db/prisma/migrations/` (6 migrations + `migration_lock.toml`: `20250101000000_init`, `20260725000000_add_media_model`, `20260726000000_add_tenant_branding_jsonb`, `20260726000001_orders_partitioning_by_year`, `20260726100000_add_search_indexes`, `20260726100001_add_lifecycle_triggers`). **Applied state (corrected 2026-07-30 — previously "UNKNOWN, no runtime in sandbox"):** M1 (`init`), M2 (`add_media_model`), M3 (`add_tenant_branding_jsonb`) and M6 (`add_lifecycle_triggers`, post-R4-fix) **verified clean-applying** on two independent live PostgreSQL 17.10 verification DBs; **M4 (`orders_partitioning_by_year`) parked non-shippable** (PREX-MIG-002 / R2 CLOSED as investigation-complete 2026-07-30 — §7/§20/§26-ranked R2.1 recommendation); **M5 (`add_search_indexes`) corrected 2026-07-30 (PREX-MIG-003 / R3) and verified clean-applying** in the standard chain M1→M2→M3→M5→M6 on live PostgreSQL 17.10 (§19 row 8). Production/provisioned-DB applied state remains UNKNOWN (no access). Seed script exists (`prisma/seed.ts`) with demo tenant `albaik` — **executable since 2026-07-30** (PREX-SEED-001 / R5 below): full fresh-DB seed + re-run verified. **Update 2026-08-03 (AUDIT-004 / §19 row 37):** the migration set is now **10 migrations** — M10 `20260803000000_add_user_branches` adds the `user_branches` join table (composite PK, tenant-owned rows, cascade FKs to users/branches/tenants) giving DOC-005 §4.2 branch scoping its first persistent source; applied clean in the 9/9 canonical `prisma migrate deploy` chain (M4 still parked). **Update 2026-08-03 (ENV-TSK-0006 / §19 row 36):** the migration set was **9 migrations** (M7 `add_order_payment_method`, M8 `add_discount_engine`, M9 `add_notifications_relations` added during Sprint 2), and the canonical Prisma CLI path works natively — `prisma migrate deploy` applied **8/8** shippable migrations on a fresh PostgreSQL 17.10 (`_prisma_migrations`: 8 applied, 0 rolled back; 32 tables), followed by the canonical `prisma:seed` (17/17 entity groups). The init migration's upstream UTF-8 BOM — which made the CLI hard-fail at migration 1 (`P3018`/`42601`) — was stripped. **M4 remains excluded/parked** per the R2/PREX-MIG-002 closure and, when included, now fails on its own documented layer-2 architecture defect (`42703 column "created_at" named in partition key does not exist`), not on the BOM. The psql-apply workaround is no longer required. **Update 2026-07-30:** `20260726100001_add_lifecycle_triggers` corrected and live-verified (PREX-MIG-001 / R4 above): applies clean and executes correctly on PostgreSQL 17.10 (all 9 triggers live: 8× `set_updated_at` BEFORE UPDATE + `log_status_change` AFTER UPDATE on `orders`). Verified via session verification DB `zayjar_r4` (M1/M2/M3 + corrected M6 applied by psql; M4/M5 excluded from that session's scope); production/provisioned-DB applied state otherwise remains as stated.
- **Relations/constraints (verified highlights):**
  - `Tenant` is tenancy root; most models carry `tenantId String @db.Uuid`; relation cascades tenant-side (`onDelete: Cascade` on `Table.tenant`, `KitchenQueue.tenant`, etc.).
  - `Table.qrCodeToken` — `@@unique` (`idx_tables_qr_token`); `Table` has `deletedAt` (soft delete; public lookups filter `deletedAt: null`).
  - `Tenant.subdomain`, `Tenant.customDomain` unique (drives middleware resolution); `Tenant.branding Json? @default("{}")` exists (schema:99); `SubscriptionPlan` has NO `branding` (verified 0 occurrences — see LATENT-TEN-001 closure).
  - `ProductVariant` (`price Decimal(10,2)`, `stockQuantity Int @default(0)`): `@@unique([sku])`, `@@index([productId])`; relation to `OrderItem[]`.
  - `KitchenQueue`: fields `id, tenantId, branchId, orderId, ticketNumber, priority, startedCookingAt, completedCookingAt` — **no `createdAt`** (see LATENT-KDS-001).
  - `SessionLog`: refresh-token hash store (`isRevoked`).
  - Unscoped (globally readable) models via tenant extension: `Tenant`, `SubscriptionPlan`, `AuditLog`, `Notification`.
- Generated client: committed at `packages/db/src/generated-client` (binaryTargets include `debian-openssl-3.0.x`, also `native` and `windows`; 21 tracked files) and copied into `dist/generated-client` by `scripts/copy-generated-client.js`.

---

# 10. Frontend Status

## 10.1 QR Menu (`apps/qr-menu`)
- **Step 3 complete.** SSR server component page (ƒ Dynamic per build output), zero mocks, real branding/table/menu from Step-1 API, real cart, checkout wired to Step-2 endpoint, confirmation view, error views (missing token / 403 subscription pause / unresolvable token).
- Build: PASS (type-checked, `next build` exit 0 — re-verified 2026-07-30). Tests: 28/28 (3 suites — re-verified 2026-07-30). Lint: clean via build's lint phase.
- Legacy `?table=` display param removed; token param is `?t=`.

## 10.2 Cashier (`apps/cashier`)
- Unchanged by Sprint 1. Terminal sends `X-Tenant-ID` (verified lines 104, 258); checkout call to staff endpoint at `CashierTerminal.tsx:253–258`; reads `localStorage.accessToken`. The only in-repo writer of that key is the backoffice onboarding wizard (`RestaurantCreationWizard.tsx:253`) — no standalone login screen exists in any frontend (auth-UI gap, see §15 item 5).

## 10.3 Backoffice (`apps/backoffice`)
- **AUDIT-014 in progress (2026-08-04).** Products and Categories are production-ready: list, search, filter, create, edit, availability/visibility toggle, archive (soft delete), archive view and restore — all against real endpoints, verified in a real browser (19/19 and 21/21 checks).
- Categories surfaces a **truthful cascade warning**: archiving a category archives its products in one transaction and the dialog names the exact count, stating that restoring the category does **not** bring them back. Confirmed against the database, not assumed.
- Branches, Tables, Customers and Staff tabs are placeholders. Customer CRUD endpoints exist (built 2026-08-04) but have no UI yet.
- CSS is real as of AUDIT-001 (Tailwind v3 + PostCSS; `globals.css` imported by `layout.tsx`).

## 10.4 KDS
- Frontend component in backoffice (above); receives WS events in rooms `tenant:{tenantId}:branch:{branchId}` (kds.gateway.ts:82); `ticket.created` payload post-Step-2 contains resolved names (orderItemId, name, quantity, size, addons, cookingStatus).

---

# 11. Backend Status

- Order pipeline (post Step 2): server-side pricing (product availability, variant absolute override incl. stock validation, size adjustment, addons, tax from restaurant `taxPercentage` at `order.service.ts:230`, `discountAmount = 0.00` placeholder comment at line 232 — intentional, documented in code), atomic transaction (`order` + `kitchenQueue`), KDS broadcast (`ticket.created` canonical + `order.created` legacy alias), state machine guarded transitions + invoice on COMPLETED (invoice `pdfUrl` currently a fabricated CDN string per `order.service.ts:451` — known placeholder noted in audit inventory; out of Sprint-1 fix scope).
- Menu: staff CRUD + (new) public guest read.
- KDS: gateway (join/leave branch rooms, JWT-verified sockets), REST ticket list (see LATENT-KDS-001), priority escalation logic.
- Billing: webhook-driven subscription status updates.
- Media/auth/notification/worker/webhook modules: unchanged; notification email/SMS/push dispatch with queue worker.

---

# 12. Security Status

Implemented (verified): tenant isolation via middleware + DB extension + ALS context; `Fail-Safe` block without context; uniform 404 anti-oracle on guest token surface; rate limiting tiers; CSRF guard with public bypass; global input sanitization middleware (DOC-006 §5.4, `SanitizationMiddleware`); argon2id hashing; refresh-token rotation + blacklist; CASL RBAC; subscription lifecycle gating on guest channel (UNPAID/CANCELED → 403); server-authoritative order binding (DOC-005 §4.6); HMAC-SHA256 QR token generation (`branch.service.ts:50–60`, `SYSTEM_PEPPER` env with fallback default string).

Known gaps (documented, not addressed in Sprint 1): HS256 vs spec RS256 (deferred by decision); QR token pepper fallback default `'zayjar-default-pepper-999!'` hardcoded fallback in code (branch.service.ts:51); invoice `pdfUrl` fabricated (order.service.ts:451); spec-drift items in §7; `X-Tenant-ID` header accepted as manual tenant override by middleware (dev header honored in any environment where reachable); dev-port mismatch (LATENT-ENV-001).

Secrets state in-repo: `main.ts:8` `REQUIRED_ENV_VARS = ['DATABASE_URL', 'JWT_SECRET', 'JWT_REFRESH_SECRET']`; non-production defaults exist in code as development fallbacks; production startup refuses missing JWT secrets (jwt.config.ts) and missing required envs (main.ts).

---

# 13. Testing Status

| Suite group | Last verified result (re-verified live 2026-07-30) | Command |
|---|---|---|
| api `src/order` (unit + HTTP integration) | 50/50 PASS | `cd apps/api && npx jest src/order --config jest.config.js` |
| api `src/menu` | 23/23 PASS | same pattern, `src/menu` |
| api `src/kds` | 41/41 PASS | same pattern, `src/kds` |
| api full | **478 passed / 2 skipped / 1 failed, 481 total (57 suites)** (1 = PREX-E2E-001, pre-existing) | `cd apps/api && npx jest --config jest.config.js` |
| api via repo script | `pnpm --filter @zayjar/api test` = 275 passed / 2 skipped / 1 failed (278 total) (script glob `jest src/**/*.spec.ts` expands only two levels deep under `sh` — pre-existing script semantics; same single failure) | `pnpm --filter @zayjar/api test` |
| qr-menu | **28/28 PASS (3 suites)** | `pnpm --filter @zayjar/qr-menu test` |
| qr-menu build | PASS (ƒ Dynamic `/`, exit 0) | `pnpm --filter @zayjar/qr-menu build` |
| ESLint `@zayjar/api` package | 0 errors (exit 0) | `npx eslint "src/**/*.ts"` |
| tsc `apps/api --noEmit` | **140 errors**, exit 2 (all pre-existing legacy; 0 from Sprint 1 — re-verified 2026-07-30; −5 attributable to the R1 fix line and its knock-ons) | `cd apps/api && npx tsc --noEmit` |
| tsc `packages/db` | 16 errors (pre-existing; emits dist regardless) | `cd packages/db && npx tsc` |
| CI status on main (remote) | UNKNOWN (no access to Actions history); by construction Job 3 (`pnpm build`) fails on PREX-TSC-001/002 | — |
| ESLint `@zayjar/db` (incl. `prisma/seed.ts`) | 0 problems (exit 0) — captured pre-R5-edit and re-run post-edit identical, 2026-07-30 | `pnpm --filter @zayjar/db lint` |
| Seed execution (live) | ✅ completes on fresh PostgreSQL 17.10 DB, twice in a row (2026-07-30, fix `084dd37`); entity counts in §7 PREX-SEED-001 | `DATABASE_URL=… pnpm --filter @zayjar/db prisma:seed` |

**Notes:** (1) CI Job 2 executes **root** `pnpm test` → root `jest.config.js` (`testMatch: '**/*.spec.ts'`, node environment, `tests/e2e/` ignored), which collects a different set than the per-package commands above. (2) The CI row covers both `.github/workflows/ci.yml` and `cd.yml` — neither is verifiable by us (no GitHub access). (3) The 2 skipped api tests are DB-gated conditional describes (verified site: `media-concurrency.integration.spec.ts:33` `describeIfDb = DATABASE_URL ? describe : describe.skip`, skipped while `DATABASE_URL` is unset); per-test itemization of both skipped cells beyond this site: CANNOT CONFIRM from aggregate output alone.

---

# 14. Engineering Decisions

Only decisions verifiable from repository history (commits + code comments):

1. `d1c6035` — Public guest surface implemented as a separate class-guard-free `PublicMenuController` with explicit `@Public()` + per-handler rate limits + controller-level `requireTenantContext` fail-safe (tenantId injected into service calls); safe projections only (no internal fields exposed).
2. `0741d5b` — Guest checkout placed in `PublicOrderController` inside **OrderModule** (comment in file: co-located with transaction logic; OrderModule already wires KdsGateway/Webhook/Notification into OrderService) rather than modifying staff `OrderController`; staff controller left byte-identical.
3. `0741d5b` — DEFECT-B resolved via **dedicated post-transaction read** for KDS display names instead of expanding the transaction `include`, explicitly to keep the checkout HTTP response payload byte-identical (code comment in `order.service.ts`).
4. `0741d5b` — Guest branch/table binding is server-authoritative from the verified token (DOC-005 §4.6), with explicit client mismatches rejected (400).
5. `5967db9` — qr-menu converted from client-rendered mock page to **SSR** (`force-dynamic`), same-origin/tenant-Host API addressing, dev-only `/api` rewrites; pricing consolidated into a shared pure `lib/pricing.ts`.
6. `5967db9` — Guest payment method fixed to `CASH` (pay-at-counter) as the only universally valid enum value without a guest payment integration; order lands `PENDING` into the existing cashier state machine (MenuBrowser.tsx:161).
7. Pre-Sprint history (upstream): `993b801` "quality hardening — security fixes, missing tests, error handling"; `9fb9c97` ESLint zero-error CI enforcement (DOC-010 §10.2); `9ed1f2d` backup/DR; `5a5d0ac` zero-downtime migration strategy; `c75372f` k8s manifests.
8. Session-approved decision NOT yet in repository code: RS256 migration (S0-T01) deferred — recorded here for continuity (cannot be evidenced from repo history itself).
9. `c15843f` (R4, 2026-07-30) — Pre-existing broken migration repaired **in place at identifier level** (no corrective chain migration authored): approved scope was "fix the migration, preserve behaviour"; behaviour parity proven at runtime (conditional audit trigger and literal payload byte-equivalent).
10. `084dd37` (R5, 2026-07-30) — Seed IDs fixed as **deterministic UUIDs derived from the original slugs** (sha256 → v4-shape) instead of random UUIDs or dropping explicit IDs: preserves reproducible demo databases + a slug→UUID audit trail; baked literals chosen over a runtime helper because the workspace has no `@types/node` and the canonical seed runner type-checks.
11. 2026-07-30 — **Push-ownership policy:** pushes are performed personally by the user; the session never pushes and never requests credentials (§6/§16 updated to match).
12. 2026-07-30 — **Step-4 degraded-path decision:** with Docker unavailable, a native runtime (apt PostgreSQL/Redis + compiled `dist` + `next dev`) was accepted as the equivalent live-verification environment; one browser-pointer assertion was declared CANNOT VERIFY instead of being faked.
13. 2026-07-30 — **Scope discipline on defect fixes:** R4 and R5 each fixed in exactly one isolated commit with before/after verification; R1/R2/R3 deliberately left OPEN pending scoping (no drive-by fixes).
14. 2026-07-31 — **Sprint 2 Task 1 API-base resolution** (`resolveApiBase` in both staff apps' `lib/auth.ts`): `NEXT_PUBLIC_API_URL` env override wins; local dev maps `*.localhost` → same hostname on the API default port 8000 (preserves the tenant subdomain for `TenantContextMiddleware` — qr-menu `resolveServerApiBase` precedent, `5967db9`); production uses same-origin (`''`) so nginx routes `/api/*` with the tenant Host intact. The `locationLike` seam is the same pattern as `fetchImpl` params elsewhere (testability without DOM mocking).
15. 2026-07-31 — **Sprint 2 Task 1 MFA UX:** a single login form with two phases — credentials, then on the backend's exact 401 `"MFA token required"` challenge an authenticator-code step that resubmits with `mfaToken` (matches `validateLogin`'s contract; verified live 401→200 with a real TOTP).
16. 2026-07-31 — **CSRF double-submit on staff mutations:** after login the backend sets `__Host-CSRF-Token` (JS-readable) + `__Host-Refresh-Token` (HttpOnly); `CashierTerminal` checkout now echoes the cookie as `X-CSRF-Token` (DOC-006 §5.3, `CsrfGuard`). Live-verified: the echo header is accepted on authenticated `POST`s.
17. 2026-07-31 — **Branch selection deferred (not part of Task 1):** the seed grants the `CASHIER` role only `menu/order/customer` permissions (no `read Branch`), so a branch picker would 403 for cashiers; the terminal keeps the `?branchId=` URL-parameter contract. A picker (or RBAC/seed change) is a recorded follow-up.
18. 2026-07-31 — **RT-ONB-002 deliberately not fixed in Task 1's commit:** discovered during Task-1 runtime verification; it is a backend provisioning defect (zero backend files in this task). Scope discipline per decision 13 — queued as §23 item 11 (Sprint 2 Task 2).
19. 2026-07-31 — **RT-ONB-002 fix — canonical-set cloning:** onboarding provisions the new owner role by cloning the canonical seeded RESTAURANT_OWNER permission set at runtime (`tx.role.findFirst({ where: { name: 'RESTAURANT_OWNER', NOT: { id: ownerRole.id } }, orderBy: { createdAt: 'asc' } })` + `rolePermission.createMany`) rather than duplicating a permission list in code — single source of truth stays the seed data, and onboarded owners are byte-identical to seeded owners by construction (SQL set-parity proven at runtime). The `NOT: { id }` exclusion prevents the lookup from matching the just-created role.
20. 2026-07-31 — **RT-ONB-002 fix — guard-vocabulary seed rows:** the seed's legacy permission vocabulary (`menu`/`kds`/`customer`/`billing`/`analytics` resources, `write` action) is dead against `CaslAbilityFactory` (Subjects `Product/Order/Branch/Table/Tenant`, Actions create/read/update/delete) — runtime-proven: an owner holding the original 14 gets `branch:read`/`order:read` but 403s on `[read on Product]` / `[create on Order]`. The fix ADDED the 8 rows the guard actually checks (product read/create, branch create, table read/create, order create/update, tenant update) and left the legacy rows in place for backward compatibility with the manager/cashier/kitchen seed filters (verified unchanged: 10/6/4 links). The guard/factory/controllers were NOT modified ("do not modify unrelated authorization behavior").
21. 2026-07-31 — **RT-ONB-002 fix — fail-fast on missing baseline:** if no canonical RESTAURANT_OWNER role exists (or it has no permissions), onboarding now throws a descriptive error instead of silently creating a permission-less tenant (the old behavior was the defect). Documented behavior change for unseeded databases only; the canonical seed path is unaffected.
22. 2026-07-31 — **RT-ONB-002 scope:** existing (pre-fix) onboarded tenants are NOT migrated by this task — the fix covers newly onboarded owners per the CTO directive; existing broken roles require re-provisioning (a follow-up can clone the set onto them with the same mechanism). Seeded demo users' placeholder password hashes remain a separate pre-existing limitation (RT-ONB-002 family, documented §7).
23. 2026-07-31 — **Sprint 2 Task 3 — nullable `Order.paymentMethod`:** the column is `PaymentMethodType?` (nullable) rather than NOT NULL, so historical rows are NOT fabricated — the pre-fix behavior genuinely never captured the method, and defaulting old rows to CASH would invent data. New orders always set it (the DTO enforces it; the single shared `createOrder` path writes it). The response echoes the real value (was null/dropped) — the intended contract fulfillment.
24. 2026-07-31 — **Sprint 2 Task 3 — client regeneration side effect:** the schema change requires the Prisma client to be regenerated (api `tsc` cannot typecheck the new field otherwise). The committed generated client was stale (predated `branding` + R3 index rows — LATENT-BRAND-001); regeneration materialized the full schema state in the committed `packages/db/src/generated-client/*`. This is the standard Prisma workflow (generated client must match `schema.prisma`); runtime re-verification of dynamic-branding delivery is a separate follow-up and was NOT claimed in this task.
25. 2026-07-31 — **Sprint 2 Task 4 — discount engine design:** (a) `DiscountService` is a **pure static utility** (no DI) — stateless pricing/validation; the order service does the DB lookup inside the checkout transaction, so a `discount` import needs no module wiring and the tx typing stays clean. (b) **Atomicity:** lookup + usage `increment` + order creation share one `$transaction`, so usageCount can never race and an invalid code aborts the whole checkout. (c) **Uniform anti-oracle error** (`DISCOUNT_INVALID_MESSAGE`) for missing/inactive/out-of-window/exhausted/zero-value codes — callers can't enumerate valid codes via error differences. (d) `Order.discountId` FK is `SetNull` (deleting a discount never cascades order history) + `discountCode` snapshot for display/reporting. (e) Code normalized trim+upper before lookup. (f) Percentage applies to pre-tax subtotal; fixed is capped at subtotal (total never negative).

---

# 15. Outstanding Work

1. **Sprint 1 Step 5** — final gate — **EXECUTED 2026-07-30** (§19 row 11, §21). Sole remaining closure blocker: PREX-TSC-001/002 (`turbo build` fails on db+api `tsc` on main; frontends individually green) — awaiting the CTO's fix-first (dedicated build-gate remediation mandate, Option-B localized slices + Step-5 re-run) vs documented-exception call; PREX-E2E-001 resolved in-gate as R7's canary (§19 row 10). Push afterward is user-owned (§16).
2. **Runtime-defect ledger (2026-07-30):** R4 FIXED (§19 r5), R5 FIXED (r6), R1 FIXED (r7), R3 FIXED (r8 — standard chain M1→M2→M3→M5→M6 verified green) — identifier-disease family closed; R2 CLOSED as investigation-complete (CTO; M4 parked, R2.1 recommends F indexes-only / D HASH(id) fallback). Remaining open runtime decisions: **paymentMethod persistence only** (§15 item 3) — guest-checkout gating parity FIXED 2026-07-30 as Runtime Defect R6 (§19 row 9); public-onboarding fail-safe block FIXED 2026-07-30 as Runtime Defect R7 (§19 row 10 — canary PREX-E2E-001 closed).
3. ~~**`paymentMethod` persistence decision (surfaced by Step-4 live verification)**~~ — `CreateOrderRequestDto` accepts `paymentMethod` and the guest UI sends `CASH` (§14 decision 6), but the `Order` model had **no such column** — the response field was `null` and the value was silently dropped. **RESOLVED 2026-07-31 (Sprint 2 Task 3, §19 row 31)** — added `Order.paymentMethod PaymentMethodType?` (nullable column; no fabricated backfill for historical rows) + migration `20260731000000_add_order_payment_method`; the service now persists `dto.paymentMethod` on the shared create path and the response echoes the real value. Live-verified on both staff and guest checkouts.
4. Guest checkout subscription-status gating parity — **RESOLVED 2026-07-30 (R6, §19 row 9)**: checkout now enforces tenant UNPAID/CANCELED uniformly with guest reads.
5. Build-gate remediation (PREX-TSC-001/002): 140 api + 16 db legacy type errors breaking `pnpm build`/CI Job 3/Docker builder. Step-5 measured nuance (2026-07-30): per-package isolation shows types/qr-menu/backoffice/cashier builds GREEN; only db (16) and api (140) fail; the aggregate `pnpm build` additionally exhibited parallel Next-build resource contention in-sandbox (generic ELIFECYCLE), eliminated by sequential per-app builds — not a code defect. **Progress 2026-07-30: CAT-1a FIXED PREX-TSC-002 (db 16 → 0; db build leg GREEN, §19 row 12); CAT-5 FIXED 13 library-drift errors (api 140 → 127, §19 row 13); CAT-6 FIXED 10 null/undefined-narrowing errors (api 127 → 117, §19 row 14); CAT-1c FIXED 7 Prisma write-input-strictness errors (api 117 → 110, §19 row 15); CAT-4 FIXED 10 response/return-contract-drift errors (api 110 → 100, §19 row 16); CAT-3b FIXED 11 AuthenticatedUser/Request-contract errors (api 100 → 89, §19 row 17); CAT-3a FIXED 23 Express HTTP-layer typing-drift errors (api 89 → 66, §19 row 18); CAT-2 FIXED 29 loose-payload errors (api 66 → 37, §19 row 19); CAT-1b FIXED 13 spec-mock errors (api 37 → 24, §19 row 20); CAT-7 FIXED 17 spec-mock-surface errors (api 24 → 7, §19 row 21); CAT-8 FIXED the final 7 errors (api 7 → 0, §19 row 22); **PREX-TSC-001 closed — api tsc = 0 (db = 0); Step-5 re-run eligible, NOT started per CTO instruction.****
6. Auth-UI gap: no standalone login screen exists in any frontend; the only in-repo writer of `localStorage.accessToken` is the backoffice onboarding wizard (`RestaurantCreationWizard.tsx:253`), so the Cashier app has no independent login path (audit-corrected 2026-07-30). — **RESOLVED 2026-07-31 (Sprint 2 Task 1)**: both staff apps now ship a standalone `/login` screen wired to `POST /api/v1/auth/login` with MFA challenge, session persistence, CSRF echo, and logout; entry pages gate on the session (§5 Sprint 2 row, §19 row 29).
7. Spec-drift inventory resolution (§7 list) — documentation or code alignment decisions (Sprint-3 doc baseline).
8. ~~Invoice PDF generation (real file instead of fabricated `pdfUrl`)~~ **RESOLVED 2026-07-31 (Sprint 2 Task 5, §19 row 33 — real pdfkit PDF + storage + servable URL)**; ~~discount engine (`discountAmount` placeholder)~~ **RESOLVED 2026-07-31 (Sprint 2 Task 4, §19 row 32 — real tenant-scoped discount engine)**; ~~FCM real provider~~ **RESOLVED 2026-07-31 (Sprint 2 Task 7, §19 row 35 — real firebase-admin provider + notifications relation/repository)**; ~~Socket.io Redis adapter vs multi-pod k8s HPA~~ **RESOLVED 2026-07-31 (Sprint 2 Task 6, §19 row 34 — Redis adapter attached, cross-pod proven)**.
9. Dev-port mismatch resolution (LATENT-ENV-001): choose canonical local API port (8000 per `main.ts` default vs 3001 assumed by qr-menu dev defaults) and align code or env documentation.
10. ~~**RT-ONB-002 (discovered 2026-07-31)**~~ — onboarding provisioned a permission-less `RESTAURANT_OWNER` role per tenant (§7 row): onboarded owners logged in but 403'd on RBAC-gated staff endpoints. **RESOLVED 2026-07-31 (Sprint 2 Task 2, §19 row 30)** — onboarding clones the canonical seeded owner permission set (22 rows incl. the 8 guard-vocabulary additions); staff flow verified end-to-end as an onboarded owner. Remaining related pre-existing limitations (NOT this task's scope): seeded demo users' placeholder password hashes; existing pre-fix onboarded tenants not migrated (see §14 decision 22).

---

# 16. Risks

| Risk | Evidence | Impact |
|---|---|---|
| Work loss: sandbox wipes local commits / `.git/config` / `node_modules` / `dist` each turn | Observed repeatedly (5+ wipe cycles by 2026-07-30); commits re-created stat-identically each time | Sprint work only safe in file contents; **push required for durability** |
| Push durability depends on user-side push (2026-07-30 policy: session never pushes, never requests credentials) | `.git/config` snapshot-excluded → `origin` re-added per session; wiped/re-created hashes logged §6 | Until the user pushes, all local commits stay at wipe risk; CI/CD (ci.yml, cd.yml) unverifiable by session |
| No Docker in sandbox (Step-4 resolved via native runtime) | session command evidence (`docker: command not found`); Step-4 completion record §19 | Retired for Sprint-1 verification on 2026-07-30 (native substitute accepted); recurs for future environment needs (any Sprint-2 e2e) |
| GitHub-hosted `e2e-live` run not yet executed (2026-08-10) | new CI job `.github/workflows/ci.yml` `e2e-live` (postgres/redis services, M4 parked via `migrate resolve`, API+qr-menu production boot, chromium); locally executed 5/5 PASS on the identical sequence (PG 17.10 vs CI postgres:15-alpine — both Prisma-supported) | the first push of this branch runs it; until then the local native run (documented §19 row 38) is the evidence of record |
| ~~Broken migrations in chain (R2/R3) + blocked Prisma engines (ENV-TSK-0006)~~ | **LARGELY RETIRED 2026-08-03 (§19 row 36):** R3 fixed 2026-07-30; ENV-TSK-0006 fixed — `prisma migrate deploy` now applies the documented chain **8/8** on a fresh DB and the canonical seed runs against it | Automated provisioning path is **green end-to-end** via canonical CLI commands (psql-apply workaround no longer required). Residual: M4 stays parked pending the R2 architecture RFC (CTO item) |
| Production build broken pre-existing | `tsc` exits 2 in api (140) and db (16); CI Job 3 runs `pnpm build`; Dockerfile builder runs it | Release packaging blocked until remediation |
| Recurring `packages/db/scripts/maintenance.sh` mode flip (100755→100644) after installs | observed repeatedly; restored each time (`chmod +x`), never committed | noise risk in future commits |
| Rate-limit/CSRF assumptions for guest channel depend on middleware host resolution | verified code paths | any proxy stripping `Host`/forwarding breaks tenancy at edge |
| Local dev misconfiguration | LATENT-ENV-001: qr-menu dev defaults (port 3001) vs API default (8000) — **CONFIRMED live 2026-07-30** (SSR error view on default env) | developer onboarding friction; SSR/checkout calls misroute without env alignment |

---

# 17. Next Sprint Entry Point

**Sprint 2 (defined 2026-07-31 by CTO direction — "determine the highest-priority Sprint 2 task and implement"):** scope = closing the staff-experience gaps for the two staff apps (Backoffice, Cashier). **Task 1 — Staff Authentication UI — IMPLEMENTED + VERIFIED + COMMITTED (local `236d52e`, pre-push), awaiting CTO push + approval of Task 2** (§5 Sprint 2 row, §19 row 29): standalone `/login` in both staff apps (email/password → MFA challenge → session), auth-gated entry pages, CSRF echo, logout; all gates green + full live runtime matrix (login 200/401/403, MFA 401→200, CSRF cookies, logout blacklist). **Task 2 — RT-ONB-002 (onboarding RBAC provisioning) — IMPLEMENTED + VERIFIED 2026-07-31, awaiting CTO approval to commit** (§5 Sprint 2 row, §19 row 30): onboarding now clones the canonical seeded RESTAURANT_OWNER permission set (22 rows incl. 8 guard-vocabulary additions) with fail-fast; staff flow verified end-to-end as an onboarded owner (menu 200, checkout 201, tables 201, branches 201, tenant 200); parity seeded≡onboarded SQL-proven. Sprint 1 remains closure-ELIGIBLE (§21); its closure decision is the CTO's and is independent of Sprint-2 task sequencing.

**Status update 2026-08-03 (Sprint 2 Task 8 — §19 row 36):** Tasks 1–8 are all implemented, verified, committed and pushed to `origin/main`. **No unfinished engineering task remains recorded in this document.** ENV-TSK-0006 — the last **High**-priority open defect — is closed, and the automated provisioning path (`pnpm install` → `prisma generate` → `prisma migrate deploy` → `prisma:seed` → API boot) is green end-to-end with zero manual workarounds. Everything still outstanding is a **CTO/architecture decision**, enumerated in §23 item 21: Sprint-1 closure, the R2/PREX-MIG-002 partitioning RFC (M4 parked), the LATENT-ENV-001 canonical dev-port choice, RS256 timing, and the SPEC-DRIFT documentation baseline (Sprint-3).

**Immediate next action: CTO decision on Sprint-1 closure** — Step 5 RE-EXECUTED 2026-07-31 under CTO execution command (§19 row 23); all former gate blockers eliminated (PREX-TSC-001/002 resolved via the CAT program, §19 rows 12–22; PREX-E2E-001 closed as R7's canary). Gate criteria with the 2026-07-31 re-run measured results:
1. Lint 0 — ✅ `turbo run lint` 6/6 workspaces, zero warnings/errors.
2. Suites green — ✅ **277 pass / 2 skip / 0 fail / 279** (31/32 suites, repo filter script) AND ✅ **496 pass / 2 skip / 0 fail / 498** (58/59 suites, root CI Job-2 command `jest --runInBand --forceExit`).
3. `turbo build` — ✅ **all 6 packages GREEN** (types / db / api / qr-menu / backoffice / cashier per-package, and the full chain via official tooling `turbo run build --concurrency=1` — 6/6 in 63 s); the default-concurrency aggregate run OOM-fails ONLY in this 1.9 GiB / 2-core sandbox (exit 137 — environment resource contention, identical class as §19 row 11; api tsc standalone = 0 errors).
4. Docker image builds — ⚠ CANNOT VERIFY in-sandbox (no daemon); statics unchanged vs upstream `df51f37` (4/4 Dockerfiles byte-identical; api builder runs `prisma:generate` → `pnpm run build` → artifact copies); CI Job 4 remains the environment of record.
5. Final commit = the Step-5 re-run docs commit (§19 row 23; local, pre-push per §6 lineage rule); push is user-owned (§16).
Then: decision backlog (§23), each in isolated single commits with before/after verification (R4–R7 pattern).

**After Sprint 1:** Sprint-2 scope — **CANNOT CONFIRM** from `IMPLEMENTATION_ROADMAP.md` (no "Sprint 2" string present in that document); direction is set by the user. Queued inputs: §15 list, SPEC-DRIFT doc baseline, LATENT-ENV-001 port decision.

---

# 18. AI Bootstrap

## CONTINUE_PROJECT_PROMPT

You are continuing the Zayjar Restaurant SaaS platform mid-Sprint-1. Read this before touching anything:

**Repo & flow.** Monorepo at `/home/user/zayjar-specification` (clone of `https://github.com/amarspk/zayjar-specification`, upstream `main` = `df51f37`). Work proceeds in user-gated steps: implement ONE step → verify (tests+lint+tsc) → report → ONE local commit → STOP for approval. Never push (no remote configured: `.git/config` is snapshot-excluded; no credentials). Never create placeholder/mock/fake anything. Fix pre-existing TS errors ONLY on lines you already modify for the step.

**Sprint-1 local commits (may be wiped by sandbox — re-create from intact working tree, identical scopes/messages/stats):**
1. `feat(menu): public QR guest read API per DOC-001 1.2 / DOC-005 4.6 (Sprint 1, Step 1)` — 4 files: `apps/api/src/menu/{menu.module.ts,public-menu.service.ts,public-menu.controller.ts,public-menu.controller.spec.ts}` (629+/2−).
2. `feat(order): guest QR checkout, variant absolute pricing, KDS ticket names per DOC-005 4.3/4.6 (Sprint 1, Step 2)` — 8 files: order module (service/dto/module/new `public-order.controller.ts`), `packages/db/src/repositories/TenantTableRepository.ts` (`findByQrCodeToken`), 3 spec files incl. `kds/kds.gateway.integration.spec.ts` (710+/24−).
3. `feat(qr-menu): SSR guest ordering … (Sprint 1, Step 3)` — 10 files: `apps/qr-menu` page.tsx SSR, MenuBrowser cart + spec, new `src/app/lib/*` (4 source + 2 specs), `next.config.mjs` (1157+/269−).

**Sandbox survival playbook (all verified, do not skip):**
- pnpm is NOT on PATH: use `npx -y pnpm@10 …` (10.34.5). Never trust a plain `npx tsc` when `node_modules` is missing — it fetches a FAKE `tsc` package; after install use `./node_modules/.bin/tsc` inside each package.
- Each turn re-check: `git log` (re-create commits if wiped), `chmod +x packages/db/scripts/maintenance.sh` (mode flip recurs; keep out of commits), `git config user.name "Sprint Agent"` / `user.email "agent@zayjar.local"`.
- After `pnpm install --frozen-lockfile` rebuild in order: `packages/types`: `./node_modules/.bin/tsc`; `packages/db`: `./node_modules/.bin/tsc` (**expect exactly 16 pre-existing errors**, dist still emitted) then `node scripts/copy-generated-client.js`; api tests resolve `@zayjar/db` from `packages/db/dist` — suites fail at load until it exists.
- argon2: ~~shipped prebuild segfaults (glibc 2.41 — reproduced live 2026-07-30); rebuild native binding manually~~ — **NO LONGER NEEDED since 2026-08-03 (§19 row 36):** `allowBuilds` approves `argon2`, so its install script compiles a working binding automatically on `pnpm install`. (Historical fallback if a future environment ever needs it: `cd node_modules/.pnpm/argon2@0.45.0/node_modules/argon2 && /usr/lib/node_modules/npm/node_modules/node-gyp/bin/node-gyp.js rebuild`, verify `node -e "require('./').hash('t').then(()=>console.log('ok'))"`. Note the failure mode is a **silent SIGSEGV that aborts the whole jest run**, not an exception.)
- Prisma CLI: engines download automatically at install since 2026-08-03 (`allowBuilds`), so `prisma generate` / `migrate deploy` / `validate` all work natively. `prisma validate` and `migrate` require `DATABASE_URL` to be set (a placeholder URL suffices for `validate` — it never connects).
- No Docker: runtime e2e impossible in-sandbox; mark live assertions CANNOT CONFIRM.

**Verification commands & green baselines (re-verified live 2026-07-30):**
- `cd apps/api && npx jest src/order|src/menu|src/kds --config jest.config.js` → 50/23/41 tests pass.
- Full api `npx jest --config jest.config.js` → 478 pass / 2 skip / **1 fail expected** in `src/common/e2e.spec.ts` (pre-existing Fail-Safe onboarding failure — never "fix" it silently; documented).
- `pnpm --filter @zayjar/api test` collects fewer suites (278 tests) due to the repo script's shell glob depth — explain, don't panic.
- `npx eslint "src/**/*.ts"` in apps/api → 0 errors. `npx tsc --noEmit` in apps/api → **140 errors, ALL pre-existing legacy** (149 pre-Sprint; −4 Step 2; −5 R1 fix 2026-07-30; any new error = you introduced it: stop). `packages/db` tsc → 16.
- qr-menu: `pnpm --filter @zayjar/qr-menu test` → 28/28; `pnpm --filter @zayjar/qr-menu build` → PASS with `/` shown as `ƒ (Dynamic)`.

**Architecture invariants to respect:** tenant resolution happens in `TenantContextMiddleware` from Host subdomain/Custom-domain/X-Tenant-ID and flows through `dbTenantContext` ALS + Prisma `$extends` injection (unscoped: Tenant, SubscriptionPlan, AuditLog, Notification); guest surface is `@Public()` (+CsrfGuard auto-bypass) with rate tiers public 120 / checkout 30 / auth 10 per min; guest order flow: token→table (`TenantTableRepository.findByQrCodeToken`, uniform 404 anti-oracle) → branch/table server-forced → shared `createOrder` pipeline → variant absolute pricing (Condition C) → tx → post-tx name-resolution → WS `ticket.created`; qr-menu SSR addresses API via incoming Host (prod same-origin; dev `API_INTERNAL_URL` incl. tenant subdomain — dev default port assumption 3001 vs API default 8000: LATENT-ENV-001); API listens on `PORT || 8000` (`main.ts:71`), compose publishes `${PORT:-8000}:8000`.

**Open questions awaiting user decision (do not resolve unilaterally):** ~~push credentials~~ → resolved 2026-07-30 by policy (user pushes personally; session never pushes, §6/§16); Sprint-1 Step-5 approval + its gate-blocker treatment (§21); RS256 deferral; ~~guest-checkout subscription-gating parity~~ (RESOLVED 2026-07-30 — R6, §19 row 9); ~~R2/R3 broken-migration fix scope~~ (RESOLVED 2026-07-30 — R3 FIXED §19 row 8, R2 CLOSED investigation-complete); `paymentMethod` persistence decision (§15 item 3); build-gate remediation ownership (140+16 legacy tsc errors make `pnpm build`/CI-Job-3/Docker fail on main); dev-port alignment (LATENT-ENV-001, CONFIRMED).

Documents of record: this file, plus `DOC-001.md…DOC-010.md`, `SPEC_INDEX.md`, `IMPLEMENTATION_ROADMAP.md`, `TSK-0006_HEALTH_CHECK.md` at repo root.

---

# 19. Verification History

Every verification executed to date. Commit hashes are **local, pre-push** (pushes are user-owned, §16); sandbox wipes of local hashes are documented in §6 — contents and stats are the durable identity.

| # | Date | Sprint | Step | Verification Result | Commit Hash | CTO Decision |
|---|---|---|---|---|---|---|
| 38 | 2026-08-10 | Phase 3 | **Live browser/DB verification in CI + build-gate repair** (exact next task from the 2026-08-09 state) | ✅ VERIFIED (6 files: ci.yml +129, new `playwright.live.config.ts`, new `tests/e2e/verify-phase3-live.spec.ts`, `rbac-permission.guard.spec.ts` +3, `MenuBrowser.tsx` ±, `lib/types.ts` +3): **(a) CI** — new `e2e-live` job in `.github/workflows/ci.yml` (needs test+build): postgres:15-alpine + redis:7-alpine services, `prisma migrate resolve --applied 20260726000001_orders_partitioning_by_year` (M4 parked per R2 — canonical Prisma mechanism, no repo modification), `migrate deploy` (13/13 shippable), canonical seed, full `pnpm build`, `/etc/hosts` `albaik.localhost`, API `dist/main.js` :8000 (health-polled), qr-menu **production** `next start` :3000 (health-polled), `npx playwright install --with-deps chromium`, live spec run via `playwright.live.config.ts` (no webServer — the earlier `npx --yes pnpm` webServer overhead caused the 120s timeouts), artifact upload on failure. **(b) live spec (no mocks)** — 5 tests: SSR menu renders real seeded data + `design:null`→ProductGrid fallback; live cart add/open/close scroll preservation + qty ±/remove; live place order → real `orders` row PENDING + `order_items` + `kitchen_queues`; live preorder via UI → `isPreorder=true`/`preorderStatus=SCHEDULED`/`scheduledAt≈+45min` persisted + server 15-min rule 400 via direct API; live draft-vs-published (TenantDesign row with distinct markers via the real tenant-scoped client → API returns published only, never draft; cleanup restores pristine). **In-task discoveries (honest):** the lazy-PrismaPromise + outer-`await` form of `dbTenantContext.run` loses the ALS context → fail-safe blocks; the proven pattern is `await` INSIDE the async callback (matches all repo specs); the earlier "scroll jump" was Playwright auto-scrolling to the alphabetically-first product (`Baklava`, page bottom — `orderBy name asc`), NOT an app defect. **(c) build-gate repair — Phase 3 branch was never production-built:** api tsc 1 error (guard spec mock missing `isPreorder/scheduledAt/preorderStatus` — added); qr-menu `next build` ESLint fail on ~24 `no-explicit-any` in `DesignSections` (Phase 3 code) — replaced with structural types, zero behavior change; `types.ts` contract gained `isPreorder?/scheduledAt?`; **real defect found + fixed:** `transform: translateZ(0)` on the qr-menu container made it the containing block for `position:fixed` overlays — bottom sheet rendered at document-bottom below the fold (A/B-proven: overlay 1710px doc vs 720px viewport; sheet y≈1311–1779 vs 252–720) — removed. **(d) gates** — api tsc 0, qr-menu build PASS (`ƒ /` Dynamic), qr-menu lint clean, qr-menu 4 suites 34/34, `rbac-permission.guard.spec` 10/10. **(e) runtime** (native stack identical to the CI job: PG 17.10 + Redis 8.0.2 + API :8000 + qr-menu production :3000 + Chromium headless 149): **5/5 live tests PASS**; DB evidence: `ORD-2026-82791` (PENDING, CASH) + `ORD-2026-99952` (PENDING, `isPreorder=t`, `preorderStatus=SCHEDULED`, `scheduledAt=2026-08-10T13:45:00Z` = +45min) + 2 `kitchen_queues` rows; 15-min rule 400 verbatim; draft marker never exposed. **Remaining:** live RTL/LTR (minor); GitHub-hosted `e2e-live` run = environment of record (local native run is the documented equivalent) | single commit (this record + 5 files) — see §28 | Next: `featured/popular` product-selection design decision (§23 item 24) |
| 37 | 2026-08-03 | Sprint 3, Task 1 | **AUDIT-004 — Staff User Management** (CTO-approved from the 2026-08-03 engineering audit) | ✅ VERIFIED (12 files): **(a) defect confirmed pre-fix** — the entire API exposed 51 route decorators and **zero** user-management routes; the only `user.create` anywhere in application code was the onboarding owner (`tenant.service.ts:127`) and the three `user.update` calls were MFA-only, so exactly one user could ever exist per tenant and the Sprint-2 Cashier/Backoffice login screens had no accounts to authenticate. **(b) implementation** — new `user` module: `POST /api/v1/users` (create + optional roles/branches), `GET /api/v1/users` (`isActive`/`branchId` filters), `GET /api/v1/users/:id`, `PUT /api/v1/users/:id` (partial; roles/branches replaced only when the key is present), `PUT /api/v1/users/:id/roles`, `PUT /api/v1/users/:id/branches`, `DELETE /api/v1/users/:id` (soft delete + deactivate). Responses funnel through one mapper so `passwordHash`/`mfaSecret` are structurally unexposed; `mfaEnabled`/`mfaSecret`/`tenantId` are deliberately not updatable (MFA stays owned by the enrolment flow; cross-tenant reassignment is never valid). **(c) branch isolation (DOC-005 §4.2)** — required a schema addition: the spec mandates that a staff token carry assigned branches, but no User↔Branch link existed, so the `branches` claim consumed by `CaslAbilityFactory`'s BRANCH_MANAGER `$nin` rules had no persistent source and was permanently empty. Added `UserBranch` (composite PK `[userId, branchId]`, denormalized tenant-owned `tenantId`, 3 cascade FKs, 2 indexes) + migration M10; assignments are validated against tenant-scoped branches and a foreign id is **rejected**, never silently dropped. **(d) RBAC integration** — `User` was already a CASL subject and already registered in the guard's tenant-repository registry, but **no permission row granted it**; added 4 seed rows (`user:read/create/update/delete`, deterministic sha256-derived UUIDs per the R5 convention). Owner-only by construction: the manager/cashier/kitchen seed filters are resource-allowlisted (`branch`/`menu`/`order`/`customer`/`kds`) so `user` cannot leak into them — SQL-proven owner **4** / cashier **0**. **(e) auth integration** — passwords hashed with the existing `AuthService.hashPassword` (Argon2id, 65 MB/3 passes), hashing performed outside the transaction so it never holds a DB lock; created credentials work against the unmodified login path. **(f) iteration honesty — two real defects found by runtime verification and fixed, not papered over:** the tenant-scoping Prisma extension injects `tenantId` into `data` for top-level creates AND into `where` for reads on every scoped model, but `UserRole` has no `tenantId` column → direct `userRole.create` failed live with `Unknown argument 'tenantId'` (HTTP 500 on create), and `userRole.findMany` failed identically (HTTP 500 on role assignment). Both were re-implemented as **nested writes/reads through the tenant-scoped parent `User`**, which keeps the extension's tenant stamping on the parent while leaving nested payloads untouched — isolation strengthened, not bypassed. Unit tests were corrected to assert the real nested shape rather than the abandoned one. **(g) gates** — tsc api 0 / db 0 / types 0; ESLint 6/6; user suite **17/17**; api filter **328 pass / 2 skip / 0 fail (36/37)** (was 311/2/0; +17), root **565 pass / 2 skip / 0 fail (65/66)** (was 548/2/0); `turbo run build --concurrency=1` 6/6. **(h) runtime** on fresh `zayjar_audit004` (migration chain **9/9** via canonical `prisma migrate deploy` + canonical seed): create **201** (role+branch attached, no secrets in body) → **the created user logs in 200** with correct `CASHIER` roles/permissions in the JWT (the capability that did not exist before this task) → RBAC cashier **403** list + **403** create, owner **200**, anonymous **401** → cross-tenant GET/PUT/DELETE **404/404/404** with the foreign row verified unmodified (404 not 403 — no existence oracle) → foreign-branch assignment **400** with existing scope intact → role swap CASHIER→MANAGER persisted and reflected in a **fresh JWT** (`branch:read/write`, `kds:*` …) → password rotation: old **401**, new **200** → self-delete **400** → soft delete returns `{deleted:true}`, row retained with `isActive=false`/`deletedAt` set → deleted user **404**, login **401**, absent from list (4 remaining). Zero regression in any pre-existing suite | — (single commit carrying the implementation + this record) | AUDIT-004 approved by CTO 2026-08-03; scope strictly limited to AUDIT-004 |
| 36 | 2026-08-03 | Sprint 2, Task 8 | ENV-TSK-0006 — install/provisioning toolchain (last High-priority open defect) | ✅ VERIFIED (3 files, +30/−1): **(a) pre-fix reproduction (clean slate)** — `rm -rf node_modules && pnpm install --frozen-lockfile` → verbatim `Ignored build scripts: @firebase/util, @nestjs/core, @prisma/client, @prisma/engines, argon2, prisma, protobufjs, sharp, unrs-resolver`; Prisma engine binaries ABSENT (`libquery_engine-*.so.node` / `schema-engine-*` missing — timestamp forensics confirmed they only ever appeared lazily at first `generate`, never at install); argon2 `build/` ABSENT and `require('argon2').hash('t')` **SIGSEGV** (silent segfault, exit-code-clean — the same crash that killed a full `jest` run mid-suite this session); `prisma migrate deploy` → **P3018 / 42601 `syntax error at or near "\ufeff"`** at migration 1; `pnpm --filter @zayjar/db exec prisma validate` → **P1012 `Environment variable not found: DATABASE_URL`**. **(b) root causes (three, independent)** — (1) pnpm 10+ blocks dependency lifecycle scripts by default and `allowBuilds` listed only 4 Datadog/msgpackr entries; (2) upstream UTF-8 BOM (`EF BB BF`) heading `20250101000000_init/migration.sql` — the CLI path had never been exercised, so the psql-with-strip-filter workaround masked it; (3) `ci.yml` Job 3 declares no `DATABASE_URL` anywhere (grep = 0 hits) while `prisma validate` resolves the datasource env var before validating. **(c) fixes (minimal, least privilege)** — `allowBuilds` += `@prisma/client` / `@prisma/engines` / `prisma` / `argon2`, each with an inline justification comment; `sharp`, `@nestjs/core`, `protobufjs`, `unrs-resolver`, `@firebase/util` deliberately left UNAPPROVED and each verified to load correctly without its script (`sharp` resolves through the prebuilt `@img/sharp-linux-x64`; `firebase-admin` requires clean) — **audit correction: the original TSK-0006 enumeration over-attributed `sharp` as a blocker**; BOM stripped with a 3-byte surgical edit (26 794 → 26 791 B, `a[3:] == b` proven — zero SQL semantics touched); CI validate step given a connection-free placeholder `DATABASE_URL` + comment (no database service added to the job). **(d) post-fix proof (clean slate again)** — install ignores only the 5 intentionally-unapproved packages; engine binaries present at install time; `argon2/build/Release/argon2.node` compiled from source; `hash`/`verify` → correct `true` **and** `false`; `prisma generate` ✅; `prisma validate` ✅ *The schema at prisma/schema.prisma is valid 🚀*; **`prisma migrate deploy` 8/8 green on fresh PostgreSQL 17.10** (`_prisma_migrations` 8 rows applied, 0 rolled back, 32 tables) — M4 `orders_partitioning_by_year` excluded per the standing R2/PREX-MIG-002 parking, and confirmed to now fail on its own documented layer-2 architecture defect (`42703 column "created_at" named in partition key does not exist`) rather than on the BOM — **the R2 parking is untouched and remains a CTO/architecture-RFC item**; canonical `pnpm --filter @zayjar/db run prisma:seed` → 17/17 entity groups (plans 3 / tenants 2 / users 5 / roles 4 / permissions 22 / products 14 / discounts 2 …). **(e) live runtime on the CLI-provisioned DB** (API `dist/main.js` :8000 + PostgreSQL 17.10 + Redis 8.0.2): `/health` **200**; guest table **200** (real branding payload); guest menu **200**; **real argon2 login **200** with valid JWT** (owner permissions in claims) and wrong password **401** — the auth path that the segfaulting prebuild made untestable; `/auth/me` **200**; RBAC-gated `GET /api/v1/menu/products` **200**; guest checkout **201** → `ORD-2026-15027`, subtotal 24 / tax 3.6 / total 27.6, `paymentMethod=CASH` persisted (Task 3 regression green); discount `SAVE10` → **2.40 off**, total 25.20 (Task 4 regression green); `kitchen_queues` rows created (KDS pipeline green). **(f) no regression** — tsc api 0 / db 0 / types 0; ESLint 6/6; api filter **311/2/0 (35/36)**; root **548/2/0 (64/65)**; `turbo run build --concurrency=1` **6/6**. Zero application source files changed. **Iteration honesty:** the pre-existing §19 row 23 parenthetical "init file's upstream-shipped UTF-8 BOM streamed through a strip filter — psql-only nuance, **Prisma engines unaffected**" is **DISPROVEN** — that claim held only for the psql workaround; the engine/CLI path hard-fails on the BOM (recorded as a §19-footer repository correction) | — (single commit carrying the fix + this record) | Autonomous CTO-workflow execution — next unfinished task per §20 priority |
| 1 | 2026-07-30 | Sprint 1 | Step 1 — public guest read API | ✅ VERIFIED (menu suites 23/23; eslint 0; tsc Δ0 against 149 pre-existing; contracts §8.1/§8.2) | `d1c6035` | APPROVED → proceed Step 2 |
| 2 | 2026-07-30 | Sprint 1 | Step 2 — guest checkout + DEFECT-A/B | ✅ VERIFIED (order 50/50, menu 23/23, kds 41/41, full api 478 pass / 2 skip / 1 pre-existing fail; tsc 149→145 with 0 added) | `0741d5b` | APPROVED → proceed Step 3 |
| 3 | 2026-07-30 | Sprint 1 | Step 3 — qr-menu SSR/cart/checkout | ✅ VERIFIED (build PASS with `ƒ /` Dynamic; qr-menu 28/28 in 3 suites; zero mocks grep-clean; api regression green; independently re-verified same date after session compaction) | `5967db9` | APPROVED → proceed Step 4 |
| 4 | 2026-07-30 | Sprint 1 | Step 4 — live end-to-end verification (native runtime, no repo changes) | ✅ VERIFIED channel end-to-end: menu/table GETs 200 (Decimal→Number, branding #0B5FFF); uniform anti-oracle 404 (incl. soft-deleted token); 403 no-tenant; gating UNPAID/CANCELED→403, PAST_DUE→200, ACTIVE→200; 4× checkout HTTP 201 with server totals 42.55 / 5.75 / 11.50 / 16.67 (last via qr-menu rewrite proxy = browser path); `tableId` server-forced; `kitchen_queues` rows created; WS `/kds` `ticket.created` with real item names; SSR branding + table/branch + error views. **1 item ⚠️ CANNOT VERIFY:** pointer-level cart clicks (no browser automation in sandbox) — equivalently covered through the Next.js rewrite-proxy order. Red pre-existing findings: R1 CONFIRMED, R2/R3/R5 CONFIRMED red, R4 trigger catastrophe CONFIRMED; LATENT-ENV-001 CONFIRMED; LATENT-MEDIA-001 runtime fear DISPROVEN | none (verification-only; zero repository changes) | APPROVED; separate fix approvals issued for R4 and R5 only; R1–R3 left open for scoping |
| 5 | 2026-07-30 | Post-Step-4 | Runtime Defect R4 fix (PREX-MIG-001) | ✅ VERIFIED: migration applies clean; 8/8 triggered tables accept UPDATEs with `updatedAt` trigger override (backdate probe); non-status order UPDATE writes 0 audit rows; DRAFT→PENDING (psql) and PENDING→ACCEPTED (API `PUT` → HTTP 200) each wrote exactly 1 correct `STATUS_CHANGE` audit row; api suite 275 pass / 2 skip / 1 pre-existing fail = baseline (no regression) | `c15843f` | APPROVED commit |
| 6 | 2026-07-30 | Post-Step-4 | Runtime Defect R5 fix (PREX-SEED-001) | ✅ VERIFIED: canonical `pnpm prisma:seed` completes on fresh PostgreSQL 17.10 DB, twice consecutively (FK-cascade cleanup); demo tenant/restaurant/products/categories/QR tokens SQL-verified; db lint 0, db tsc 16, api suite 275/2/1 = baselines (no regression) | `084dd37` | APPROVED commit |
| 7 | 2026-07-30 | Post-Step-4 | Runtime Defect R1 fix (LATENT-KDS-001) — investigate + minimal fix mandate | ✅ VERIFIED after 3-stage proof: (a) static — same line was tsc TS2353 at `kds.service.ts(76,20)` + 4 knock-on collapses; (b) type system after one-line fix `orderBy: { order: { createdAt: 'asc' } }` — kds module 6→1 errors, api total 145→140, 0 added (remaining kds error `kds.controller.ts:46` pre-existing on untouched line, deliberately not fixed); (c) live on fresh DB `zayjar_r1` — `GET /api/v1/kds/tickets` → HTTP 200 (pre-fix 500), oldest-order-first ASC despite newest-first inserts, COMPLETED-order ticket excluded (filter intact), hydration/elapsed/RUSH-escalation/404-path intact, zero `Unknown argument` in API log. Suites: kds 41/41, full api 275 pass / 2 skip / 1 pre-existing fail = baselines. Severity classified **Major** (confirmed production-facing 500 on staff endpoint; not Critical — read-only, WS push path unaffected, no gate criterion hit). Blocking analysis: did NOT block the Sprint-1 Step-5 gate criteria (suites/build/guest channel unaffected), resolved by elimination | — (single commit carrying this record; local, pre-push per §6 lineage rule) | APPROVED commit (minimal one-line fix authorized by mandate) |
| 8 | 2026-07-30 | Post-Step-4 | Runtime Defect R3 fix (PREX-MIG-003) — autonomous-execution mandate (CTO directive 2026-07-30) | ✅ VERIFIED: identifiers-only fix (5 quoted snake_case → camelCase: `"tenantId"`, `"branchId"`, `"createdAt"`, `"orderId"`, `"cookingStatus"`) + provenance comment; fresh DB `zayjar_r3f` chain M1→M2→M3→M5→M6 ON_ERROR_STOP green; object proofs — `tsv_menu_search` (generated ALWAYS) + `idx_products_tsv_menu_search` (GIN) + `idx_orders_kds_polling` + `idx_order_items_kds` with verbatim indexdefs matching schema map names (`schema.prisma:481`/`:508`); FTS functional probe (broast 2A / chicken 5B weights, @@ hits on both). Regression: tsc api 140 / db 16, api suites 275 pass / 2 skip / 1 pre-existing fail, kds 41/41 = baselines → no regression. Classification: identifier bug (R4 family); severity Major (provisioning halt + KDS/FTS indexes missing on all provisioned DBs); did NOT block Step-5 gate. Drift note: pre-fix partial-apply environments need manual reconcile (re-run fails at statement 1 by design) | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | AUTONOMOUS mandate commit |
| 9 | 2026-07-30 | Post-Step-4 | Runtime Defect R6 fix (guest-checkout subscription-gating parity) — autonomous-execution mandate (CTO directive 2026-07-30) | ✅ VERIFIED: (a) runtime pre-fix proof — parity matrix verbatim on live API: tenant UNPAID/CANCELED → table 403 / menu 403 but **checkout 201**; (b) minimal fix — `createGuestOrder` enforces `tenant.status` after scan resolution (`ForbiddenException`, message identical to menu surface: "Online ordering is temporarily unavailable for this restaurant."); scoped P1/P5 spec fixtures chosen over a global mock after the global mock perturbed the staff-path middleware tenant lookup (caught by the missing-header integration test → reverted, scoped version green); (c) live post-fix matrix on `zayjar_rtv` — UNPAID/CANCELED → 403/403, ACTIVE → 200/201, PAST_DUE grace → 200/201, zero orders leaked during gated states. Suites: order 51/51 (51 = 50 + new G6), full api **276 pass / 2 skip / 1 pre-existing fail** (new baseline; +1 vs pre-R6 275), tsc api 140 / db 16, kds 41/41 → no regression. Severity: enforcement gap / policy inconsistency (no data corruption; legitimate orders); did NOT block Step 5 | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | AUTONOMOUS mandate commit |
| 10 | 2026-07-30 | Sprint 1 Step 5 | Runtime Defect R7 fix (RT-ONB-001 — public onboarding blocked by the tenant fail-safe; root cause of canary PREX-E2E-001) — Step-5 defect policy class A (runtime → fix immediately) | ✅ VERIFIED: (a) runtime pre-fix proof — live anonymous `POST /api/v1/tenants` via resolvable tenant host (`Host: albaik.localhost:8000`, pre-fix `dist` emit) → **HTTP 500**, API log verbatim `Fail-Safe Block: Cross-tenant data insertion attempt detected and blocked.`; code-path trace: route `@Public()` (`tenant.controller.ts:34-43`) → service ran context-less → `prisma.user.findFirst` blocked at `packages/db/src/index.ts:34`, tx creates cross-tenant-blocked; apex host anonymous → 403 (`TenantContextMiddleware:88`, invariant); (b) minimal fix — explicit platform scope `dbTenantContext.run({ isPlatformOwner: true }, () => this.executeOnboarding(dto))` (`tenant.service.ts:45-59`, body unchanged; fail-safe + middleware gating untouched) + spec hermeticity repair (mock `prisma.user.findFirst` → null — the fail-safe-served failure is replaced by the production fix in the service, not masked); (c) live post-fix on fresh DB `zayjar_step5` (migration chain 5/5 + canonical seed) — onboard **201** with full provisioning SQL-counted (tenant/subscription TRIALING/owner/RESTAURANT_OWNER role/restaurant+branch =1 each), **409** duplicate subdomain (`The requested subdomain is already registered.`), **409** duplicate email (`An account with this email address already exists.` — the cross-tenant check is global again), **403** apex anonymous (invariant unchanged). Suites: full api **277 pass / 2 skip / 0 fail / 279** (new baseline; PREX-E2E-001 green), tenant suites 16/16, e2e suite 5/5, tsc api 140 / db 16 unchanged (0 added), eslint on modified files 0. Severity Major (merchant self-signup dead in production without a platform-owner token); fixed in-gate per Step-5 defect policy A | see §5 R7 row (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | Step-5 mandate fix (defect policy A) |
| 11 | 2026-07-30 | Sprint 1 | Step 5 — Final Quality Gate (CTO mandate; defect policy A/B applied in-gate) | ⚠️ EXECUTED — evidence-complete, mixed result: (1) ESLint ✅ `turbo run lint` (official CI command) — 6/6 workspaces, zero warnings/errors; (2) TypeScript ❌ counts re-verified via `tsc --noEmit`: api **140** (PREX-TSC-001), db **16** (PREX-TSC-002), types **0** — both OPEN, larger-refactor class → policy B: documented, NOT fixed in-gate; (3) Unit tests ✅ + (4) Integration tests ✅ — repo script `pnpm --filter @zayjar/api test`: **277 pass / 2 skip / 0 fail / 279 total, 31 of 32 suites** (integration suites order.checkout/kds.gateway/media included); interim baseline 276/2/1 reproduced exactly pre-R7; (5) Build ❌ — official `pnpm build` (turbo) fails: `@zayjar/db#build` tsc exit 2 (16); api dependent-skipped. Per-package isolation: db exit 2 (16) / api exit 2 (140) / types ✅ / **qr-menu ✅ / backoffice ✅ / cashier ✅** (all Next production builds pass; the earlier concurrent ELIFECYCLE reproduced as sandbox resource contention from 3 parallel Next builds — not a code defect); (6) Docker builds ⚠ CANNOT VERIFY — `docker: command not found` in sandbox (CI Job 4 remains the authoritative environment; Dockerfiles unchanged since upstream); (7) Runtime sanity ✅ — fresh DB `zayjar_step5`: migration chain 5/5 green (20250101000000_init → add_media_model → add_tenant_branding_jsonb → add_search_indexes [R3 site] → add_lifecycle_triggers [R4 site]; M4 partitioning parked-skipped per closure), canonical seed green (all 16 entity counts incl. plans 3/tenants 2/users 5/products 14/orders 3/orderItems 6), API `dist/main.js` boot clean (only known MediaCleanup fail-safe warn), `/health` 200 with tenant context / 403 without, guest table resolve 200, guest menu 200 (`Host: albaik.localhost:8000`), onboarding 201/409/409/403 matrix (row 10); (8) PROJECT_STATE consistency ✅ — this row + §5/§6/§7/§15/§17/§20/§21/§22/§23 synced to measured reality, mirror sha-verified. **Gate decision: Step CANNOT CLOSE — sole surviving blockers PREX-TSC-001 (140, Critical) + PREX-TSC-002 (16, Major), the `turbo build` criterion; returned to CTO per defect policy B (larger refactor → document + return). In-gate defect: R7 found and FIXED+VERIFIED (policy A, row 10)** | — (docs commit carrying this record; local, pre-push per §6 lineage rule) | Step-5 mandate — AWAITING CTO call (remediation mandate vs documented exception) |
| 12 | 2026-07-30 | Sprint 1 Step-5 remediation (CTO mandate: TS blockers highest priority; fix category-by-category, one commit each) | TypeScript **CAT-1a** (db — PREX-TSC-002): 1 unique defect producing 16 errors | ✅ VERIFIED: root = hand-rolled `PrismaModelDelegate<T>` arrow-property interface (`BaseTenantRepository.ts`) structurally rejecting the $extended tenant-scoped client's `DynamicModelExtensionThis` delegates — contravariant param mismatch (verbatim `Type 'Record<string, unknown>' is not assignable to type 'Exact<A, AddonItemFindFirstArgs<…>>'`). Method-signature variant attempted first, REJECTED by the same chain (verbatim `Type 'A' is not assignable to type 'Exact<A, …>'`) — reverted honestly; final fix = constructor accepts `unknown` + single internal assertion at the choke point (interface contract + 16 subclass sites untouched; no `any` — eslint `no-explicit-any: error` honored; zero behavior change). Results: db tsc **16 → 0** (empty tsc output), api tsc **140 unchanged** (proves 0 added), db build leg GREEN (`tsc` + `copy-generated-client`), db eslint 0, api suites **277/2/0 = 279** (zero regression), types 0. Runtime: type-only change, emit semantics identical — mandate loop (tsc + tests) satisfied | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 1 of 11; CAT-5 next per approved order |
| 13 | 2026-07-30 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-5 only) | TypeScript **CAT-5** (api — third-party library type drift): 6 library roots, 13 errors | ✅ VERIFIED: fixed sites — (a) jwt expiry literals (`jwt.config.ts`) typed via `TokenExpiry = NonNullable<SignOptions['expiresIn']> & string` (**iteration honesty:** first attempt `import type { StringValue } from 'ms'` added TS2307 +1 — `@types/ms` is not linked into the app's visible node_modules scope; replaced by the jsonwebtoken-derived type, re-verified); fixes auth.module:17 + auth.service:91/96 (3). (b) `SetMetadata<K>` misuse corrected in `public.decorator.ts` + `require-permission.decorator.ts` → `CustomDecorator<string>` (2). (c) CASL v7 (7.0.1) builder `conditions` typed `MongoQuery<never>` for string-subject tuples (v6 accepted `MongoQuery`) → one assertion at the destructured `cannot` choke point, rule sites' values untouched (3). (d) CASL v7 `can()` no longer typable with `ForcedSubject` instances for string tuples → single assertion at the guard call, identical runtime tag (`subject()` stamps `__caslSubjectType__`) (1). (e) dd-trace 5.117 `TracerOptions` dropped `analytics` → options object annotated `TracerOptions & { analytics: boolean }`, identical object passed (1). (f) winston-daily-rotate-file 5.0.0 augments a deep path not reflected on the surface `Transports` type AND the package was never imported (latent crash on the file-logging branch) → direct constructor import (identical runtime registration semantics) + `info.meta` narrowing cast (3). Results: api tsc **140 → 127** (exactly −13 predicted; the only auth-tree remainder is `current-user.decorator.ts:8` = CAT-3a, deliberately untouched per scope rule), db tsc **0** maintained, eslint full-api **0**, suites **277/2/0 = 279** (zero regression). Runtime: annotation/assertion-only everywhere except the drf import, which repairs a latently-crashing branch with identical registration semantics (documented, not a behavior regression) | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 2 of 11; CAT-6 next |
| 14 | 2026-07-30 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-6 only) | TypeScript **CAT-6** (api — null/undefined narrowing): 2 roots, 10 errors (subscription.service.ts ×7 TS18048 + billing.service.ts ×3 TS2322) | ✅ VERIFIED: (a) `subscription.service.ts` — 4 methods (`checkProductLimit`, `checkCustomDomainAllowed`, `checkOnlinePaymentsAllowed`, `checkAnalyticsAllowed`) dereferenced `plan: Record<string, unknown> \| undefined` unguarded while sibling `checkBranchLimit` (:61-63) already holds the file's guard idiom → replicated the identical guard (`if (!plan) throw new NotFoundException('Subscription plan not found')`) into the 4 methods (+16 lines, −0); reachable-path behavior byte-identical (plan-undefined runtime-disproven 2026-07-30 — required Prisma relation always loaded, per the LATENT-SUB-001 row; on the proven-unreachable path fail-fast 404 now replaces an uncontrolled TypeError — the sanctioned file idiom). (b) `billing.service.ts` — all 3 TS2322 (`string \| undefined` → `string` at :208/:250/:341) share one choke point: `:130 const eventId = event.id as string \| undefined` → narrowed at its own declaration site to `as string` (assertion honest to Stripe's contract — `Event.id` is always present and the idempotency machinery :140-152 already keys on it; `as` erases at emit → zero runtime change). **Iteration honesty:** the narrowing unmasked a previously-suppressed TS2741 at the terminal return (:338, missing `action`) — a pre-existing defect implied by the same declared webhook contract that already forces CAT-4 :157/:163; resolved contract-honestly: terminal return now emits `action: 'processed'` (uniform `action` discriminator across all 7 return sites of the method) + contract `action: string` → `action?: string` — the minimal pair keeping every reachable return's emitted value byte-identical to pre-fix (the terminal return never serialized `action` before; `action?: string` is exactly as serializable) while making the annotation truthful. Alternatives evaluated + rejected: contract-only change (would dissolve CAT-4 :157/:163 — category-scope violation); `action` assertion/fabrication guard (type-lie / behavior change). Results: api tsc **127 → 117** (exactly −10 predicted; verbatim sorted-list delta = the 10 CAT-6 lines removed, 0 genuinely added — CAT-4 :157/:163 re-quoted with the corrected contract excerpt, same defect/site, CAT-4 count unchanged 10; CAT-2 :362/:449/:450 line-shifted +1 by the inserted line, same code), db tsc **0** maintained, eslint modified-files **0** + full-api **0**, suites **277/2/0 = 279 (31/32 suites)** — zero regression. Cycle-4 sandbox wipe this session: CAT-1a/CAT-5 commits re-created stat-identically (§6 pins) before the CAT-6 work | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 3 of 11; CAT-1c next per approved order |
| 15 | 2026-07-30 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-1c only) | TypeScript **CAT-1c** (api — Prisma write-input strictness): 5 roots, 7 errors | ✅ VERIFIED: (a) `order.service.ts:266` (Exact<> nested-create array): root = builder arrays declared `Array<Record<string, unknown>>` → re-declared with truthful generated types `Prisma.OrderItemUncheckedCreateWithoutOrderInput[]` + `Prisma.OrderItemAddonUncheckedCreateWithoutOrderItemInput[]`; pushed literals now structurally checked clean — zero reshaping (1). (b) `audit.service.ts:34/35` (nullable-Json write boundary): `(entry.oldValues \|\| null) as Prisma.InputJsonValue` ×2 — runtime value (incl. plain null) unchanged (2). (c) `billing.service.ts:296/302` (enum writes `string → SubscriptionStatus/TenantStatus` inside the tx closure where the outer narrowing resets): per-site assertions, same strings/same updates (2). (d) `tenant.service.ts:33` (plan entity rows `Decimal` vs declared DTO `number` on a READ contract): extracted to `const plans` + site-local double assertion to the declared 9-field DTO — projection/map/Number() rejected as runtime-changing; assertion erases at emit (1). (e) `admin.service.ts:41` (TS2352 plan `Decimal` vs claimed `{ priceMonthly?: number \| null }`): claim relaxed to `{ priceMonthly?: unknown }` — the site only truthiness-checks + `Number()`s the value (1). **Iteration honesty:** first implementation misplaced the `Prisma` import into the `@nestjs/common` block (verbatim `error TS2305: Module '"@nestjs/common"' has no exported member 'Prisma'`, +1) — caught by the sorted-list delta, corrected into the `@zayjar/db` import, re-verified. Results: api tsc **117 → 110** (exactly −7 predicted; normalized-signature delta = 6 unique signatures retired across the 7 sites, 0 new signatures; CAT-2 `order.service` ×3 and CAT-8 `tenant.branding` ×3 line-shifted +1/+12 by the import/declaration lines — same defects, CAT-2/CAT-8 counts unchanged), db tsc **0** maintained, eslint modified-files **0** + full-api **0**, suites **277/2/0 = 279 (31/32 suites)** — zero regression. Runtime: annotation/assertion-only at all 5 sites — query plans, payloads, enum/json values byte-identical. Cycle-5 sandbox wipe this session: CAT-1a/5/6 commits re-created stat-identically (§6 pins; byte-convergence proof on the CAT-5 PSM layer) before the CAT-1c work | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 4 of 11; CAT-4 next per approved order |

**Documentation-misquote corrections (logged at CAT-1c, 2026-07-30):** (1) §5 CAT-6 row describes the billing diff as "+11/−10 net"; the committed minimal diff is **+3/−2** (git coalesces the object-literal insertion; verified against commit `5ef52f9` stat). (2) The ✳ `decimalToNumber`/CurrencyRounding provenance annotations drafted in the CAT-1c working session were session notes only — never committed; the byte-verified CAT-6-mid code state is the reconstruction anchor (session note retained untracked as `LATENT_prism_decision_notes.md`). (3) The CAT-3a completion report quoted mirror sha256 `82adb8f6…` — that was the pre-renumber intermediate; the committed CAT-3a-era PSM sha256 is **`1cf200f9173c4227bbad…`** (the 17.5→18 row renumber landed after that reading; content otherwise identical). (4) The §21 measured-results line's reference "§19 rows 12–17.5" (drafting artifact of the same renumber) normalized 2026-07-31 to "§19 rows 12–18". (5) The §6 cycle-9 chain line (a) omitted `86d9a95`(CAT-3b) between CAT-3a and CAT-4 and (b) pointed CAT-2's hash at "§19 row 19 / §5 table", which carry no hash (a category commit carries the PSM, so its own hash can only be pinned at the following cycle) — both repaired in the cycle-10 chain rewrite; CAT-2's lineage (`2f1903d` → `8384856`) is named in the tenth-cycle pin sentence. (6) The §21 measured-results reference "§19 rows 12–18" had lagged the CAT-2 update (rows already reached 19) — normalized to "§19 rows 12–20" at this CAT-1b update.
| 16 | 2026-07-30 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-4 only) | TypeScript **CAT-4** (api — response/return-contract drift): 5 roots, 10 errors | ✅ VERIFIED: all fixes are contract-truthful re-declarations (annotation-only; query bodies, payloads, control flow byte-identical) — (a) `tenant.service.ts` onboarding: `onboard` + `executeOnboarding` declared `Promise<Record<string, unknown>>` while returning the fixed 4-object shape → re-declared to the truthful `{tenant, owner, restaurant, branch}` shape (verified from the method's own return literal; matches the controller's public annotation) — retires tenant.controller:43 + e2e.spec:86/87/88 (4). (b) `payment/wallet.service.ts`: `createWalletPayment` + 3 sub-methods declared `Record<string, unknown>` while all 6 return literals carry the controller's full 11-field shape → truthful contract on all 4 methods; TS then **structurally proved** each return literal — exposing one deeper in-root drift (`nextAction.url` live in the Tap-mock path, absent from the public contract) → service-side `nextAction` widened with `url?: string`; controller annotation left unchanged (width-subtyping accepts the wider service type; public response shape unaffected) (1). (c) `billing.service.ts` webhook sub-methods: callers pass the post-CAT-6 always-string `eventId`, yet params/contracts declared it optional → `eventId: string` at both signatures — retires :157/:163 (2). (d) `kds.controller.ts:22-37` response annotation said `createdAt: unknown` / `addons: string[]` while the service provably returns `Date` / `(string \| null)[]` → re-declared to truth (kds module retires to 0) (1). (e) `email.service.ts` `sendEmail` + 3 sibling contracts required `to/subject/template/messageId`, but 2 failure paths provably omit them and all consumers read only `.success` → the four fields widened to optional across the 4-method delegation chain (2). **Iteration honesty:** (i) a first batch aborted atomically on an assertion (email contract block exists ×4 — siblings share it; billing contract pair miscounted) — files left byte-untouched, edits re-applied with verified multiplicities; (ii) the wallet structural proof surfaced the `nextAction.url` knock-on (+1 TS2353) — resolved in-root by the truthful widening, net still exactly −10. Results: api tsc **110 → 100** (exactly −10 predicted; normalized-signature delta = 9 unique signatures retired across the 10 sites, 0 new; CAT-8 `tenant.branding` ×3 line-shifted +10 by the annotation blocks — same defects), db tsc **0** maintained, eslint modified-files **0** + full-api **0**, suites **277/2/0 = 279 (31/32 suites)** — zero regression. Cycle-6 sandbox wipe this session: CAT-1a/5/6/1c commits re-created stat-identically (§6 pins; baseline reproduced byte-identical at 110) before the CAT-4 work | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 5 of 11; CAT-3b next per approved order |
| 17 | 2026-07-30 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-3b only) | TypeScript **CAT-3b** (api — AuthenticatedUser/Request contract): 3 roots, 11 errors | ✅ VERIFIED: runtime truth established first — `JwtStrategy.validate()` is the sole production producer of `req.user` and returns `{id: payload.sub, email, tenantId, roles, permissions}`: a `sub` property **never exists at runtime** (the second producer, test-helpers `MockJwtAuthGuard`, carries none either), so in all six `user.id \|\| user.sub` expressions the right operand was a dead read and `user.id` (always a non-empty string mapped from the required JWT `sub` claim) is the value every call already used → (a) dead fallbacks collapsed on the exact erroring lines: `asset.controller.ts:36`, `billing.controller.ts:31`, `device-token.controller.ts:30/50/61` (×3), `payment.controller.ts:37` (6× TS2339) — runtime behavior byte-identical (short-circuit never evaluated the removed operand). (b) `billing.controller.ts:56` TS2352: webhook handler reads the raw-body-middleware-attached `.rawBody` off the `AuthenticatedRequest` → compiler-prescribed `as unknown as` hop at the existing cast (assertions are erased at runtime; emitted JS identical) (1). (c) `media.controller.integration.spec.ts` ×4 TS2345: controller-level spec where guards never run and `{ user: { tenantId: 't1' } }` can never structurally satisfy `AuthenticatedRequest extends express.Request` → the 4 fixture declarations annotated `as unknown as AuthenticatedRequest` (+1 import) — declares the post-guard contract; jest runtime identical (4). **Deliberately untouched:** `rate-limit.guard.ts:36` `.sub` read (no tsc error — its request is not typed `AuthenticatedUser`; outside CAT-3b's 11 recorded errors), the `AuthenticatedUser` interface itself (adding `sub` would falsify the contract — runtime never provides it), and all CAT-3a/2/1b/7/8 sites per scope rule (`current-user.decorator.ts:8` included). Results: api tsc **100 → 89** (exactly −11 predicted; sorted-list delta = exactly the 11 recorded CAT-3b errors retired verbatim, 0 new; normalized-signature delta 11 retired / 0 new; **payment + asset modules retire to 0**), db tsc **0** maintained, eslint full **0** (6/6 workspaces), suites **277/2/0 = 279 (31/32 suites)** — zero regression. Cycle-7 sandbox wipe this session (first to also wipe `/tmp`): all five CAT commits re-created stat-identically before work began (§6 pins — anchors +27/−11, +62/−22, +30/−11, +38/−20, +97/−37 all reproduced; CAT-1a-era PSM layer re-derived under anchor constraint, sha `7a7016df…` durably backed up in `cat-recon-backup/c7/`); baseline reproduced exactly at **100** (sorted-list capture + per-module recount = recorded post-CAT-4 map) | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 6 of 11; CAT-3a next per approved order |
| 18 | 2026-07-31 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-3a only) | TypeScript **CAT-3a** (api — Express HTTP-layer typing drift, @types/express 5.0.6 / essc 5.1.2): 6 roots, 23 errors | ✅ VERIFIED: all fixes declare the verified runtime truth with byte-identical runtime behavior on every currently-reachable path — (a) `audit.interceptor.ts` (11): the `user as Record<string, unknown>` cast at :31 was a TS2352 that poisoned `userId` to `{} | null` (tsc truthiness-narrowing of the cast's `unknown` member) feeding 2 knock-ons at the two `log()` calls — since CAT-3b proved `sub` never exists at runtime, the dead fallback was collapsed to `user?.id || null` (kills :31/:48/:66); `parseEntityFromUrl`'s `params` declared `Record<string, string>` while essc 5.1.2's `ParamsDictionary` carries `string \| string[]` values (wildcard params) → helper re-declared `Record<string, string \| string[]>` (its own `String(...)` reads already handle both — kills :44); `ipAddress` typed via `headers['x-forwarded-for']` (`string \| string[]` per @types/node) while `AuditLogEntry.ipAddress: string` is the Prisma String-column write contract → write-choke `as string` on the const, CAT-1c doctrine (kills :54/:72); `sanitizeValues` returned `unknown` and indexed a `{}` → return declared `Record<string, unknown> \| null` with the spread input explicitly typed (kills :135 + :53/:71); `AuditLogEntry.oldValues/newValues` widened `\| null` in `audit.service.ts` — `log()` already coerces `entry.oldValues \|\| null` at runtime and the interceptor is the sole caller (kills :52/:70). (b) `current-user.decorator.ts:8`: indexed access `user?.[keyof AuthenticatedUser]` yields the full property union `string \| string[] \| null \| undefined` while the declared return hid `null`/`string[]` → return declared to the exact runtime union — all 4 consumers use `@CurrentUser()` with no data arg and param types are decorator-decoupled, verified 0 ripple (1). (c) `http-logging.middleware.ts:43`: `res.end` wrapper rest-args typed `any[]` vs the overloaded `end` tuple → `...args: Parameters<Response['end']>` (last-overload tuple = the same shape `apply` demands); runtime-identical rest params; the now-unused first eslint-disable removed, the still-live `as any` overload-proxy kept (1). (d) `correlation-id.middleware.spec.ts` ×6 TS18048: mock declared `Partial<Request>` making `headers` optional while every test assigns `{ headers: {} }` → declared `Partial<Request> & { headers: NonNullable<Request['headers']> }` (Request remains assignable to it, so the existing `req as Request` stays a legal downcast; jest runtime unchanged) (6). (e) `media.controller.ts:52/61/71` + (f) `order.controller.ts:22`: 4 methods passed `req.user?.tenantId` (`string \| null`) straight into `string`-typed service params while every sibling controller (asset/billing/device-token/payment) and this file's own `upload()` narrow first — service-side widening was considered and rejected (service bodies feed `tenantId` into Prisma String where-inputs: widening would create NEW errors, forbidden) → the house guard idiom added exactly as written in `upload()` / `billing.controller` (`BadRequestException('Tenant context is required')` ×3; `ForbiddenException('Tenant context missing from authenticated request')` ×1): runtime-identical for every tenant-bound request; the previously-untyped null path now fails closed 4xx instead of surfacing downstream (CAT-6 guard precedent) (4). **Iteration honesty:** the :44 ParamsDictionary fix unmasked a previously-masked third-arg error (`responseData: unknown`, :44,77) — resolved in-root by a call-site assertion to the helper's own declared `Record<string, unknown> \| undefined` (zero runtime tokens; same-line knock-on, CAT-6 :130/:338 pattern); net delta still exactly −23. Results: api tsc **89 → 66** (exactly −23 predicted; sorted-list delta = the 23 recorded CAT-3a errors retired verbatim, 0 new; normalized-signature 23/0; **auth module retires to 0**), db tsc **0** maintained, eslint full **0** (6/6 workspaces), suites **277/2/0 = 279 (31/32 suites)** — zero regression. Cycle-8 sandbox wipe this session: all six CAT commits re-created stat-identically before work began (§6 pins; anchors +27/−11 … +23/−20 all reproduced from SHA-backed backups; baseline byte-compared identical at **89** vs the previous cycle's verified list) | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 7 of 11; CAT-2 next per approved order |
| 19 | 2026-07-31 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-2 only) | TypeScript **CAT-2** (api — loose service payloads): 5 roots, 29 errors | ✅ VERIFIED: every edit is a type assertion at a verified choke point — **zero emitted-JS delta** (assertions erased at compile time; runtime byte-identical, stronger than the guard-idiom classes) — (a) `billing.service.ts` (14): Stripe webhook `dataObject` is intentionally loose (`Record<string, unknown> \| undefined`, line 129); the handler's own later code already used per-access assertions (`:361-362 as string \| null`) — completed the file's own idiom on the unasserted reads: subscription/invoice/customer branch ID extractions `as string` / `as string \| null` / `(subscription \|\| id) as string` (6); `metadata` typed-view `(… as Record<string, unknown> \| undefined)?.tenantId` + `… as string` at the two metadata fallbacks (:199/:200, :450/:451) and the checkout sub-method (:363) (5); Stripe unix-second fields under their existing truthy guards `(… as number) * 1000` ×3 (:270/:273/:279) (3). (b) `dispatch.service.ts` (4) + `worker.ts` (4): the two byte-similar channel switches feed `payload: Record<string, unknown>` locals into string-typed downstream contracts — all feeding locals asserted on their own lines: `emailTo`, `phone`, `smsMessage`, `userId (\| undefined, guard narrows)`, `title`, `body`, `branchId (\| undefined, guard narrows)`; downstream signatures verified beforehand (`sendEmail(to: string, template: string, variables: Record<string, unknown>)`, `sendSms(to, message, _tenantId?)`, `sendPushNotification(tenantId, userId, title, body, data?)`, `broadcastOrderEvent(tenantId, branchId, eventName, payload: BroadcastPayload)`) — tsc reports only the first bad argument per call, so `title`/`body`/`smsMessage` were masked knock-ons behind `userId`/`phone` (CAT-6 unmask pattern) and were asserted pre-emptively; net still exactly −8 for the pair ×2 files. (c) `email.service.ts` (1): SendGrid delivery-webhook `event.email` is a string when present → `(event.email as string \| undefined)?.toLowerCase()` (a non-string runtime value would crash today identically). (d) `order.service.ts` (5): `generateInvoice`'s loose `order: Record<string, unknown>` feeds `sendInvoiceEmail`'s strict declared payload — the constructed order payload provably carries string `orderNumber`/`branchId` and numeric totals (`createOrder` accumulates `subtotal = 0` numerically) → per-field `as string`/`as number` on the 5 literal lines (:480/:482/:483/:484/:485); the documented `pdfUrl` placeholder untouched. (e) `webhook.service.ts` (1): `catch (err)` (`useUnknownInCatchVariables`) assigned to `lastError: Error \| null` → `err as Error` — the file's own idiom on the very next line (`(err as Error).message`) (1). **Iteration honesty:** first application verified clean — no knock-on required a second pass; the masked-arg risk identified during investigation (tsc first-bad-arg reporting) was neutralized by asserting all feed locals in (b). **Scope proof:** remaining 37 errors partition exactly onto the untouched categories (CAT-1b 13 / CAT-7 17 / CAT-8 7). Results: api tsc **66 → 37** (exactly −29 predicted; sorted-list delta = the 29 recorded CAT-2 errors retired verbatim, 0 new; normalized-signature 29/0; **billing, notification, webhook, worker modules retire to 0**), db tsc **0** maintained, eslint full **0** (6/6 workspaces), suites **277/2/0 = 279 (31/32 suites)** — zero regression. Cycle-9 sandbox wipe this session: all seven CAT commits re-created stat-identically before work began (§6 pins — anchors +27/−11 … +35/−22 all reproduced; baseline byte-compared identical at **66** vs the previous cycle's verified list) | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 8 of 11; CAT-1b next per approved order |
| 20 | 2026-07-31 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-1b only) | TypeScript **CAT-1b** (api — spec mocks vs the $extended Prisma client): 4 roots, 13 errors | ✅ VERIFIED: every edit is a type-only assertion/annotation inside `*.spec.ts` — zero runtime surface (specs never enter the runtime bundle; assertions erase at compile time — the weakest-risk fix class) — (a) `order.service.spec.ts` ×8 TS2345 (:280–:283, :286–:289): the lifecycle fixture `makeOrder()` returned an 8-field partial Order while `TenantOrderRepository.prototype.findById/update` resolve to the full post-CAT-1a extended-client `Order` (13 fields — `type`, `createdAt`, `updatedAt`, `customerId`, `tableId`, `specialNotes`, …); the file's standing idiom mocks every repository result with `as any` (45 occurrences in-file) → choke-point fixed with a single explicit `: any` return annotation on `makeOrder` (1 edit retires all 8 chained `mockResolvedValueOnce` sites; object literals byte-untouched). (b) `audit.service.spec.ts` ×2 TS2345 (:104, :128) + `subscription.service.spec.ts` ×1 TS2345 (:158): `jest.spyOn(prisma.<model>, '<method>').mockImplementation(async (args: any) => …)` failed assignability — the implementation's inferred `Promise<any>` return is incompatible with the extended client's **branded** `PrismaPromise<T>` (`[Symbol.toStringTag]: "PrismaPromise"` literal tag vs plain Promise's `string`) → each implementation fn wrapped in the file's own idiom `((async …) as any)`; captured locals and seeded return literals untouched (3). (c) `tenant.service.spec.ts` ×2 TS2345 (:42, :83): the `$transaction` mock implementation accepted its callback as `(tx: typeof txMock) => Promise<unknown>` — contravariance against the extended client's full-client callback parameter fails under strictFunctionTypes → same `as any` wrap on the two identical lines; `txMock` shapes and all assertions untouched (2). **Iteration honesty:** single pass, no second iteration — every edited call expression takes exactly one argument, so no masked later-arg risk (the CAT-6/CAT-3a pattern) could exist; the verbatim sorted-list delta confirms. **Scope proof:** remaining 24 errors partition exactly onto the untouched categories (CAT-7 17 / CAT-8 7); CAT-7/CAT-8 files byte-untouched. Results: api tsc **37 → 24** (exactly −13 predicted; sorted-list delta = the 13 recorded CAT-1b errors retired verbatim, 0 new; normalized-signature 13/0; **order, audit, subscription modules retire to 0**), db tsc **0** maintained, eslint full **0** (6/6 workspaces; the `*.spec.ts` no-explicit-any exemption is the repo's standing `.eslintrc.json` overrides block — no config change), suites **277/2/0 = 279 (31/32 suites)** — zero regression. Cycle-10 sandbox wipe this session: all eight CAT commits re-created stat-identically before work began (§6 pins — anchors +27/−11 … +47/−45 all reproduced from SHA-backed backups incl. the new `cat-recon-backup/c10/` CAT-2 layer; baseline byte-compared identical at **37** vs the previous cycle's verified list `cat-work/after-cat2.txt`) | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 9 of 11; CAT-7 next per approved order |
| 21 | 2026-07-31 | Sprint 1 Step-5 remediation (CTO approval: proceed CAT-7 only) | TypeScript **CAT-7** (api — spec media/device mocks vs repo type surfaces): 3 roots, 17 errors | ✅ VERIFIED: every edit is a type-only annotation/declaration/assertion inside `*.spec.ts` — zero runtime surface (specs never enter the runtime bundle) — (a) factory self-reference (×2 files): the hoisted `jest.mock('@zayjar/db', …)` factory declared `const mockPrisma = { …, $transaction: jest.fn((cb: any) => cb(mockPrisma)) }` — mockPrisma referenced in its own initializer → noImplicitAny inference cycle (TS7022 :7 self-type + TS7024 :18 implicit-return, per file) → a single explicit `const mockPrisma: any = {` in each factory (`media.service.spec.ts`, `media-retry-cleanup.spec.ts`) retires all 4; the exemption is the standing `.eslintrc.json` overrides block — no config change (4). (b) jest-mock surface vs interface declarations (9 TS2339): `let mockStorage: StorageProvider` / `let mockQueue: MediaCleanupQueueService` typed the mocks as the production interfaces, so the runtime `jest.fn()` properties `.mock` (`media.service.spec:287/312/466`, `media-retry-cleanup:96/118/306`), `.mockImplementation` (`media-retry-cleanup:209/214`) and `.mock.calls.find` (`media.service.spec:507` on `enqueue`) were invisible → declarations re-typed to `jest.Mocked<StorageProvider>` (×2 files) and `jest.Mocked<MediaCleanupQueueService>` — the @types/jest mapped type made for exactly this; the assigned `jest.fn()` literals re-verified assignable with **no** `as any` added; `new MediaService(mockStorage, mockProcessor, mockQueue)` unchanged (Mocked is a subtype); declaration-only — emit-irrelevant (9). **Iteration honesty:** the stronger typing unmasked one previously-hidden TS18048 (`media.service.spec:511` — `enqueueCall` possibly `undefined`, masked behind the :507 `.mock` error) → resolved in-root as `enqueueCall![0]` — the immediately-preceding `expect(enqueueCall).toBeDefined()` matcher aborts the test on undefined, so the assertion codifies an existing runtime guard (CAT-6/CAT-1b unmask doctrine); net delta still exactly −17, 0 genuinely added. (c) `device-token.service.spec.ts` ×3 TS2571 (:168–:170): `sendPushNotification`'s declared contract is intentionally loose (`payloads?: Array<{ message: Record<string, unknown> }>`, service:114–117) so deeper reads yield `unknown` → hoisted one typed view of `.message` with the shape verified from the service's own payload literal (`{ notification: { title, body }, data: Record<string, unknown> }`, service:126–136); the service contract left untouched — widening a non-spec production file is out of CAT-7 scope, and the loose mock-shape placeholder is the service author's documented intent (same class as the CAT-2 pdfUrl placeholder) (3). (d) `media-concurrency.integration.spec:49` ×1 TS2353: the seed passes `price: 10`, but the live `Product` model has **no `price` column** — renamed to `basePrice` (schema:353) — and requires `categoryId` → Restaurant chain (schema:327–339); a rename probe empirically surfaced the previously-masked TS2322 missing-`categoryId` in the same literal (first-error masking). Full schema-truthful repair would fabricate a restaurant→category→product seed chain (new runtime statements) in a `DATABASE_URL`-gated suite (`describeIfDb`/`describe.skip` per the file header; the standing 2-skipped-tests figure) → per the preserve-runtime rule the literal was asserted `as any` (zero emitted-JS delta: `create` receives the identical object; when DB-gated on it fails exactly as today with Prisma unknown-arg `price`) plus an in-file drift note. **Functional seed repair (restaurant→category→`basePrice` realignment) DEFERRED to an owner decision** — same deferred class as paymentMethod persistence (§15 item 3) — and flagged in the completion report (1). **Scope proof:** the remaining 7 errors are exactly CAT-8 (queue-health 1, media.service 3, tenant.service 3); CAT-8 files byte-untouched. Results: api tsc **24 → 7** (exactly −17 predicted; sorted-list delta = the 17 recorded CAT-7 errors retired verbatim, 0 new; normalized-signature 17/0; **device-token module retires to 0**), db tsc **0** maintained, eslint full **0** (6/6 workspaces), suites **277/2/0 = 279 (31/32 suites)** — zero regression. Cycle-11 sandbox wipe this session: all nine CAT commits re-created stat-identically before work began (§6 pins — anchors +27/−11 … +21/−19 all reproduced incl. the new `cat-recon-backup/c11/` CAT-1b layer; baseline byte-compared identical at **24** vs the previous cycle's verified list `cat-work/after-cat1b.txt`) | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 10 of 11; CAT-8 final category per approved order |
| 22 | 2026-07-31 | Sprint 1 Step-5 remediation (CTO approval: execute CAT-8 only) | TypeScript **CAT-8** (api — final projection/nullability debts): 4 roots, 7 errors | ✅ VERIFIED: every edit is a type-only annotation/assertion on production sites whose runtime truth was verified first — **zero runtime delta** (assertions erase; all emitted paths semantically identical) — (a) `media.service.ts:73` TS2345: `MEDIA_TYPE_CONFIG` is `as const` (constants.ts:47–96) → `config.allowedTypes` is a union of string-literal tuples and `.includes(string)` collapses to parameter `never` → call-site assertion to the widening supertype `readonly string[]` (legal upcast; `.includes` runtime identical). (b) `media.service.ts:112` TS2552 (LATENT-MEDIA-001 — closed by this fix): stray `typeof urls` referenced a non-existent value → re-declared to the truthful bundle `{ originalUrl: string; thumbnailUrl: string \| null; mediumUrl: string \| null; largeUrl: string \| null }` — shape verified against the Media columns (schema:695–698) and all three assignments (:119, :141, :170) + reads; sibling locals pass the identical definite-assignment CFA today, so no TS2454 surface — confirmed post-fix. Emitted JS was and remains a bare declaration. (c) the `tx.media.create` `mediaType` TS2322: the `:68–70` config guard already rejects any non-enum `mediaType` with a 400 BEFORE the create (MEDIA_TYPE_CONFIG keys ≡ Prisma `MediaType` members — verified identical sets) → choke-point assertion `mediaType as MediaType` (shorthand expanded — semantically identical property write; `MediaType` already imported at :2). (d) `tenant.service.ts` ×3 TS2339 (:270, :338, :347): **the checked-in generated Prisma client predates the `branding` Json column** — DECISIVELY verified: the client snapshot `packages/db/src/generated-client/schema.prisma` contains zero `branding` refs, so the DMMF cannot SELECT the column and `tenant.branding` is `undefined` at runtime today, with the existing `\|\| {}` fallbacks already yielding `{}` → reads asserted `(X as { branding?: unknown }).branding` (zero emitted-JS delta; behavior identical in both runtime scenarios). **NEW verified functional gap LATENT-BRAND-001** (§7/§20): dynamic-branding JSONB is silently degraded in the distributed client; remedy = client regeneration (blocked in-sandbox by ENV-TSK-0006; toolchain/architecture decision outside the CAT mandate) — deferred to CTO; LATENT-TEN-001's code-level closure cross-referenced but stands. (e) `queue-health.service.ts:81` TS7053: `QueueName` includes `dead-letter-queue` while `WORKER_CONFIG` intentionally maps only the 3 worker queues (DLQ falls to the inline default) → read-site assertion to `Partial<Record<QueueName, (typeof WORKER_CONFIG)[keyof typeof WORKER_CONFIG]>>`; the `\|\|` default fold semantics unchanged. **Iteration honesty:** (i) one edit initially clipped the adjacent `originalName` line in media.service.ts — caught on immediate context re-read and restored before any verification ran; final file byte-verified by numstat + full gates. (ii) a MID-TURN sandbox wipe (cycle 13) landed between the api-tsc=0 verification and the lint/test gates — survived: `/home/user/cat8-final/` snapshot preserved the edits, 10 layers rebuilt (anchors reproduced), the 3 files re-applied from `cat-recon-backup/c13/`, toolchain reinstalled, ALL gates re-run post-reconstruction with identical results. Results: api tsc **7 → 0** (exactly −7; sorted-list delta = the 7 recorded CAT-8 errors retired verbatim, 0 added; **every module 0 — PREX-TSC-001 eliminated**), db tsc **0** maintained, eslint full **0** (6/6 workspaces), suites **277/2/0 = 279 (31/32 suites)** — zero regression. **TypeScript gate GREEN; Sprint-1 Step-5 re-run eligible — NOT started per CTO instruction** | — (single commit carrying fix + this record; local, pre-push per §6 lineage rule) | CAT program — mandated category fix 11 of 11 — **program COMPLETE** |

| 23 | 2026-07-31 | Sprint 1 | **Step 5 — Final Quality Gate RE-RUN (CTO execution command 2026-07-31)** on the post-remediation branch (CAT program complete 1a–8; PREX-TSC-001/002 eliminated) | ✅ ALL ATTAINABLE CRITERIA GREEN — every leg re-measured live this turn: (1) ESLint ✅ `pnpm lint` (turbo run lint, the official CI Job-1 command) — 6/6 workspaces, zero warnings/errors (10.9 s); (2) TypeScript ✅ `tsc` per-package — api **0** / db **0** / types **0** (the former sole blockers PREX-TSC-001 140 and PREX-TSC-002 16 — eliminated via §19 rows 12–22; baseline byte-compared identical to the verified CAT-8 zero-error list `cat-work/after-cat8.txt`); (3)+(4) Suites ✅ — repo filter script `pnpm --filter @zayjar/api test`: **277 pass / 2 skip / 0 fail / 279, 31 of 32 suites** (byte-parity with the standing baseline; integration suites order.checkout / kds.gateway / media included) AND the actual CI Job-2 command root `pnpm test` (`jest --runInBand --forceExit`): **496 pass / 2 skip / 0 fail / 498, 58 of 59 suites** — zero failing specs in either collection; (5) Build ✅ with one documented environment-only caveat — official default-concurrency `pnpm build` OOM-killed (exit 137) with 3 parallel Next builds on this 1.9 GiB / 2-core sandbox (identical contention class as row 11 — NOT a code defect); per-package isolation: types ✅ / db ✅ (tsc + copy-generated-client) / api ✅ (tsc, dist emitted, ~11 s = 0 errors) / qr-menu ✅ (`/` shown as ƒ Dynamic) / backoffice ✅ (6/6 static pages) / cashier ✅ (4/4 static pages); full chain via official tooling serialized `turbo run build --concurrency=1`: **6/6 successful (63 s; 2 cached)** — the complete production build chain is GREEN end-to-end; (6) Docker ⚠ CANNOT VERIFY — `docker: command not found` (no daemon in sandbox; environment-only; CI Job 4 authoritative); statics unchanged vs upstream `df51f37` — 4/4 Dockerfiles byte-identical, api builder runs `pnpm --filter @zayjar/db run prisma:generate` (:35) before `pnpm run build` (:38), runner copies built node_modules + `apps/api/dist` + `packages/db` (:53/:56); (7) Runtime sanity ✅ on fresh DB `zayjar_step5` (environment provisioning only — postgresql-17 + redis-server installed in-sandbox via apt to reproduce the §19 row 4 runtime-of-record PostgreSQL 17.10 + Redis 8.x; zero repo delta): migration chain **5/5 green** (20250101000000_init → add_media_model → add_tenant_branding_jsonb → add_search_indexes [R3 site] → add_lifecycle_triggers [R4 site]; M4 orders_partitioning parked-skipped per R2 closure; init file's upstream-shipped UTF-8 BOM streamed through a strip filter — psql-only nuance, Prisma engines unaffected, file untouched); canonical seed **16/16 entity counts exact** (plans 3 / tenants 2 / subscriptions 2 / users 5 / roles 4 / permissions 14 / restaurants 2 / branches 3 / tables 14 / categories 7 / products 14 / productSizes 12 / productVariants 6 / customers 3 / orders 3 / orderItems 6); `dist/main.js` boot clean — sole domain warn = the known MediaCleanup fail-safe; probes: `/health` **200** with tenant context (`Host: albaik.localhost:8000`) / **403** apex-anonymous; guest table resolve **200**; guest menu **200**; onboarding matrix **201 / 409 dup-subdomain / 409 dup-email / 403 apex** with verbatim message parity (`The requested subdomain is already registered.` / `An account with this email address already exists.` / `Access denied: Missing valid tenant context.`) + full provisioning SQL-counted (tenant 1 / subscription TRIALING 1 / owner 1 / RESTAURANT_OWNER link 1 / restaurant 1 / branch 1 — identical to row 10); (8) PROJECT_STATE consistency ✅ — this row + header/§5/§6/§17/§20/§21/§22/§23 synced to measured reality, mirror sha-verified post-commit. CTO-added verification dimensions: **CI ✅ static** — Job 1 (`pnpm install --frozen-lockfile` + `pnpm lint`) and Job 2 (`pnpm test`) reproduced locally green; Job 3 (`prisma:generate` + `prisma validate` + `pnpm build`) — build chain locally green, prisma:generate engine-blocked in-sandbox only (ENV-TSK-0006; available on GH runners); Job 4 (docker api + qr-menu, push: false) — environment-blocked as above. **Production packaging ✅ static** — 4 Dockerfiles unchanged, 3 compose files (dev/staging/production) present, api `dist/` + 3 Next production outputs emitted this turn. **Deployment readiness ✅ static** — k8s/ tree 26/26 manifests YAML-parse clean (api deployment/hpa/pdb/service, 3 frontends, worker, pgbouncer, postgres, redis, ingress, configmap/secrets, namespace, kustomization); documented SPEC-DRIFT unchanged (socket.io Redis adapter absent vs HPA 2–10 pods — minor, pre-existing). **Zero source files changed in-gate; no CAT-1→CAT-8 work modified; no new defects found.** Sandbox wipe cycle 14 survived pre-gate: all 11 CAT commits re-created stat-identically (§6 pins — anchors +27/−11 … +30/−21 all reproduced) and the baseline api tsc byte-identical to the verified zero-error CAT-8 list before the gate began. Iteration honesty: one mid-leg seed-up mishap recorded verbatim — the first migration application was attempted without the BOM-strip and partially applied `add_media_model` (orphan `MediaType` enum); the database was dropped/recreated fresh and the full chain re-applied cleanly (migrations never run against a dirty database in this gate). **Gate scoreboard: legs (1)–(5)(7)(8) GREEN, leg (6) environment-only CANNOT VERIFY; Sprint 1 closure decision rests with the CTO per the standing closure-lock — evidence supports eligibility** | — (docs commit carrying this record; local, pre-push per §6 lineage rule) | Step-5 re-run mandate — executed 2026-07-31 |
| 24 | 2026-07-31 | Post-Sprint-1 | **Security patch C-1 / AUTHZ-001 (CTO execution command)** — Engineering Audit v1 Critical finding: authenticated cross-tenant isolation bypass via caller-supplied `X-Tenant-ID` | ✅ FIXED+VERIFIED with live A/B runtime proof: (a) root cause verified in code — middleware `X-Tenant-ID` acceptance at `tenant-context.middleware.ts:38–41`, no reconciliation in `JwtStrategy.validate`, ALS as sole scope source (`BaseTenantRepository.getTenantId`); (b) minimal identity-layer fix — inside `validate` after signature verification + blacklist check: reject mismatched `X-Tenant-ID` (403, verbatim `Tenant context mismatch: X-Tenant-ID does not match the authenticated tenant.`), reject mismatched middleware-resolved context (403, verbatim `Tenant context mismatch: resolved tenant does not match the authenticated tenant.`), otherwise reconcile the ALS store to the verified claim; PLATFORM_OWNER exempt (documented capability preserved); array-header normalization; ZERO changes to middleware/fail-safe/RBAC surfaces (C-2 untouched per CTO sequencing); (c) tests — new `auth/strategies/jwt.strategy.spec.ts` (+8: match, header-mismatch 403, host-mismatch 403, matching-header, array header, platform-owner exemption, tenant-less token, blacklist regression); (d) **live A/B on seeded `zayjar_c1`** (PG 17.10 + Redis 8, dist emit): P1 control 200; P2 `X-Tenant-ID` attack — **pre-fix HTTP 200 (leak live) / post-fix HTTP 403** with the exact new message; P3 foreign-host attack 403; P4 matching header 200; P5 anonymous public table resolve 200 (public path regression-clean); P6 repo-surface attack on `GET /api/v1/orders` 403 with the C-1 message thrown BEFORE RBAC (clean-token control 403s with the distinct RBAC privilege message — channels distinguishable); (e) regression gates — tsc api **0** / db **0** maintained post-change, eslint 0 on both files, filter script **277/2/0** (pre-existing glob-depth semantics: depth-3 spec not collected — documented in §13), root `pnpm test` **504/2/0, 59/60 suites (= 496 + 8 new)**. **Iteration honesty:** (i) the first live run probed a STALE pre-fix process — last turn's Step-5 API child (pid 9701) survived the sandbox cycle and held :8000; the new boot failed `EADDRINUSE`; the stale 200/200 responses are recorded as the accidental pre-fix A/B control, then the stale process was killed and the post-fix matrix re-run green; (ii) suite-count behavior of the two collections verified and reported (277 vs 504), not hidden. Cycle-15 sandbox wipe survived pre-fix: all 12 layers re-created stat-identically (anchors +27/−11 … +20/−18 reproduced; baseline api tsc byte-identical to the verified zero-error list). C-2 (AUTHZ-002 — unverified decode-based platform-owner flag) remains OPEN by design, gated on CTO approval | — (single commit carrying fix + spec + this record; local, pre-push per §6 lineage rule) | Audit C-1 mandate — executed 2026-07-31 |
| 25 | 2026-07-31 | Post-Sprint-1 | **Security patch C-2 / AUTHZ-002 (CTO execution command)** — eliminate every authorization decision based on unverified `jwt.decode` claims | ✅ FIXED+VERIFIED: (a) enumeration — exactly two production decode sites found (middleware platform-owner flag; `auth.service.decodeToken` used by the refresh handler); (b) middleware fix — `jwt.decode` → `jwt.verify(token, JWT_CONFIG.accessTokenSecret)`: platform-owner context derived ONLY post-signature-verification; forged/malformed/expired yield NO context; comment-only reference to the old call retained; layering follows the existing kds.gateway JWT_CONFIG-import precedent — no DI/middleware-order changes; (c) refresh-flow fix — `rotateRefreshToken` returns `sub` sourced from its own `verifyAsync` payload (verified claims), `auth.controller` consumes it, `decodeToken` helper deleted; sole caller verified by grep before removal; (d) **zero unverified decode sites in production code** (final grep over `apps/api/src` + `packages/db/src`); (e) unit tests — middleware spec +6 C-2 cases; iteration honesty: first spec run failed 5/6 because apex custom-domain resolution in a DB-less unit run errors inside the cache fetch and surfaces as NotFoundException — spec harness now simulates the database's live answer (resolve → null) instead of touching prisma; production code untouched by that correction; (f) regression — suites root **510 pass / 2 skip / 0 fail, 59/60 suites** (= 504 + 6), filter script **277/2/0** (glob-depth semantics preserved), tsc api **0** / db **0** maintained post-change, eslint **6/6 workspaces 0 errors**; (g) **live runtime A/B** (PG 17.10 + Redis 8 + seeded `zayjar_c1`, post-fix dist emit verified to contain the patch): probe A forged PLATFORM_OWNER Bearer at apex `GET /health` → **403** (the audit-established pre-fix path decoded any claims → bypass → 200); probe B valid owner apex → **200** (behavior preserved verbatim); probe C expired owner → **403**; probe D valid staff apex → **403** (unchanged); probes E1/E2 C-1 preservation — X-Tenant-ID attack **403** with the C-1 message / clean staff **200**; probe F layered defense — forged bearer on protected `/auth/me` → **401** from the strategy; probe G anonymous public table → **200** (public path regression-clean). Cycle-16 sandbox wipe survived pre-fix: 13 layers re-created stat-identically (anchors +27/−11 … +138/−3 reproduced incl. `cat-recon-backup/c15/`); baseline api tsc byte-identical to the verified zero-error list; iteration honesty: the first reconstruction attempt committed the C-1 spec into the CAT-1a layer because `git checkout -- .` does not remove untracked files — detected by anchor mismatch (124/11 f:3 vs 27/11 f:2), layer reset (`reset --hard`), stray file cleaned, 13 layers re-run green. **C-1 and C-2 now both closed; H-1/H-2/H-3 untouched per CTO instruction** | — (single commit carrying fix + specs + this record; local, pre-push per §6 lineage rule) | Audit C-2 mandate — executed 2026-07-31 |
| 26 | 2026-07-31 | Post-Sprint-1 | **Deployment fix H-1 / DEPLOY-001 (CTO execution command)** — Kubernetes readiness/liveness probe configuration did not match the actual health endpoint | ✅ FIXED+VERIFIED: (a) verification-first enumeration — repo-wide `grep` located FOUR live probe/healthcheck sites of the same defect: `k8s/api/deployment.yml:143,151` (liveness + readiness `path: /api/v1/health`), `docker-compose.production.yml:29`, `docker-compose.staging.yml:26` (the staging site beyond the audit's own enumeration — identical defect class and identical one-line fix, fixed in the same commit and reported openly here and in the header); the actual endpoint is `GET /health` on the root-mounted `HealthController` (`health.controller.ts:5`, `@Controller()` + `@Get('health')`, **no global prefix** in `main.ts` — grep-verified); remaining `/api/v1/health` references classified and intentionally untouched: `.github/workflows/cd.yml:223-224` smoke URL (inside the H-3 CD-placeholder file — CTO sequencing), two logging specs (arbitrary sample URL strings, passing as-is), `packages/db/BACKUP-RECOVERY.md:295` runbook line + `tests/e2e/fixtures/api-mocks.ts:276` mock fixture (out-of-scope docs/fixture drift — flagged for the CTO, no mandate), `k8s/README.md:106` probe-table row updated in-commit as collateral consistency (the fix would otherwise make it stale); (b) fix — four path strings → `/health`; pre-commit `git diff` = exactly 4 files / +5/−5 with ONLY path lines changed (README row = the fifth line); **zero application-code change → runtime behavior preserved by construction**; (c) **Kubernetes manifest verification** — YAML-parsed assertions: liveness + readiness `httpGet.path == /health`, `httpGet.port == http`, all other probe params unchanged (liveness 45/10/5/×5, readiness 10/5/3/×3); compose healthcheck params unchanged (production 10s/5s/×5/45s; staging 15s/10s/×3/30s — staging's distinct timings are by design and were asserted against their own HEAD values); (d) **runtime verification** on the freshly rebuilt stack (cycle-17 also wiped `/tmp` + the apt packages — postgresql-17 + redis reinstalled, fresh cluster, 5/5 migration chain, canonical seed 16/16, post-reconstruction `dist` emit booted): OLD configured target `/api/v1/health` → **404** on resolvable tenant host / **403** apex — the pre-fix probe could never succeed in any environment (kubelet requires 2xx; tenant hosts yield 404 because the route never existed; apex yields 403 from the H-2 gating layer); NEW configured target `/health` → **200** on `Host: albaik.localhost` and `Host: tokyoramen.localhost` (body `{"status":"ok","timestamp":…,"uptime":…}` verbatim) and **200** via `X-Tenant-ID: albaik` — the configured probes now reach the real health controller; no other route touched; apex `403` residual = **H-2 (health-endpoint tenant-gating) — explicitly untouched** per CTO sequencing, as is H-3; ⚠ CANNOT VERIFY: in-cluster kubelet 2xx (requires H-2 plus a cluster; no docker daemon / kubectl in sandbox — compensated with YAML parse assertions + live origin-server evidence above); (e) regression gates — tsc api **0** / db **0** (empty outputs, byte-identical to the verified zero-error baseline `cat-work/after-cat8.txt`), ESLint **6/6 workspaces 0 errors**, suites filter **277 pass / 2 skip / 0 fail (31/32)** + root CI command **510 pass / 2 skip / 0 fail (59/60)** — exact parity with the post-C-2 baseline (expected, zero code touched). Cycle-17 sandbox wipe survived pre-fix: all 14 layers re-created stat-identically (anchors +27/−11 … +112/−30 reproduced; `git clean` purge of the later-layer untracked spec BEFORE layering per the cycle-16 lesson; baseline api tsc byte-identical). Iteration honesty: (i) the first CAT-1a attempt staged only the repository fix (anchor 1f 15/1 ≠ expected 2f 27/11) — halt-on-mismatch fired; the CAT-1a-era PSM was located at its sha-pinned backup `c7/layerA1.psm` (+12/−10 vs `7ebf0dd` re-verified) and all 14 anchors re-ran green; (ii) a self-inflicted `pkill -f` pattern matched its own shell twice (exit −1, empty output) — corrected with the bracket-trick pattern, target process verified dead and :8000 free; (iii) environment rebuild per (d). **H-2 / H-3 remain OPEN per CTO sequencing; nothing else was touched** | — (single commit carrying fix + README sync + this record; local, pre-push per §6 lineage rule) | Audit H-1 mandate — executed 2026-07-31 |
| 27 | 2026-07-31 | Post-Sprint-1 | **Deployment fix H-2 / DEPLOY-002 (CTO execution command)** — make `/health` usable by infrastructure probes without tenant resolution; preserve tenant protection on every other endpoint | ✅ FIXED+VERIFIED: (a) root cause verified in code — the 403 came from `TenantContextMiddleware`'s fail-safe (no resolved tenant + no platform-owner → `ForbiddenException('Access denied: Missing valid tenant context.')`), registered `.forRoutes('*')` in `app.module.ts`; downstream safety of an exempted health request verified BEFORE the fix: `HealthController` returns static JSON (no DB, no ALS), `AuditInterceptor` passes non-mutating GETs straight through (zero store access), the db $extends fail-safe engages only at query time, and the correlation/logging/sanitization middlewares already ran on apex `/health` (they precede the tenant gate); (b) minimal fix — consumer-level path-exact exemption `.exclude('health')` on the `TenantContextMiddleware` segment ONLY; the C-2-hardened middleware file is byte-untouched (security surface not re-opened), no DI/order changes, no business logic, no deployment automation; (c) unit coverage — new `app.module.spec.ts` (+4: the four segments apply in standing order; the tenant segment excludes exactly `['health']` and serves `'*'`; all other segments are exemption-free; the whole consumer's exclusion set is exactly `['health']`) — a declarative wiring guard against regression; (d) **runtime A/B** (post-reconstruction `dist` emit verified to contain the patch): NEW behavior — `GET /health` **200** default Host (apex), **200** kubelet-style `Host: 10.244.1.17:8000` (mandate criterion 5 — Kubernetes probes now receive HTTP 200: live origin evidence for exactly the request kubelet sends; ⚠ CANNOT VERIFY a real cluster — no docker/kubectl in sandbox), **200** HEAD (compose `wget --spider` semantics), **200** `Host: albaik.localhost`, **200** `X-Tenant-ID: albaik`, body `{"status":"ok",…}` verbatim; PRESERVATION byte-exact vs pre-fix baseline — `/` apex **403**, `/healthx` apex **403** (path-exact exemption, zero widening), `/api/v1/health` **403** apex / **404** tenant host (H-1 evidence holds unchanged), anonymous public table resolve **403** apex / **200** tenant host, C-1 attack (staff JWT + mismatched `X-Tenant-ID`) **403** with the verbatim C-1 message on the real protected route `GET /api/v1/auth/me`, forged PLATFORM_OWNER apex **403** on non-health surfaces (C-2 intact — forged confers nothing), clean staff protected **200** (tenant host AND matching-header apex variants); (e) mandated consequence documented honestly — C-2's earlier live probe shape “forged bearer at apex `/health` → 403” now yields **200 by H-2 design**: the tenant middleware no longer runs on `/health` at all — no token evaluated, no ALS, no privilege conferred; the layered protection on protected routes is probe-verified intact; (f) regression gates — tsc api **0** / db **0**, ESLint **6/6 0 errors**, suites filter **277/2/0 (31/32)** unchanged + root **514 pass / 2 skip / 0 fail, 60/61 suites (= 510 + 4 wiring specs)**. Cycle-18 sandbox wipe survived pre-fix: all 15 layers re-created stat-identically (anchors +27/−11 … +10/−6 reproduced; baseline api tsc byte-identical to `cat-work/after-cat8.txt`); `/tmp` cluster + apt stack SURVIVED this cycle (seed 16/16 re-verified live — no rebuild). Iteration honesty: (i) the token-signing recipe first failed `MODULE_NOT_FOUND` at repo root (pnpm layout — `jsonwebtoken` resolves from `apps/api`), rerun from the correct cwd; (ii) the first preservation probes targeted `/auth/me`, which a 404 proved to be a NON-EXISTENT route — re-pointed at the real `GET /api/v1/auth/me` (`auth.controller.ts:14,155`) with the exact matrix above (the stray 404s are documented, not hidden). **H-3 remains OPEN per CTO sequencing; no other file touched** | — (single commit carrying fix + wiring spec + this record; local, pre-push per §6 lineage rule) | Audit H-2 mandate — executed 2026-07-31 |
| 28 | 2026-07-31 | Post-Sprint-1 | **Deployment automation H-3 / DEPLOY-003 (CTO execution command)** — replace the CD deployment placeholder with the real deployment flow; preserve runtime behavior; no business logic; do not touch C-1/C-2/H-1/H-2 | ✅ FIXED+VERIFIED: (a) verification-first read-through of the actual automation surface — the two deploy jobs ended in printed placeholders; `.github/scripts/health-check.sh` curled `/api/v1/health` (route eliminated as non-existent in H-1, made probe-facing in H-2) and its `${1:?}` guard hard-failed an unconfigured URL making its own skip branch DEAD CODE; `.github/scripts/rollback.sh` offered ECS + compose strategies but had NO strategy for the repo's actual k8s topology; staging had no rollback step at all; the production smoke test curled the same stale route; (b) the real flow (repo-anchored, zero invention) — registry/prefix come from the workflow's own env (`ghcr.io`, `owner/zayjar` — exactly the manifests' images, worker reuses the api image); both jobs: `azure/setup-kubectl@v4`, cluster credentials from `KUBECONFIG_STAGING` / `KUBECONFIG_PRODUCTION` (raw YAML or base64 auto-detected; fail-fast with an actionable message when unconfigured — a silent no-op deploy would be a lie), converge manifests SELECTIVELY (`namespace.yml`, `configmap.yml`, `k8s/{api,worker,qr-menu,backoffice,cashier}/` — `secrets.yml` placeholders and the stateful data tier deliberately not re-applied, README-documented external-secret ownership respected), pin all five workloads to the immutable build tag (`kubectl set image … :${DEPLOY_TAG}`), `rollout status` ×5 with timeouts (probes now health-gated on the real `/health` — H-1/H-2 chain), rollback-on-failure added to staging (production already had the step hook); `rollback.sh` gained the primary `rollback_k8s()` strategy (rollout undo for api/worker/qr-menu/backoffice/cashier + api status wait) with kubectl/kubeconfig detection and ECS/compose fallbacks BYTE-PRESERVED; `health-check.sh` endpoint → `/health` + `${1:?}` → `${1:-}` (skip branch revived); smoke test → `/health`; (c) **runtime verification** of the deploy gate scripts against the live API (post-reconstruction dist emit): health-check PASS **200 attempt 1** on the running server, SKIP exit 0 on an unconfigured URL, FAIL **exit 1** after 10 retries against a dead port; rollback executed against a stub kubectl — argv recorded EXACT (`-n zayjar rollout undo deployment/{api,worker,qr-menu,backoffice,cashier}` in order + `rollout status deployment/api --timeout=300s`), no-strategy environment still exits 1 with k8s-first manual steps (ECS/compose selection logic preserved); (d) **static verification of the deployment workflow** — cd.yml YAML-parsed; all 15 embedded `run:` blocks pass `bash -n` (GitHub expressions neutralized); zero placeholder strings remain; both jobs contain kubectl setup → credentials → selective apply → pin ×5 → rollout ×5 → health check → rollback → notify in order; no blind `kubectl apply -k` (secrets/data-tier protection asserted in-code); (e) **CI workflow verification** — ci.yml byte-untouched (post-commit diff empty); its legs reproduced live this turn: Job 1 lint **6/6**, Job 2 tests **277/2/0 + root 514/2/0**, Job 3 build chain **6/6 serialized (88 s)**, Job 4 docker ⚠ CANNOT VERIFY runtime (no daemon — environment-only; statics next); (f) **Docker deployment flow verification** — static cross-mapping asserted: matrix apps ↔ 4 Dockerfiles on disk ↔ manifest images (`ghcr.io/amarspk/zayjar/*`, worker=api image) ↔ CD pin targets; build-and-push tags (sha/latest/release-tag) cover the exact tags deploy resolves; 4 Dockerfiles byte-unchanged from the H-1-verified state; ⚠ daemon absent (environment-only); (g) **Kubernetes deployment verification** — kubectl v1.30.0 provisioned in-sandbox (zero repo delta): `kubectl kustomize k8s/` renders **22 manifests** with probe path `/health` ×2 and ZERO stale `/api/v1/health`; PyYAML structure pass 14/14 on the CD-applied set (kind/name/apiVersion/namespace=zayjar); the exact pin commands executed OFFLINE via `kubectl set image --local` — all five workloads transformed to the pinned tag in emitted YAML; ⚠ CANNOT VERIFY live-cluster semantics — `kubectl apply` performs server discovery even with client dry-run (verified verbatim: connection refused to localhost:8080) and no cluster exists in the sandbox; compensated with the render+offline-pin+stub-verified argv evidence above; (h) regression gates — tsc api **0** / db **0**, ESLint **6/6 0 errors**, suites filter **277/2/0 (31/32)** + root **514/2/0 (60/61)** — exact parity (zero application-code files in the commit). Cycle-19 sandbox wipe survived pre-fix: all 16 layers re-created stat-identically (anchors +27/−11 … +92/−1 reproduced). Iteration honesty: (i) one pkill self-match recurrence (compound command containing the boot string — split into two commands, documented); (ii) script exec bits are 644 after reconstruction by design (the workflow chmod+x's them — invoked via `bash` locally); (iii) the `kubectl apply` discovery requirement and the `--local` selector syntax nuance (file form takes bare `CONTAINER=IMAGE`) were discovered empirically and are recorded instead of hidden; (iv) the turbo package-manager-detection failure on npx-invoked pnpm was worked around with `pnpm exec` (environment nuance, zero repo delta); (v) pre-existing cosmetic artifact noticed: the health-check log can print `HTTP 000000` on connection refusal (curl's own `000` concatenated with the `\|\| echo 000` fallback — pre-existing pattern, detection logic unaffected, NOT fixed as unrelated refactoring). **C-1/C-2/H-1/H-2 surfaces byte-untouched; Sprint 2 not started; nothing else modified** | — (single commit carrying the workflow + scripts + this record; local, pre-push per §6 lineage rule) | Audit H-3 mandate — executed 2026-07-31 |



| 29 | 2026-07-31 | Sprint 2, Task 1 | Staff Authentication UI (Auth-UI gap, §15 item 6) — standalone login flow for both staff apps, wired to the existing auth surface | ✅ VERIFIED (17 files, +1,065/−44; zero backend files): **(a) static gates** — cashier suites 28/28 (3 suites; 20 new tests: `auth.spec.tsx` 12 + `LoginForm.spec.tsx` 8), backoffice suites 42/42 (5 suites; +20 new, same specs), api filter **277 pass / 2 skip / 0 fail (31/32)** and root **514 pass / 2 skip / 0 fail (60/61)** — exact parity with the §19 row 23 baselines (new specs are `.spec.tsx`, outside the root-node `**/*.spec.ts` match); tsc api 0 / db 0 / types 0; ESLint 6/6 zero errors; build chain **6/6 serialized** (`turbo run build --concurrency=1`; cashier `/login` + backoffice `/login` routes emitted); **(b) live runtime matrix** on native PostgreSQL 17.10 + Redis 8.0.2 + API :8000, fresh DB `zayjar_rt3` (migrations M1→M2→M3→M5→M6 clean, 30 tables; canonical seed 16/16) — onboarding (R7 path) 201 → login on the new tenant's host **200** with the exact `{accessToken, csrfToken, expiresIn, user{id,tenantId,email,roles,firstName,lastName,mfaEnabled}}` shape the client parses; wrong password **401** `Invalid credentials`; unknown tenant host **403** middleware; `Set-Cookie: __Host-CSRF-Token` (JS-readable) + `__Host-Refresh-Token` (HttpOnly) emitted; CSRF echo (`X-CSRF-Token` header) accepted on authenticated `POST /auth/mfa/enable` and `/auth/mfa/verify` (200, real TOTP via speakeasy → `mfaEnabled: true`); post-MFA login without code **401** verbatim `"MFA token required"` (the exact string the form detects) → with code **200**; logout **200** `{success:true}` → same token now **401** (blacklisted); **(c) new pre-existing defect discovered — RT-ONB-002**: onboarded owner's per-tenant role has zero `role_permissions` → `GET /api/v1/menu/products` and `GET /api/v1/branches` **403** (`[read on Product]` / `[read on Branch]`) even for the owner — backend provisioning gap, out of this task's scope, recorded §7/§15/§20/§23 (item 11) | — (single commit carrying the implementation + this record; local, pre-push per §6 lineage rule) | Sprint 2 Task 1 — implemented; commit + push await CTO approval |

| 30 | 2026-07-31 | Sprint 2, Task 2 | RT-ONB-002 fix — onboarding RBAC provisioning (canonical RESTAURANT_OWNER permission set for newly onboarded owners) | ✅ VERIFIED (4 files, +150/−10): **(a) static gates** — api filter **279 pass / 2 skip / 0 fail (31/32)** (was 277/2/0; +2 new tenant fail-fast tests), root **516 pass / 2 skip / 0 fail (60/61)** (was 514/2/0), tsc api 0 / db 0 / types 0, ESLint 6/6 (api + db seed), build chain **6/6 serialized**; **(b) post-fix runtime** — seed → 22 permission rows, owner role **22 links**, manager 10 / cashier 6 / kitchen 4 unchanged (backward compatibility); onboard 201 → SQL set-parity **seeded ≡ onboarded (22=22, identical both directions)**; as the onboarded owner: branches 200, menu/products 200, category 201, product 201, checkout 201 (ORD-2026-28059 PENDING), tables 200, table create 201 (64-char QR), branch create 201 (growth plan), tenant update 200 (growth plan); **(c) fail-fast runtime proof** — cloned DB with roles deleted: onboard **500**, API log `Cannot provision owner permissions: no RESTAURANT_OWNER role exists in the database. Run the canonical seed to provision the baseline roles.`; **(d) design notes** — canonical-set cloning keeps the seed as single source of truth; `NOT: { id: ownerRole.id }` excludes the just-created role from the lookup; the 8 guard-vocabulary rows were added (legacy rows retained, §14 decisions 19–22); guard/factory/controllers untouched | — (single commit carrying the implementation + this record; local, pre-push per §6 lineage rule) | Sprint 2 Task 2 — implemented; commit + push await CTO approval |

| 31 | 2026-07-31 | Sprint 2, Task 3 | Order.paymentMethod persistence (§15 item 3) — add the column + persist the DTO value on the shared create path | ✅ VERIFIED (13 files): **(a) static gates** — tsc api 0 / db 0 / types 0 (single knock-on: `rbac-permission.guard.spec.ts` Order mock gained `paymentMethod: null` — the regenerated client types the nullable column as a required key), ESLint 6/6, order suites 60/60 (order.service + checkout integration + kds gateway), api filter **279/2/0 (31/32)** + root **516/2/0 (60/61)** unchanged (assertions strengthened inside existing tests, no count change — no regression), build chain 6/6 serialized; **(b) live runtime** (fresh DB `zayjar_rt5`, full migration chain M1→M2→M3→M5→M6→M7 `add_order_payment_method` clean, 30 tables + new column) — staff checkout **ORD-2026-79953** response `paymentMethod: CASH` + DB `paymentMethod=CASH`; guest checkout (public QR path, real seed token) **ORD-2026-46938** → `CASH` persisted + echoed; pre-existing seeded order stays `NULL` (no fabricated backfill — behavior preserved); **(c) client regeneration** — required for the schema change (api tsc fails otherwise); the committed generated client was stale, so regeneration also materialized `branding` (LATENT-BRAND-001) + the R3 KDS/search index rows — the client now matches `schema.prisma` exactly (documented §7/§20/§21; runtime branding re-verification is a follow-up, not part of this task) | — (single commit carrying the implementation + this record; local, pre-push per §6 lineage rule) | Sprint 2 Task 3 — implemented; commit + push await CTO approval |

| 32 | 2026-07-31 | Sprint 2, Task 4 | Discount Engine — replace the `discountAmount = 0.00` placeholder with a real tenant-scoped discount engine | ✅ VERIFIED (13 files): **(a) static gates** — tsc api 0 / db 0 / types 0; ESLint 6/6; discount service suite 12/12, order suites 51/51, guards suite green; api filter **296 pass / 2 skip / 0 fail (32/33)** (was 279/2/0; +17 new tests: 12 discount-service + 3 order-service + 2 checkout-integration); root **533 pass / 2 skip / 0 fail (61/62)**; build chain 6/6 serialized; **(b) design** — pure `DiscountService` (no DI; static `computeDiscountAmount` percentage/fixed capped at subtotal with 2dp rounding, `validateDiscount` with one uniform `BadRequestException` for every invalid path — anti-oracle); applied **inside** the checkout transaction (lookup `tx.discount.findUnique` + usage `increment` + order snapshot `discountId/discountCode` atomic — no race); code normalized (trim+upper); `Order.discountId` FK SetNull (deleting a discount never cascades history); **(c) live runtime** (fresh `zayjar_rt6`, migrations M1…M8 clean, 31 tables, seed incl. `SAVE10` 10% + `FIXED5` $5 with usageLimit) — no code → 0 / 27.60; `SAVE10` → 2.40 persisted + usage 1; `FIXED5` → 5.00 persisted + usage 1; invalid `NOPE` → 400 `The discount code is invalid or expired.` no order no usage; cross-tenant (albaik code on fresh onboarded tenant) → 400 usage unchanged | — (single commit carrying the implementation + this record; local, pre-push per §6 lineage rule) | Sprint 2 Task 4 — implemented; commit + push await CTO approval |

| 35 | 2026-07-31 | Sprint 2, Task 7 | Firebase Cloud Messaging — real provider replacing the log-only FCM stub + notifications table relation/repository | ✅ VERIFIED (16 files + dep): **(a) static gates** — tsc api 0 / db 0 / types 0; ESLint 6/6; fcm.service 4/4, device-token 10/10 (incl. real-path tests: multicast send + success count, unregistered token pruning, fallback preserved); api filter **311 pass / 2 skip / 0 fail (35/36)** (was 304/2/0; +7), root **548 pass / 2 skip / 0 fail (64/65)** (was 541/2/0); build chain 6/6 serialized; **(b) design** — `FcmService` lazy-init from `FIREBASE_SERVICE_ACCOUNT` (JSON) or `FIREBASE_SERVICE_ACCOUNT_PATH` (file); `isAvailable()` gate; `sendPush` uses `messaging.sendEachForMulticast` (data coerced to strings) and returns success/failure counts + `messaging/registration-token-not-registered` tokens; never throws (send errors → full-failure result). `DeviceTokenService.sendPushNotification`: real path → send, prune unregistered tokens (tenant-scoped delete), persist in-app `Notification` mirror via the new repository; fallback path (unconfigured) → previous log-only behavior byte-identical. `notifications` gained `tenant` FK relation + `Tenant.notifications` + `idx_notifications_tenant` (migration M9). **(c) runtime** (fresh `zayjar_rt8`, 9-migration chain clean): API boots with new FcmService wiring; live device-token registration 201 + DB row; notification repository create/read against real PG (tenant-scoped); live push fallback `{sent:1, payloads:1}` + FCM-disabled lazy-init warning. Real FCM send covered by unit tests (mocked SDK; no real Firebase credentials available in-sandbox — documented limitation, CI/staging with real creds is the environment of record) | — (single commit carrying the implementation + this record; local, pre-push per §6 lineage rule) | Sprint 2 Task 7 — implemented; commit + push await CTO approval |
| 34 | 2026-07-31 | Sprint 2, Task 6 | Socket.io Redis Adapter — cross-pod KDS realtime via Redis pub/sub | ✅ VERIFIED (4 files + dep): **(a) static gates** — tsc api 0 / db 0 / types 0; ESLint 6/6; kds suites 45/45 (incl. new `kds.gateway.redis.spec.ts` 4 tests: no-URL → in-memory preserved, REDIS_URL → adapter attached, unreachable → graceful fallback, module destroy → clients quit); api filter **304 pass / 2 skip / 0 fail (34/35)** (was 300/2/0; +4), root **541 pass / 2 skip / 0 fail (63/64)** (was 537/2/0); build chain 6/6 serialized; **(b) design** — `KdsGateway.afterInit` reads `SOCKET_IO_REDIS_URL`→`REDIS_URL` (TLS via `REDIS_TLS`), creates `redis` pub+sub clients, attaches `createAdapter` (SPEC_INDEX §7.6); any failure logs a warning and keeps the in-memory adapter (single-pod/dev behavior byte-identical — no crash, no behavior change); `OnModuleDestroy` quits both clients; **(c) runtime cross-pod proof** (Redis 8.0.2) — two independent Socket.io servers sharing Redis: client on pod B in room `tenant:t1:branch:b1` received pod A's `ticket.created` broadcast (payload `{"ticketId":"t-1","pod":"A"}`) — impossible with the in-memory adapter, the exact multi-pod HPA scenario | — (single commit carrying the implementation + this record; local, pre-push per §6 lineage rule) | Sprint 2 Task 6 — implemented; commit + push await CTO approval |
| 33 | 2026-07-31 | Sprint 2, Task 5 | Real Invoice PDF Generation — replace the fabricated `pdfUrl` with real PDF generation + storage | ✅ VERIFIED (8 files + deps): **(a) static gates** — tsc api 0 / db 0 / types 0; ESLint 6/6; invoice pdf suite 3/3, order.service suite 27/27; api filter **300 pass / 2 skip / 0 fail (33/34)** (was 296/2/0; +4 new tests), root **537 pass / 2 skip / 0 fail (62/63)**; build chain 6/6 serialized; **(b) design** — `InvoicePdfService` (pure, pdfkit, A4, real display data), `InvoiceStorageService` (reuses media `StorageProvider`: local `/uploads/...` or S3 by `STORAGE_PROVIDER`, deterministic tenant-scoped key), `InvoiceModule` wired into OrderModule; `generateInvoice` resolves real customer/branch/restaurant (prisma) and persists the real stored URL; static `/uploads/*` route added in main.ts (NestExpressApplication) so local URLs are genuinely servable; fallback to the legacy CDN string ONLY if the PDF services are unwired; **(c) live runtime** (fresh `zayjar_rt7`, STORAGE_PROVIDER=local) — full order lifecycle to COMPLETED → invoice `INV-2026-767883`, `pdfUrl=/uploads/invoices/<tenant>/INV-2026-767883.pdf` persisted; file on disk (1926 bytes); HTTP GET → **200 application/pdf, %PDF-1.3 magic**; decompressed text verified to contain the real company name (Invoice Kitchen), branch (Main Branch), order number, and totals — a genuine, downloadable invoice | — (single commit carrying the implementation + this record; local, pre-push per §6 lineage rule) | Sprint 2 Task 5 — implemented; commit + push await CTO approval |

Anything not listed in this table is not verified.

**Repository Corrections applied in the 2026-07-30 documentation pass** (document-vs-repository contradictions fixed in place; repository wins):
1. §9 migration applied-state was "UNKNOWN (no runtime)" → M1/M2/M3/M6 verified clean-applying (two independent live DBs); M4/M5 verified FAILING — recorded.
2. R2 (`orders_partitioning_by_year`) and R3 (`add_search_indexes`) were absent from the defect inventory → added to §7 and §20 (both CONFIRMED red 2026-07-30).
3. LATENT-KDS-001 was "CANNOT CONFIRM" → CONFIRMED at runtime (HTTP 500 + verbatim API log).
4. LATENT-MEDIA-001 carried a runtime-ReferenceError claim → DISPROVEN (bare emitted declaration); downgraded to compile-time debt.
5. LATENT-ENV-001 was "runtime impact CANNOT CONFIRM" → CONFIRMED (default-env SSR error view; `API_INTERNAL_URL` override green).
6. §6/§16 said push "blocked: no remote/credentials" → superseded by the 2026-07-30 user-owned push policy.
7. §15/§17 carried Step-4-pending content → Step 4 marked complete; Step 5 gated on approval; Sprint-2 scope marked CANNOT CONFIRM (roadmap contains no "Sprint 2").
8. §18 open-question "push credentials" → resolved by the same push policy (correction 6).

**Repository Corrections applied in the 2026-08-03 pass** (same pattern — repository reality wins, §26 rule 7):
9. §19 row 23 stated the init migration's UTF-8 BOM was a "psql-only nuance, **Prisma engines unaffected**" → **DISPROVEN 2026-08-03**: `prisma migrate deploy` hard-fails on it at migration 1 (`P3018` / `42601 syntax error at or near "\ufeff"`). The original claim was true only of the psql-with-strip-filter workaround; the engine/CLI path had never been exercised. BOM removed at the source (§19 row 36).
10. ENV-TSK-0006 / `TSK-0006_HEALTH_CHECK.md` listed `sharp` among the packages blocked by build approval → **over-attributed**: `sharp` loads correctly without its install script via the prebuilt `@img/sharp-linux-x64` package (verified live). It is deliberately left unapproved in `allowBuilds`; the real blockers were the Prisma engine trio and `argon2` (§19 row 36).
11. §22 / §18 documented "argon2 node-gyp rebuild after every install" and "Prisma CLI engines absent" as standing workarounds → **obsolete since 2026-08-03**: both are handled automatically by `allowBuilds`; a clean-slate install needs zero manual steps.

---

# 20. Open Blocking Defects

All remaining **verified** open defects — none invented; IDs match §7. "Blocking Which Step" names the earliest affected gate.

| ID | Description (verified) | Current Status | Priority | Blocking Which Step |
|---|---|---|---|---|
| PREX-TSC-001 | 140 api tsc errors; `tsc` is the api build script | **FIXED+VERIFIED 2026-07-31 (CAT program complete 1a–8, §19 rows 12–22)** — api tsc 140 → 0; Sprint-1 Step-5 RE-EXECUTED 2026-07-31 — all attainable criteria GREEN (§19 row 23) | resolved | — |
| PREX-TSC-002 | 16 db tsc errors (single `BaseTenantRepository` delegate-interface root — 1 unique defect) | **FIXED+VERIFIED 2026-07-30 (CAT-1a)** — db tsc 16→0, db build leg GREEN (§19 row 12) | resolved | — |
| PREX-E2E-001 | `src/common/e2e.spec.ts` onboarding test failed (Fail-Safe 'User') | **CLOSED 2026-07-30 — canary of R7/RT-ONB-001** (§19 row 10; suite green) | resolved | — |
| RT-ONB-001 (R7) | public onboarding ran context-less under the tenant fail-safe (live 500 on merchant self-signup; 403 at apex) | **FIXED+VERIFIED 2026-07-30** (§19 row 10 — live A/B 500→201, suites 277/2/0, tsc/lint unchanged) | resolved | — |
| PREX-MIG-002 (R2) | orders-partitioning migration fails on apply — layer-1 broken `created_at` partition key; layer-2 proven PK/partition-key architectural incompatibility | **CLOSED 2026-07-30 — investigation complete** (M4 parked non-shippable; R2.1 ranked: F indexes-only recommended, D `HASH("id")` fallback; revival requires architecture RFC) | High — decision recorded | none while parked |
| PREX-MIG-003 (R3) | search-index migration failed at line 14 (snake_case quoted identifiers) + mirror line 17 | **FIXED+VERIFIED 2026-07-30** (§19 row 8 — chain green, indexes live, FTS proven) | resolved | — |
| ENV-TSK-0006 | install scripts blocked (prisma engines, argon2); CLI migrate path unusable — plus an init-migration UTF-8 BOM (`P3018`/`42601`) and a `DATABASE_URL`-less CI validate step (`P1012`) | **FIXED+VERIFIED 2026-08-03 (Sprint 2 Task 8, §19 row 36)** — least-privilege `allowBuilds` + BOM strip + CI env fix; clean-slate install requires zero manual workarounds; `prisma migrate deploy` 8/8 on fresh PG 17.10; canonical seed 17/17; live argon2 login 200 / wrong-password 401; guest checkout 201 | resolved | — |
| LATENT-SUB-001 | unguarded `plan` dereference `subscription.service.ts:114–116` (+ second site `:102`) | OPEN — **runtime DISPROVEN 2026-07-30** (graceful 404 `No subscription found for tenant`, `subscription.service.ts:22`; plan-null DB-impossible); compile-time caution folded into PREX-TSC-001 | Low | none currently |
| LATENT-MEDIA-001 | TS2552 `urls` at `media.service.ts:112` | **FIXED 2026-07-31 (CAT-8, §19 row 22)** — annotation re-declared to the truthful URL-bundle type | resolved | — |
| LATENT-BRAND-001 | dynamic-branding JSONB runtime-degraded by a stale generated client (zero `branding` refs in the client snapshot/DMMF → column never SELECTed; existing `|| {}` fallbacks mask it) | **RESOLVED+VERIFIED 2026-07-31** — client regenerated (Task 3) + runtime re-verification passed live: JSONB `branding` now returned/merged on `GET /api/v1/tenants/:id` (§7 row) | Low | dynamic-branding feature correctness (DOC-001 §1.5) |
| AUTHZ-001 (Audit C-1) | authenticated cross-tenant isolation bypass via caller-supplied `X-Tenant-ID` (middleware accepted unvalidated tenant header; no JWT-claim reconciliation) | **FIXED+VERIFIED 2026-07-31** (§19 row 24 — identity-layer reconciliation, 403 on both mismatch vectors, live A/B, suites green) | resolved | — |
| AUTHZ-002 (Audit C-2) | platform-owner context derived from unverified `jwt.decode` claims in TenantContextMiddleware (fail-safe bypass via forged tokens); secondary unverified decode in the refresh flow | **FIXED+VERIFIED 2026-07-31** (§19 row 25 — signature-verified derivation, `decodeToken` deleted, zero decode sites; live A/B 403/200/403/403, C-1 preserved, layered 401) | resolved | — |
| DEPLOY-001 (Audit H-1) | k8s liveness/readiness probe paths + production/staging compose healthchecks targeted `/api/v1/health` — a route that does not exist (actual endpoint `GET /health`, root-mounted, no global prefix; probes could never succeed: 404 tenant-host / 403 apex runtime-verified) | **FIXED+VERIFIED 2026-07-31** (§19 row 26 — 4 paths corrected, all other probe params YAML-asserted unchanged, `k8s/README.md` probe row synced; live A/B: new `/health` 200 on both seeded tenants + `X-Tenant-ID`, apex 403 = H-2 residual untouched per CTO sequencing; zero application-code change; suites 277/2/0 + root 510/2/0; tsc 0/0; eslint 6/6) | resolved | — |
| DEPLOY-002 (Audit H-2) | `/health` was tenant-gated by the middleware fail-safe — infrastructure probes (pod-IP/apex Host) could never receive HTTP 200 despite the H-1 path fix | **FIXED+VERIFIED 2026-07-31** (§19 row 27 — path-exact consumer exemption `.exclude('health')`; C-2-hardened middleware byte-untouched; kubelet-style / apex / HEAD / tenant live 200; preservation matrix byte-exact incl. C-1/C-2 surfaces and public-surface gating; +4 wiring specs; root 514/2/0 (60/61); filter 277/2/0; tsc 0/0; eslint 6/6) | resolved | — |
| DEPLOY-003 (Audit H-3) | CD deploy jobs printed a placeholder instead of deploying; post-deploy health gate + smoke test curled the non-existent `/api/v1/health`; `rollback.sh` lacked a Kubernetes strategy; health-check `${1:?}` guard deadlocked its skip path | **FIXED+VERIFIED 2026-07-31** (§19 row 28 — real k8s deploy in both jobs: kubeconfig secrets raw-or-base64 + fail-fast, selective manifest apply (secrets/data-tier excluded), `set image` pin ×5, rollout status ×5, rollback both jobs; rollback.sh primary k8s strategy stub-verified argv-exact; health gates on `/health` executed live PASS/SKIP/FAIL; static gates: YAML parse + bash-n 15/15 + zero placeholders + name-mapping + kustomize 22 + offline set-image ×5; ci.yml untouched; zero app code) | resolved | — |
| LATENT-ENV-001 | qr-menu dev defaults assume API port 3001 vs actual default 8000 | OPEN — CONFIRMED 2026-07-30 | Medium | local developer DX only (not the CI/gate) |
| SPEC-DRIFT | documentation drift inventory (§7 row) | OPEN | Low | Sprint-3 documentation baseline |
| RT-ONB-002 | onboarding provisioned a per-tenant `RESTAURANT_OWNER` role with zero `role_permissions` → onboarded owners logged in (200) but 403'd on RBAC-gated staff endpoints (`read Product` / `read Branch` live-verified 2026-07-31); seeded demo users have full permissions but cannot log in (placeholder argon2 hash) | **FIXED+VERIFIED 2026-07-31 (Sprint 2 Task 2, §19 row 30)** — onboarding clones the canonical seeded owner set (22 rows incl. 8 guard-vocabulary additions); staff flow verified end-to-end as an onboarded owner; parity seeded≡onboarded SQL-proven | resolved | — |

---

# 21. Quality Gate

- **Current Sprint:** **Phase 3 — Eleva Builder / Preorder Integration (committed)** — code complete (60db09f) AND **live browser/DB verified 2026-08-10** (§19 row 38: CI `e2e-live` job + live spec, 5/5 PASS). Sprint 1 remains closure-ELIGIBLE (§19 row 23); Sprint 2 Tasks 1–8 all complete and pushed; Sprint 3 Task 1 (AUDIT-004) complete and pushed.
- **Current Step:** Phase 3 live verification COMPLETE (§19 row 38). Build gate repaired in-task (the Phase 3 branch was never production-built: api guard-spec mock fields added, qr-menu `DesignSections` typed, `translateZ(0)` fixed-overlay defect removed — A/B-proven). **Next: `featured/popular` product-selection design decision (§23 item 24).**
- **Critical Defects: 0** — (Resolved: **AUTHZ-002 (Audit v1 C-2) FIXED+VERIFIED 2026-07-31** — §19 row 25; **AUTHZ-001 (Audit v1 C-1) FIXED+VERIFIED 2026-07-31** — §19 row 24; **PREX-TSC-001 FIXED+VERIFIED 2026-07-31** — api tsc 140 → 0 via CAT program 1a–8, §19 rows 12–22; Sprint-1 Step-5 re-run executed 2026-07-31, §19 row 23.)
- **Major Defects: 0** — (Resolved 2026-08-03: **ENV-TSK-0006 FIXED+VERIFIED** — §19 row 36: least-privilege `allowBuilds`, init-migration BOM strip, CI validate `DATABASE_URL`; clean-slate install needs zero manual workarounds and `prisma migrate deploy` runs 8/8.) (Resolved 2026-07-31: **RT-ONB-002 FIXED+VERIFIED** — §19 row 30.) (Resolved 2026-07-30: **PREX-TSC-002 FIXED (CAT-1a)** — §19 row 12; **R3/PREX-MIG-003 FIXED+VERIFIED** — §19 row 8; **R2/PREX-MIG-002 CLOSED as investigation-complete** — §7/§20; **R1/LATENT-KDS-001 FIXED+VERIFIED** — §19 row 7; **R6/RT-GATE-001 FIXED+VERIFIED** — row 9; **R7/RT-ONB-001 FIXED+VERIFIED in-gate during Step 5** — row 10; **PREX-E2E-001 CLOSED** as R7's canary. **Resolved 2026-08-10: Phase 3 build-gate defects** — §19 row 38: api tsc spec-mock fields, qr-menu `DesignSections` `no-explicit-any` ×~24, `translateZ(0)` fixed-overlay defect.)
- **Minor Defects: 3** — LATENT-SUB-001 (runtime disproven 2026-07-30), LATENT-ENV-001, SPEC-DRIFT. (Resolved: LATENT-MEDIA-001 FIXED 2026-07-31 by CAT-8; **LATENT-BRAND-001 RESOLVED+VERIFIED 2026-07-31** — client regenerated + live JSONB branding delivery verified.)
- **Step-5 2026-07-31 re-run measured results (§19 row 23):** ESLint ✅ 6/6 workspaces · suites ✅ **277/2/0** (filter) + ✅ **496/2/0** (root CI command) · TypeScript ✅ api **0** / db **0** / types **0** (PREX-TSC-001/002 eliminated, §19 rows 12–22) · build ✅ **6/6 packages** (per-package + official serialized `turbo run build --concurrency=1`; default-concurrency OOM in the 1.9 GiB sandbox = environment-only) · Docker ⚠ CANNOT VERIFY (no daemon — environment-only; statics unchanged vs upstream) · runtime sanity ✅ (fresh `zayjar_step5`: migrations 5/5, seed 16/16 exact, boot clean, `/health` 200 tenant / 403 apex, guest table 200, guest menu 200, onboarding 201/409/409/403 verbatim-parity) · CI ✅ static (Jobs 1–2 reproduced green locally; Job-3 build chain green, prisma:generate engine-blocked in-sandbox only; Job-4 environment-blocked) · production packaging ✅ static · deployment readiness ✅ static (k8s 26/26 manifests clean) · PROJECT_STATE consistency ✅.
- **Can Current Step Be Closed? ELIGIBLE — the closure decision rests with the CTO** (standing closure-lock: the session does not close Sprint 1 unilaterally and has not done so).
- **Reason (verified evidence only):** the former sole blocker — PREX-TSC-001/002, the TypeScript leg of `turbo build` — is eliminated: api **0** / db **0** / types **0** (CAT program complete, §19 rows 12–22) and the full production build chain passes under official tooling (`turbo run build --concurrency=1` 6/6; §19 row 23, 2026-07-31 re-run). Every remaining non-green cell is environment-only and explicitly evidenced: Docker daemon absent in-sandbox → CI Job 4 authoritative, 4/4 Dockerfiles byte-identical to upstream; default-concurrency `pnpm build` OOM on the 1.9 GiB / 2-core sandbox → serialized official run 6/6 green, per-package 6/6 green; prisma:generate engines absent in-sandbox (ENV-TSK-0006) → available on CI runners and in the Docker builder; LATENT-BRAND-001 reviewed and classified **NOT A PRODUCTION BLOCKER** by the CTO 2026-07-31 (hygiene decision deferred post-gate). No code fix was required in-gate; no CAT-1→CAT-8 work was modified (§19 row 23).

---

# 22. Technical Debt

Quantified, verified debt — nothing speculative:

- **Type errors:** api **0 ✅ — PREX-TSC-001 eliminated** (was 140 — **CAT-5 FIXED 2026-07-30**, §19 row 13: 6 library roots → 13 errors; **Repository correction 2026-08-10:** the Phase 3 branch carried 1 further api tsc error in a spec (`rbac-permission.guard.spec.ts` mock `Order` missing `isPreorder/scheduledAt/preorderStatus`) + ~24 qr-menu `no-explicit-any` in `DesignSections` that broke `next build` — both fixed in §19 row 38 (type-only; the Phase 3 branch had never been production-built); **CAT-6 FIXED 2026-07-30**, §19 row 14: 2 narrowing roots → 10 errors; **CAT-1c FIXED 2026-07-30**, §19 row 15: 5 strictness roots → 7 errors; **CAT-4 FIXED 2026-07-30**, §19 row 16: 5 contract roots → 10 errors; **CAT-3b FIXED 2026-07-30**, §19 row 17: 3 contract roots → 11 errors; **CAT-3a FIXED 2026-07-31**, §19 row 18: 6 drift roots → 23 errors; **CAT-2 FIXED 2026-07-31**, §19 row 19: 5 payload roots → 29 errors; **CAT-1b FIXED 2026-07-31**, §19 row 20: 4 spec roots → 13 errors; **CAT-7 FIXED 2026-07-31**, §19 row 21: 3 spec roots → 17 errors; **CAT-8 FIXED 2026-07-31**, §19 row 22: 4 roots → 7 errors; per-module state in §7 PREX-TSC-001 — all modules 0) + db **0** (was 16 — **CAT-1a FIXED 2026-07-30**, §19 row 12: single `BaseTenantRepository` delegate-interface root produced all 16). Both TypeScript legs of the production build chain are GREEN (db tsc 0, api tsc 0) and the full chain passes under official tooling — `turbo run build --concurrency=1` 6/6 plus per-package 6/6 (§19 row 23, 2026-07-31 Step-5 re-run); the in-sandbox default-concurrency OOM (exit 137) and Docker-daemon absence are environment-only, and CI Job 3 / the Docker builder inherit this green chain outside the sandbox.
- **Tests:** zero failing specs at the Step-5 baseline (277 pass / 2 skip / 0 fail — former PREX-E2E-001 closed as R7's canary, §19 row 10); re-verified 2026-07-31 (§19 row 23): filter script 277/2/0 AND root CI command `pnpm test` **496 pass / 2 skip / 0 fail / 498, 58/59 suites**; 2 skipped cells from DB-gated conditional describes (verified site `media-concurrency.integration.spec.ts:33`; finer itemization CANNOT CONFIRM from aggregate output).
- **Install/runtime workarounds (ENV-TSK-0006):** ~~argon2 node-gyp rebuild after every install; Prisma CLI engines absent (migrations applied via psql; seed via canonical pnpm script); sharp blocked~~ — **ELIMINATED 2026-08-03 (Sprint 2 Task 8, §19 row 36).** `allowBuilds` now approves the Prisma engine trio + `argon2`, so a clean `pnpm install --frozen-lockfile` yields working engines and a source-compiled argon2 binding with **zero manual steps**; `prisma migrate deploy` applies the documented chain 8/8 (M4 still parked per R2) and the canonical seed runs against it. `sharp` never needed its script (prebuilt `@img` package) — audit-corrected. Residual environment-only gap: no Docker daemon in-sandbox (CI Job 4 remains the environment of record).
- **Placeholders/stubs (documented, approved out-of-scope):** none remaining from the pre-Sprint-2 inventory. (Resolved 2026-07-31: `discountAmount = 0.00` placeholder **REPLACED by the discount engine** — §19 row 32; invoice `pdfUrl` fabricated **REPLACED by real PDF generation + storage** — §19 row 33; **Socket.io Redis adapter added** for cross-pod KDS — §19 row 34; **FCM log-only stub REPLACED by real firebase-admin provider** — §19 row 35.)
- **Data-layer chain:** R2/R3 migrations fail on apply → the chain cannot be applied by `prisma migrate deploy` anywhere until they are fixed AND engines are resolvable; M6 fixed (R4 `c15843f`); seed repaired (R5 `084dd37`).
- **Process friction:** `packages/db/scripts/maintenance.sh` exec-bit flip recurs after installs (never committed); local-commit re-creation ritual each session until the user pushes (§6).
- **Tenant-extension join-table constraint (discovered 2026-08-03, AUDIT-004):** the tenant-scoping Prisma extension (`packages/db/src/index.ts`) injects `tenantId` into `data` on every top-level `create` and into `where` on every read for scoped models. Join tables without a `tenantId` column (`UserRole`, `RolePermission`) therefore **cannot** be accessed via their own delegates — `Unknown argument 'tenantId'` at runtime (reproduced live twice during AUDIT-004). Correct pattern, applied in `user.service.ts`: route join-table writes/reads through the tenant-scoped parent model as nested operations. Any future feature touching these tables must follow it.
- **Documentation drift:** SPEC-DRIFT inventory (§7) — README table count/clone URL, KDS room naming, HS256-vs-RS256 (decision-deferred), DOC-003 route coverage, etc.

---

# 23. TODO

Ordered actionable backlog (**user** = decision/approval/push; **agent** = execution after approval):

1. ~~[user] Approve and scope Sprint 1 Step 5~~ **EXECUTED 2026-07-30** (§19 row 11). **[user] SUPERSEDED 2026-07-31:** PREX-TSC-001/002 eliminated by the CAT program (item 6) and Step 5 RE-EXECUTED under CTO execution command (§19 row 23) — all attainable criteria GREEN, zero in-gate code changes, no new defects. (PREX-E2E-001 resolved in-gate as R7's canary.) **[user] Remaining: Sprint-1 closure decision (CTO authority) + the user-owned push (item 8).**
2. ~~[user] Fix-scope decisions: PREX-MIG-002 (R2), PREX-MIG-003 (R3)~~ **RESOLVED 2026-07-30** — R3/PREX-MIG-003 FIXED+VERIFIED under autonomous-execution mandate (§19 row 8); R2/PREX-MIG-002 CLOSED as investigation-complete (CTO decision; R2.1 architecture analysis — M4 parked, F indexes-only recommended).
3. [user] `paymentMethod` persistence decision (§15 item 3): add `Order.paymentMethod` column or trim DTO/UI.
4. [user] RS256 migration timing (deferred S0-T01; no code change made).
5. ~~[user] Guest-checkout subscription-gating parity~~ **RESOLVED 2026-07-30 — R6 FIXED+VERIFIED** (§19 row 9: checkout gate uniform with reads; suites 276/2/1 new baseline).
6. ~~[user] Build-gate remediation ownership~~ **IN PROGRESS 2026-07-30 under CTO mandate** — category-by-category program: CAT-1a complete (db 16 → 0, §19 row 12); CAT-5 complete (api 140 → 127, §19 row 13); CAT-6 complete (api 127 → 117, §19 row 14); CAT-1c complete (api 117 → 110, §19 row 15); CAT-4 complete (api 110 → 100, §19 row 16); CAT-3b complete (api 100 → 89, §19 row 17); CAT-3a complete (api 89 → 66, §19 row 18); CAT-2 complete (api 66 → 37, §19 row 19); CAT-1b complete (api 37 → 24, §19 row 20); CAT-7 complete (api 24 → 7, §19 row 21); CAT-8 complete (api 7 → 0, §19 row 22); **CAT program COMPLETE — PREX-TSC-001 eliminated (api 0, db 0).**
7. [user] Canonical local API port decision (LATENT-ENV-001): 8000 vs 3001.
8. [user] **Push** local commit chain `d1c6035` … `bf56b7c` (R6) → R7 `64e8c29` → Step-5 docs commit (§6 lineage) to `origin` (user-owned; the session has pushed nothing, ever).
9. [agent] After approvals: execute approved fixes in isolated single commits with before/after verification (the R4/R5 pattern).
10. ~~[user] Define Sprint-2 scope~~ **DEFINED 2026-07-31 by CTO** — staff-experience gaps (see §17): **Task 1 (Staff Authentication UI) IMPLEMENTED + VERIFIED, awaiting approval to commit + push** (§19 row 29); Task 2 = item 11 below.
11. ~~[user→agent] RT-ONB-002 fix (Sprint 2 Task 2)~~ **EXECUTED 2026-07-31 (§19 row 30)** — onboarding clones the canonical seeded RESTAURANT_OWNER permission set (22 rows incl. 8 guard-vocabulary additions); fail-fast on missing baseline; staff flow verified end-to-end as an onboarded owner; parity SQL-proven. **Awaiting CTO approval to commit.**
12. ~~[agent] After approval of Sprint 2 Task 1: execute the approved commit per the §28 procedure.~~ **Task 1 COMMITTED 2026-07-31 as `236d52e`** (approved by CTO 2026-07-31); **push pending — user-owned (GitHub Desktop / PUSH APPROVED)**.
13. [agent] After approval of Sprint 2 Task 2: create ONE isolated commit (this task's 5 files incl. PROJECT_STATE.md records) per §28; push is user-owned.
14. [user] Optional follow-up backlog (not this task's scope, documented §7/§14): (a) seeded demo users' placeholder argon2 password hashes (`seed.ts:298`) — no seeded user can actually log in; (b) migrate pre-fix onboarded tenants' permission-less roles via the same clone mechanism; (c) legacy seed permission vocabulary (`menu`/`kds`/`customer`/`billing`/`analytics`/`write`) remains CASL-dead — retained for backward compatibility; (d) manager/cashier/kitchen roles' effective abilities still need vocabulary alignment (their `write` rows are dead).
15. ~~[agent] Order.paymentMethod persistence (Sprint 2 Task 3 — §15 item 3)~~ **EXECUTED 2026-07-31 (§19 row 31)** — column added (nullable, no backfill), migration M7, service persists `dto.paymentMethod`; staff + guest checkouts live-verified; **awaiting CTO approval to commit**.
16. ~~[agent] Discount Engine (Sprint 2 Task 4)~~ **EXECUTED 2026-07-31 (§19 row 32)** — real tenant-scoped discount engine (model + pure service + DTO + atomic checkout application + 17 tests + migration) replaces the `discountAmount = 0.00` placeholder; runtime-verified incl. cross-tenant isolation. **Awaiting CTO approval to commit + push.**
17. ~~[agent] Real Invoice PDF (Sprint 2 Task 5)~~ **EXECUTED 2026-07-31 (§19 row 33)** — real PDF generation (pdfkit) + storage (StorageProvider local/S3) + real data resolution + static serving; runtime-verified (downloadable valid PDF). **Awaiting CTO approval to commit + push.**
18. ~~[agent] Socket.io Redis Adapter (Sprint 2 Task 6)~~ **EXECUTED 2026-07-31 (§19 row 34)** — Redis adapter attached in KDS gateway (`@socket.io/redis-adapter`, REDIS_URL/SOCKET_IO_REDIS_URL, graceful fallback); cross-pod broadcast runtime-proven. **Awaiting CTO approval to commit + push.**
19. ~~[agent] Firebase Cloud Messaging (Sprint 2 Task 7)~~ **EXECUTED 2026-07-31 (§19 row 35)** — real FCM provider (`firebase-admin`), real send path with unregistered-token pruning + in-app notification persistence, log-only fallback preserved; notifications table FK/relation + `TenantNotificationRepository` added (migration M9). **Awaiting CTO approval to commit + push.**
20. ~~[agent] ENV-TSK-0006 install/provisioning toolchain (Sprint 2 Task 8)~~ **EXECUTED + COMMITTED + PUSHED 2026-08-03 (§19 row 36)** — least-privilege `allowBuilds` (Prisma engine trio + argon2), init-migration UTF-8 BOM stripped, CI Job-3 `prisma validate` given a connection-free `DATABASE_URL`. Clean-slate install needs **zero manual workarounds**; `prisma migrate deploy` 8/8 on fresh PG 17.10; canonical seed 17/17; live argon2 login 200 / 401, guest checkout 201. **The last High-priority open defect is closed.**
21. **[user/CTO] Pre-audit decision backlog** (unchanged; not engineering tasks): (a) **Sprint-1 closure decision** (standing closure-lock, §21 — all attainable gate criteria green since §19 row 23); (b) **R2/PREX-MIG-002 architecture RFC** — M4 `orders_partitioning_by_year` stays parked non-shippable; R2.1 ranked recommendation **F (no partitioning, indexes only)**, fallback **D (`HASH("id")`)**; (c) **LATENT-ENV-001 canonical dev-port decision** — 8000 (`main.ts` default, compose, `.env.example`) vs 3001 (qr-menu dev defaults); (d) **RS256-vs-HS256** migration timing (deferred S0-T01); (e) **SPEC-DRIFT documentation baseline** (Sprint-3 scope); (f) optional follow-ups in item 14 (seeded demo users' placeholder argon2 hashes — note these are now *testable* since argon2 works natively; migrating pre-fix onboarded tenants; legacy permission-vocabulary alignment).
22. ~~[agent] AUDIT-004 — Staff User Management (Sprint 3 Task 1)~~ **EXECUTED + COMMITTED + PUSHED 2026-08-03 (§19 row 37)** — full CRUD + role assignment + branch assignment; `UserBranch` model/migration M10 gives DOC-005 §4.2 branch scoping its first persistent source; 4 `user:*` seed permissions activate the pre-existing `User` CASL subject (owner-only). Live-proven: a created staff user can log in — the capability the platform previously lacked entirely.
23. **[user/CTO] CTO Engineering Audit backlog (2026-08-03, `CTO_AUDIT_2026-08-03.md` + `CTO_AUDIT_EVIDENCE.md`)** — 25 findings; production readiness measured **≈55%**. Remaining P0 after Task 1: **AUDIT-001** (no CSS framework exists — entire UI unstyled, 4 exhaustive searches empty), **AUDIT-002** (payment verification bypass: `verifyPayment` returns hardcoded `verified:true`; Tap mocked even with a live key), **AUDIT-005** (no password reset / email verification — `sendPasswordResetEmail` exists but is never called and the schema has no token fields). P1: AUDIT-006/007 (menu+branch create-only: the whole API has just 6 mutating routes), AUDIT-009/010/011/012/016/017/020. Retracted by evidence: AUDIT-003 downgraded Critical→Low (password check blocks the mock-user path; prod hard-exits without `DATABASE_URL`), AUDIT-013/018 reclassified out-of-scope (absent from every spec document). **Each item requires explicit CTO approval before implementation.**
24. ~~[agent] Phase 3 live browser/DB verification in CI~~ **EXECUTED + COMMITTED 2026-08-10 (§19 row 38)** — `e2e-live` CI job, `playwright.live.config.ts`, `tests/e2e/verify-phase3-live.spec.ts` (5/5 PASS on the identical native stack; DB + Chromium evidence recorded). **Next: [CTO] `featured`/`popular` product-selection design decision** — `DesignSections` currently uses placeholder `flatProducts.slice(...)` (no `Product.isFeatured` — explicitly not invented); options: (a) `Product.isFeatured` boolean + seed/design-builder control, (b) section-level explicit product selection stored in the published design JSON, (c) keep slice-placeholder and document it as curated-by-position. After the decision: implement + live-verify via the new e2e-live path.

---

# 24. End of Session Procedure

Every future session MUST end with all of the following, in order:

1. Update PROJECT_STATE.md.
2. Update Sprint.
3. Update Step.
4. Update Verification History (§19).
5. Update Risks (§16).
6. Update Technical Debt (§22).
7. Update TODO (§23).
8. Update Open Blocking Defects (§20).
9. Update Quality Gate (§21).
10. Commit.
11. Push if GitHub is available.

If GitHub is unavailable — or pushing is not the session's to perform (§16 policy) — state clearly:

**"Push not performed."**

Never claim that push happened without verification (verifiable push output or remote ref match required).

---

# 25. Session Recovery

How any new AI (or human) continues this project using ONLY this document, the repository, and commit history:

1. **Locate:** repository `/home/user/zayjar-specification`; canonical doc = repo-root `PROJECT_STATE.md` (mirror `~/PROJECT_STATE.md`, byte-synced at every doc commit — verify with `sha256sum`).
2. **Read in this order:** §6 (current position) → §19 (what is proven) → §20/§21 (what blocks) → §7 (defect detail) → §15/§23 (what's next) → §16/§22 (risk and debt) → §14 (decisions) → §18 (toolchain survival playbook) → §13 (green baselines and exact commands).
3. **Verify before believing:** `git log --oneline`; compare the HEAD chain against the hashes recorded in §5/§19. If sprint or fix commits are missing (sandbox wipes local commits, §6): re-create them from the intact working tree using the messages and stats recorded in §5/§19 — file contents, not commit objects, are the durable identity until the user pushes.
4. **Re-establish the toolchain per §18:** pnpm via `npx -y pnpm@10`; per-package `./node_modules/.bin/tsc` (never bare `npx tsc` — downloads a fake package); argon2 node-gyp rebuild; build order `packages/types` → `packages/db` (expect exactly 16 errors) → `node scripts/copy-generated-client.js`.
5. **Run the §13 baseline commands and match the recorded numbers before any modification.** Any baseline deviation means *you* introduced it — stop and revert.
6. **Runtime work** follows the native-runtime recipe (PostgreSQL/Redis via apt, migrations applied via psql, fixture `/home/user/e2e/fixture.sql` with its documented corrections; Step-4 evidence under `/home/user/e2e/`).
7. **Proceed only within user-gated steps** (§26 CTO Rules; §23 TODO ordering). If any claim in this document cannot be reproduced from the repository, treat the repository as truth and record the correction (§19 footer pattern).

---

# 26. CTO Rules

Permanent. They apply to every contributor, human or AI, without exception:

1. Never guess.
2. If something cannot be verified, write **CANNOT CONFIRM**.
3. Never change architecture without documenting it (record in §14).
4. Never delete information without documented justification.
5. Never close a Step before verification.
6. Every approved decision must be recorded (§14 / §19).
7. Repository reality overrides documentation — when they conflict, fix the document to match the repository and log the correction (§19 footer).

---

# 27. Future Architecture

The following files will be created **only after MVP completion** (the formal MVP scope definition: CANNOT CONFIRM from the repository; to be declared by the user/CTO):

- `AGENTS.md`
- `JARVIS.md`
- `CTO_PLAYBOOK.md`

They are intentionally NOT created now. Until then, this PROJECT_STATE.md remains the single source of truth.

---

# 28. Session Completion Policy

**Permanent rule — no future session may be considered complete until:**

- PROJECT_STATE.md is updated (per the §24 procedure, items 1–9), AND
- a Commit is created, AND
- a Push is performed if available — otherwise an explicit "**Push not performed.**" statement (§24 / §16).

---

# 29. Last Completed Work

> Session of **2026-08-04** (Asia/Dubai). Commit `dee3527` on `main`.
> Every defect below was **reproduced at runtime before any code was changed**
> and re-verified after. Nothing here was fixed on suspicion.

## 29.1 Completed modules

| Area | Module | State |
|---|---|---|
| P0 | **AUDIT-001** — UI/CSS pipeline (qr-menu, backoffice, cashier) | ✅ Complete |
| P0 | **AUDIT-002** — Real Tap/Stripe payment implementation | ✅ Complete |
| P0 | **AUDIT-006/007** — Products/Categories/Branches/Tables CRUD (soft-delete only) | ✅ Complete |
| Security | **Customer module** — guards + full CRUD + soft delete/restore | ✅ Complete |
| Security | **CSRF enforcement** across all 51 mutating routes | ✅ Complete |
| API | **Restaurant read endpoints** (`GET /restaurants`, `/restaurants/:id`) | ✅ Complete (reads only; writes are AUDIT-008) |
| Phase 2 | **Backoffice — Products UI** | ✅ Production-ready |
| Phase 2 | **Backoffice — Categories UI** | ✅ Production-ready |
| Phase 2 | **Backoffice — Branches UI** | ✅ Production-ready (full CRUD, 409 guard, cascade to tables, validation + unit tests) |
| Phase 2 | **Backoffice — Tables UI** | ✅ Production-ready + **RUNTIME VERIFIED** (full CRUD: Create/Edit/Archive/Restore in real Chromium; branchId + number immutable (QR HMAC); QR token display + copyable; 409 on orders-in-progress; seatingCapacity + status only updatable; validation + unit tests; DB inserts + soft-delete/restore confirmed) |
| Phase 2 | Backoffice — Customers UI | ✅ Production-ready + **RUNTIME VERIFIED** (full CRUD: Create/Edit/Archive/Restore in real Chromium; search by name/email/phone; loyalty points editable on update; customer-validation.spec 20/20; DB inserts + soft-delete/restore confirmed; auth 401 unauthenticated / 403 cross-tenant confirmed) |
| Phase 2 | Backoffice — Staff UI | ✅ Production-ready + **RUNTIME VERIFIED** (full CRUD: Create/Edit/Delete in real Chromium; role checkbox selector; branch checkbox selector; password field; isActive toggle; soft-delete confirmed; staff-validation.spec 20/20; DB soft-delete confirmed) |

**12 CRUD endpoints** added in P0 (4 update, 4 soft-delete, 4 restore) plus
**6 customer endpoints** and **2 restaurant endpoints**. There is **no
hard-delete endpoint anywhere** — order history is preserved per DOC-002 §638.

## 29.2 Fixed defects

| ID | Severity | Defect | Runtime proof (before → after) |
|---|---|---|---|
| **H** | 🔴 Critical | `CustomerController` had **no `@UseGuards`**; no global auth guard exists (only `CsrfGuard` is an `APP_GUARD`). The whole customer PII table was world-readable | `curl /api/v1/customers` with no `Authorization` → **200** with names/emails/loyalty → **401** |
| **I** | 🔴 Critical | `CsrfGuard` is an `APP_GUARD`, so Nest ran it **before** `JwtAuthGuard`; `request.user` was always undefined → `if (!user?.id) return true` bypass on **all 51** mutating routes. Its unit tests passed only because they inject `user` by hand | `X-CSRF-Token: TOTALLY-BOGUS-VALUE-123` → **200** → **403** |
| **K** | 🔴 Critical | CORS dev fallback was `origin:'*'` with no credentials → **every** browser call failed at preflight. Invisible to curl, which does not enforce CORS | all SPA fetches `net::ERR_FAILED` → **200**; production now **fails closed** if `CORS_ORIGIN` is unset |
| **G** | 🟠 High | `createOrder` validated `branchId` and every `productId` but wrote client-supplied `tableId` with **no lookup** — defeating `DELETE /tables/:id` | checkout on a soft-deleted table → **201** → **404**; cross-branch table → **400**; cross-tenant → **404** |
| **D** | 🟠 High | `idx_tables_qr_token` was a **full** unique index while the QR token is a deterministic HMAC of `tenantId:branchId:number`, so archiving table "12" permanently burned that number. DOC-002 §234/§602 mandate **partial** indexes | `duplicate key … idx_tables_qr_token` → number reusable; 5 indexes rebuilt `WHERE deleted_at IS NULL` |
| **C** | 🟠 High | Owner JWT carried no `*:update` / `*:delete` and no `category:*` at all — the new routes would have 403'd for every caller including the owner | 17 permission rows granted; owner token verified |
| **L** | 🟠 High | `POST /menu/categories` and `POST /branches` both require `restaurantId`, but **no endpoint exposed one** — neither form could ever be submitted | `GET /restaurants` **404** → **200** |
| **M** | 🟡 Medium | 3 routes leaked `Inconsistent column data: Error creating UUID` as HTTP 500 | `/tenants/:id`, `/webhooks/:id`, `/device-tokens/:id` **500** → **400** |
| **N** | 🟡 Medium | Foreign `restaurantId` leaked a raw FK violation | **500** → **404** (fixed in `createCategory` *and* `createBranch`) |
| **J** | 🟡 Medium | The 5 restore endpoints were **unreachable from any client** — every list filters `deletedAt IS NULL`, so no UI could obtain an archived id | added opt-in `?includeDeleted=true` |
| **J(b)** | 🟡 Medium | Declaring the flag `boolean` let the global `ValidationPipe` coerce it before the param pipe ran (`"1"`→false, `"yes"`→false, never 400) | declared `string` + `BooleanQueryPipe`; `1`→true, `yes`→**400** |

**Self-inflicted, caught by my own verification (not shipped):**
`DEFECT-E` — the RBAC guard resolves `:id` via `findById`, which filters exactly
the rows a restore targets, so every restore endpoint 404'd (fixed with
`@IncludeSoftDeleted()`). `DEFECT-F` — the cascade used `updateMany`, which the
tenant-scoped Prisma extension blocks → HTTP 500 (rewritten as batched per-row
updates in one transaction). Also a boot-time DI failure (`CustomerModule`
missing `AuthModule`) and a wrong CASL subject choice for `/restaurants/:id`
that made the guard search the **branches** table.

## 29.3 Runtime verification

All against a real API process, real PostgreSQL 17 and real Redis; browser
checks in real Chromium against the live stack. **No mocks in any runtime test.**

- **Tenant isolation** — cross-tenant `PUT`/`DELETE`/`RESTORE` on products,
  categories, branches, tables, customers and restaurants: **404 on every
  probe**, target rows verified unmodified (404 not 403 — no existence oracle).
- **Order history preservation** — archiving a product with sales history keeps
  `order_items` intact and the historical join still resolves the product name.
- **Cascade truthfulness** — category with 2 products → archive → 0 live / 2
  archived → restore category → still 0 live (as the dialog promises) → restore
  each product → 2 live.
- **Concurrency** — 5 simultaneous DELETEs → one 200 + four 404, **one
  tombstone, zero orphans**; the tombstone is never rewritten by a retry.
- **Guest surface** — soft-deleted products vanish from the public menu and
  reappear on restore; a deleted table's QR stops resolving, as does every table
  under a deleted branch.
- **Migrations** — full chain applies cleanly on a fresh database (M4 excluded
  per the standing R2/PREX-MIG-002 parking).

## 29.4 Tests passed

```
TypeScript   types 0 / db 0 / api 0 / backoffice 0
ESLint       6/6 workspaces clean
Jest         906 passed, 0 skipped, 0 failed     (baseline 613 → +293)
Build        turbo run build --concurrency=1 → 6/6
Browser E2E  Products   19/19      Categories 21/21
Adversarial  Products    7/7       Categories 13/13
```

Adversarial coverage: RBAC bypass, cross-tenant access, forged CSRF, stored XSS
(neither executed nor stored — 0 raw payloads in the database), malformed and
boundary input, field smuggling (`tenantId`/`deletedAt`/`id` all rejected by
`forbidNonWhitelisted`), re-parenting, concurrency and expired tokens.

## 29.5 Remaining work

**Phase 2 (AUDIT-014) — in progress**

| # | Module | Notes |
|---|---|---|
| 3 | **Branches** UI | 409 when orders are in progress; cascades to tables |
| 4 | **Tables** UI | `branchId` + `number` immutable (QR HMAC); surface the QR token |
| 5 | **Customers** UI | ✅ COMPLETE (full CRUD + runtime/browser/DB verified) |
| 6 | **Staff users** UI | ✅ COMPLETE (full CRUD + role/branch selectors + soft-delete + runtime/browser/DB verified) |
| — | ~~Remove `AdminPanel.tsx`~~ | DONE — deleted; `/kds` uses `KDSTerminal` directly |
| — | Full-app adversarial pass | once every module is wired |

**P1 backlog** — AUDIT-005 (no password reset / email verification),
AUDIT-020 (no Helmet), AUDIT-012 (E2E fully API-mocked and excluded from CI; no
DB service in the `test` job — nothing would have caught DEFECT-G),
AUDIT-011 (no OpenAPI; ~21+ routes undocumented), AUDIT-023 (no `/metrics`, no
readiness/liveness split), plus two new items: a **CI guard asserting the 5
partial indexes are not reverted** by a future `prisma migrate dev`, and a
**product decision on staff-role menu permissions** (currently owner-only).

**P2 backlog** — AUDIT-008 (no restaurant write module), AUDIT-009, AUDIT-010,
AUDIT-013, AUDIT-015, AUDIT-017, AUDIT-021, AUDIT-022, AUDIT-024, AUDIT-025.

### ⚠️ Deployment caution

Migration `20260804000000` rebuilds 5 unique indexes as partial. Index rebuilds
take a brief **exclusive lock** — run in a maintenance window, or convert to
`CREATE INDEX CONCURRENTLY` in a transaction-less migration for large tables.
Prisma cannot express partial indexes, so `schema.prisma` still shows plain
`@@unique`; a future `prisma migrate dev` could try to revert this fix.

## 29.6 Next recommended task

Phase 2 stabilization complete (commit below). All six modules verified clean.
Next: full-app adversarial pass across all modules, then P1 backlog items.
