apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  namespace: zayjar
  labels:
    app.kubernetes.io/name: pgbouncer
    app.kubernetes.io/part-of: zayjar
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pgbouncer
  ports:
    - port: 6432
      targetPort: pgbouncer
      protocol: TCP
      name: pgbouncer
